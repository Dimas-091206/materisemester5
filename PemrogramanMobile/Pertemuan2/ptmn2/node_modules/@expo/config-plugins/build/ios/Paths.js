"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
0 && (module.exports = {
    findSchemeNames: null,
    findSchemePaths: null,
    getAllEntitlementsPaths: null,
    getAllInfoPlistPaths: null,
    getAllPBXProjectPaths: null,
    getAllXcodeProjectPaths: null,
    getAppDelegate: null,
    getAppDelegateFilePath: null,
    getAppDelegateHeaderFilePath: null,
    getAppDelegateObjcHeaderFilePath: null,
    getEntitlementsPath: null,
    getExpoPlistPath: null,
    getFileInfo: null,
    getInfoPlistPath: null,
    getPBXProjectPath: null,
    getPodfilePath: null,
    getSourceRoot: null,
    getSupportingPath: null,
    getXcodeProjectPath: null
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: Object.getOwnPropertyDescriptor(all, name).get
    });
}
_export(exports, {
    get findSchemeNames () {
        return findSchemeNames;
    },
    get findSchemePaths () {
        return findSchemePaths;
    },
    get getAllEntitlementsPaths () {
        return getAllEntitlementsPaths;
    },
    get getAllInfoPlistPaths () {
        return getAllInfoPlistPaths;
    },
    get getAllPBXProjectPaths () {
        return getAllPBXProjectPaths;
    },
    get getAllXcodeProjectPaths () {
        return getAllXcodeProjectPaths;
    },
    get getAppDelegate () {
        return getAppDelegate;
    },
    get getAppDelegateFilePath () {
        return getAppDelegateFilePath;
    },
    get getAppDelegateHeaderFilePath () {
        return getAppDelegateHeaderFilePath;
    },
    get getAppDelegateObjcHeaderFilePath () {
        return getAppDelegateObjcHeaderFilePath;
    },
    get getEntitlementsPath () {
        return getEntitlementsPath;
    },
    get getExpoPlistPath () {
        return getExpoPlistPath;
    },
    get getFileInfo () {
        return getFileInfo;
    },
    get getInfoPlistPath () {
        return getInfoPlistPath;
    },
    get getPBXProjectPath () {
        return getPBXProjectPath;
    },
    get getPodfilePath () {
        return getPodfilePath;
    },
    get getSourceRoot () {
        return getSourceRoot;
    },
    get getSupportingPath () {
        return getSupportingPath;
    },
    get getXcodeProjectPath () {
        return getXcodeProjectPath;
    }
});
function _fs() {
    const data = require("fs");
    _fs = function() {
        return data;
    };
    return data;
}
function _glob() {
    const data = require("glob");
    _glob = function() {
        return data;
    };
    return data;
}
function _path() {
    const data = /*#__PURE__*/ _interop_require_wildcard(require("path"));
    _path = function() {
        return data;
    };
    return data;
}
const _errors = require("../utils/errors");
const _glob1 = require("../utils/glob");
const _warnings = require("../utils/warnings");
const _Entitlements = /*#__PURE__*/ _interop_require_wildcard(require("./Entitlements"));
function _getRequireWildcardCache(nodeInterop) {
    if (typeof WeakMap !== "function") return null;
    var cacheBabelInterop = new WeakMap();
    var cacheNodeInterop = new WeakMap();
    return (_getRequireWildcardCache = function(nodeInterop) {
        return nodeInterop ? cacheNodeInterop : cacheBabelInterop;
    })(nodeInterop);
}
function _interop_require_wildcard(obj, nodeInterop) {
    if (!nodeInterop && obj && obj.__esModule) {
        return obj;
    }
    if (obj === null || typeof obj !== "object" && typeof obj !== "function") {
        return {
            default: obj
        };
    }
    var cache = _getRequireWildcardCache(nodeInterop);
    if (cache && cache.has(obj)) {
        return cache.get(obj);
    }
    var newObj = {
        __proto__: null
    };
    var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
    for(var key in obj){
        if (key !== "default" && Object.prototype.hasOwnProperty.call(obj, key)) {
            var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
            if (desc && (desc.get || desc.set)) {
                Object.defineProperty(newObj, key, desc);
            } else {
                newObj[key] = obj[key];
            }
        }
    }
    newObj.default = obj;
    if (cache) {
        cache.set(obj, newObj);
    }
    return newObj;
}
const ignoredPaths = [
    '**/@(Carthage|Pods|vendor|node_modules)/**'
];
function getAppDelegateHeaderFilePath(projectRoot) {
    const [using, ...extra] = (0, _glob1.withSortedGlobResult)((0, _glob().globSync)('ios/*/AppDelegate.h', {
        absolute: true,
        cwd: projectRoot,
        ignore: ignoredPaths
    }));
    if (!using) {
        throw new _errors.UnexpectedError(`Could not locate a valid AppDelegate header at root: "${projectRoot}"`);
    }
    if (extra.length) {
        warnMultipleFiles({
            tag: 'app-delegate-header',
            fileName: 'AppDelegate',
            projectRoot,
            using,
            extra
        });
    }
    return using;
}
function getAppDelegateFilePath(projectRoot) {
    const [using, ...extra] = (0, _glob1.withSortedGlobResult)((0, _glob().globSync)('ios/*/AppDelegate.@(m|mm|swift)', {
        absolute: true,
        cwd: projectRoot,
        ignore: ignoredPaths
    }));
    if (!using) {
        throw new _errors.UnexpectedError(`Could not locate a valid AppDelegate at root: "${projectRoot}"`);
    }
    if (extra.length) {
        warnMultipleFiles({
            tag: 'app-delegate',
            fileName: 'AppDelegate',
            projectRoot,
            using,
            extra
        });
    }
    return using;
}
function getAppDelegateObjcHeaderFilePath(projectRoot) {
    const [using, ...extra] = (0, _glob1.withSortedGlobResult)((0, _glob().globSync)('ios/*/AppDelegate.h', {
        absolute: true,
        cwd: projectRoot,
        ignore: ignoredPaths
    }));
    if (!using) {
        throw new _errors.UnexpectedError(`Could not locate a valid AppDelegate.h at root: "${projectRoot}"`);
    }
    if (extra.length) {
        warnMultipleFiles({
            tag: 'app-delegate-objc-header',
            fileName: 'AppDelegate.h',
            projectRoot,
            using,
            extra
        });
    }
    return using;
}
function getPodfilePath(projectRoot) {
    const [using, ...extra] = (0, _glob1.withSortedGlobResult)((0, _glob().globSync)('ios/Podfile', {
        absolute: true,
        cwd: projectRoot,
        ignore: ignoredPaths
    }));
    if (!using) {
        throw new _errors.UnexpectedError(`Could not locate a valid Podfile at root: "${projectRoot}"`);
    }
    if (extra.length) {
        warnMultipleFiles({
            tag: 'podfile',
            fileName: 'Podfile',
            projectRoot,
            using,
            extra
        });
    }
    return using;
}
function getLanguage(filePath) {
    const extension = _path().extname(filePath);
    if (!extension && _path().basename(filePath) === 'Podfile') {
        return 'rb';
    }
    switch(extension){
        case '.mm':
            return 'objcpp';
        case '.m':
        case '.h':
            return 'objc';
        case '.swift':
            return 'swift';
        default:
            throw new _errors.UnexpectedError(`Unexpected iOS file extension: ${extension}`);
    }
}
function getFileInfo(filePath) {
    return {
        path: _path().normalize(filePath),
        contents: (0, _fs().readFileSync)(filePath, 'utf8'),
        language: getLanguage(filePath)
    };
}
function getAppDelegate(projectRoot) {
    const filePath = getAppDelegateFilePath(projectRoot);
    return getFileInfo(filePath);
}
function getSourceRoot(projectRoot) {
    const appDelegate = getAppDelegate(projectRoot);
    return _path().dirname(appDelegate.path);
}
function findSchemePaths(projectRoot) {
    return (0, _glob1.withSortedGlobResult)((0, _glob().globSync)('ios/*.xcodeproj/xcshareddata/xcschemes/*.xcscheme', {
        absolute: true,
        cwd: projectRoot,
        ignore: ignoredPaths
    }));
}
function findSchemeNames(projectRoot) {
    const schemePaths = findSchemePaths(projectRoot);
    return schemePaths.map((schemePath)=>_path().parse(schemePath).name);
}
function getAllXcodeProjectPaths(projectRoot) {
    const iosFolder = 'ios';
    const pbxprojPaths = (0, _glob1.withSortedGlobResult)((0, _glob().globSync)('ios/**/*.xcodeproj', {
        cwd: projectRoot,
        ignore: ignoredPaths
    })// Drop leading `/` from glob results to mimic glob@<9 behavior
    .map((filePath)=>filePath.replace(/^\//, '')).filter((project)=>!/test|example|sample/i.test(project) || _path().dirname(project) === iosFolder)).sort((a, b)=>{
        const isAInIos = _path().dirname(a) === iosFolder;
        const isBInIos = _path().dirname(b) === iosFolder;
        // preserve previous sort order
        if (isAInIos && isBInIos || !isAInIos && !isBInIos) {
            return 0;
        }
        return isAInIos ? -1 : 1;
    });
    if (!pbxprojPaths.length) {
        throw new _errors.UnexpectedError(`Failed to locate the ios/*.xcodeproj files relative to path "${projectRoot}".`);
    }
    return pbxprojPaths.map((value)=>_path().join(projectRoot, value));
}
function getXcodeProjectPath(projectRoot) {
    const [using = '', ...extra] = getAllXcodeProjectPaths(projectRoot);
    if (extra.length) {
        warnMultipleFiles({
            tag: 'xcodeproj',
            fileName: '*.xcodeproj',
            projectRoot,
            using,
            extra
        });
    }
    return using;
}
function getAllPBXProjectPaths(projectRoot) {
    const projectPaths = getAllXcodeProjectPaths(projectRoot);
    const paths = projectPaths.map((value)=>_path().join(value, 'project.pbxproj')).filter((value)=>(0, _fs().existsSync)(value));
    if (!paths.length) {
        throw new _errors.UnexpectedError(`Failed to locate the ios/*.xcodeproj/project.pbxproj files relative to path "${projectRoot}".`);
    }
    return paths;
}
function getPBXProjectPath(projectRoot) {
    const [using = '', ...extra] = getAllPBXProjectPaths(projectRoot);
    if (extra.length) {
        warnMultipleFiles({
            tag: 'project-pbxproj',
            fileName: 'project.pbxproj',
            projectRoot,
            using,
            extra
        });
    }
    return using;
}
function getAllInfoPlistPaths(projectRoot) {
    const paths = (0, _glob1.withSortedGlobResult)((0, _glob().globSync)('ios/*/Info.plist', {
        absolute: true,
        cwd: projectRoot,
        ignore: ignoredPaths
    })).sort(// longer name means more suffixes, we want the shortest possible one to be first.
    (a, b)=>a.length - b.length);
    if (!paths.length) {
        throw new _errors.UnexpectedError(`Failed to locate Info.plist files relative to path "${projectRoot}".`);
    }
    return paths;
}
function getInfoPlistPath(projectRoot) {
    const [using = '', ...extra] = getAllInfoPlistPaths(projectRoot);
    if (extra.length) {
        warnMultipleFiles({
            tag: 'info-plist',
            fileName: 'Info.plist',
            projectRoot,
            using,
            extra
        });
    }
    return using;
}
function getAllEntitlementsPaths(projectRoot) {
    const paths = (0, _glob().globSync)('ios/*/*.entitlements', {
        absolute: true,
        cwd: projectRoot,
        ignore: ignoredPaths
    });
    return paths;
}
function getEntitlementsPath(projectRoot) {
    return _Entitlements.getEntitlementsPath(projectRoot);
}
function getSupportingPath(projectRoot) {
    return _path().resolve(projectRoot, 'ios', _path().basename(getSourceRoot(projectRoot)), 'Supporting');
}
function getExpoPlistPath(projectRoot) {
    const supportingPath = getSupportingPath(projectRoot);
    return _path().join(supportingPath, 'Expo.plist');
}
function warnMultipleFiles({ tag, fileName, projectRoot, using, extra }) {
    const usingPath = projectRoot ? _path().relative(projectRoot, using) : using;
    const extraPaths = projectRoot ? extra.map((v)=>_path().relative(projectRoot, v)) : extra;
    (0, _warnings.addWarningIOS)(`paths-${tag}`, `Found multiple ${fileName} file paths, using "${usingPath}". Ignored paths: ${JSON.stringify(extraPaths)}`);
}

//# sourceMappingURL=Paths.js.map
