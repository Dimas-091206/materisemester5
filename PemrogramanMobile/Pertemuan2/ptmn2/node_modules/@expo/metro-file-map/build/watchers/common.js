/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * Originally vendored from
 * https://github.com/amasad/sane/blob/64ff3a870c42e84f744086884bf55a4f9c22d376/src/common.js
 */ "use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
0 && (module.exports = {
    ALL_EVENT: null,
    DELETE_EVENT: null,
    RECRAWL_EVENT: null,
    TOUCH_EVENT: null,
    includedByGlob: null,
    posixPathMatchesPattern: null,
    typeFromStat: null
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: Object.getOwnPropertyDescriptor(all, name).get
    });
}
_export(exports, {
    get ALL_EVENT () {
        return ALL_EVENT;
    },
    get DELETE_EVENT () {
        return DELETE_EVENT;
    },
    get RECRAWL_EVENT () {
        return RECRAWL_EVENT;
    },
    get TOUCH_EVENT () {
        return TOUCH_EVENT;
    },
    get includedByGlob () {
        return includedByGlob;
    },
    get posixPathMatchesPattern () {
        return posixPathMatchesPattern;
    },
    get typeFromStat () {
        return typeFromStat;
    }
});
function _micromatch() {
    const data = /*#__PURE__*/ _interop_require_default(require("micromatch"));
    _micromatch = function() {
        return data;
    };
    return data;
}
function _path() {
    const data = /*#__PURE__*/ _interop_require_default(require("path"));
    _path = function() {
        return data;
    };
    return data;
}
function _interop_require_default(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
const DELETE_EVENT = 'delete';
const TOUCH_EVENT = 'touch';
const RECRAWL_EVENT = 'recrawl';
const ALL_EVENT = 'all';
function includedByGlob(type, globs, dot, relativePath) {
    // For non-regular files or if there are no glob matchers, just respect the
    // `dot` option to filter dotfiles if dot === false.
    if (globs.length === 0 || type !== 'f') {
        return dot || _micromatch().default.some(relativePath, '**/*');
    }
    return _micromatch().default.some(relativePath, globs, {
        dot
    });
}
const posixPathMatchesPattern = _path().default.sep === '/' ? (pattern, filePath)=>pattern.test(filePath) : (pattern, filePath)=>pattern.test(filePath.replaceAll(_path().default.sep, '/'));
function typeFromStat(stat) {
    // Note: These tests are not mutually exclusive - a symlink passes isFile
    if (stat.isSymbolicLink()) {
        return 'l';
    }
    if (stat.isDirectory()) {
        return 'd';
    }
    if (stat.isFile()) {
        return 'f'; // "Regular" file
    }
    return null;
}

//# sourceMappingURL=common.js.map
