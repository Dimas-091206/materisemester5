var e = require("node:fs/promises");

var t = require("node:path");

var n = require("node:net");

var i = require("node:readline");

var o = require("./sessionSockets-chunk.js");

async function listSessions(n = {}) {
  o.cleanStaleSessionsSync();
  const i = o.getSessionBaseDir();
  const s = await e.readdir(i, {
    withFileTypes: !0
  }).catch(() => []);
  const r = [];
  for (const e of s) {
    if (!e.isDirectory()) {
      continue;
    }
    const n = t.join(i, e.name);
    const s = o.readMetaSync(n);
    if (!s) {
      continue;
    }
    r.push({
      id: e.name,
      pid: s.pid,
      formatVersion: s.formatVersion,
      alive: !1,
      startedAt: s.startedAt,
      command: s.command,
      cwd: s.cwd,
      version: s.version,
      origin: s.origin,
      sessionDir: n
    });
  }
  const c = o.newestSessionIds(r);
  for (const e of r) {
    e.alive = o.isPidAlive(e.pid) && c.has(e.id);
  }
  return function filterSessions(e, t) {
    const n = t?.trim();
    if (!n) {
      return e;
    }
    const i = e.filter(e => function matchesSessionExactly(e, t) {
      return sessionSearchValues(e).some(e => e === t);
    }(e, n));
    if (i.length) {
      return i;
    }
    const o = n.toLowerCase();
    return e.filter(e => sessionSearchValues(e).some(e => e.toLowerCase().includes(o)));
  }(r, n.selector).sort((e, t) => t.startedAt - e.startedAt);
}

async function readFirstLine(e) {
  for await (const t of readLines(e)) {
    return t;
  }
  return null;
}

function isSessionStartLine(e) {
  return e.startsWith('{"_e":"root:init"');
}

async function* readLines(e) {
  let t = "";
  for await (const n of e.createReadStream({
    encoding: "utf8"
  })) {
    t += n;
    let e = -1;
    while ((e = t.indexOf("\n")) >= 0) {
      const n = t.slice(0, e);
      t = t.slice(e + 1);
      if (n) {
        yield n;
      }
    }
  }
  if (t) {
    yield t;
  }
}

function formatSessionSelector(e) {
  return `${e.pid} (${e.command})`;
}

function readLiveLine(e, t) {
  if (!t) {
    return e.next();
  }
  if (t.aborted) {
    return Promise.resolve(null);
  }
  return new Promise(n => {
    const onAbort = () => n(null);
    t.addEventListener("abort", onAbort, {
      once: !0
    });
    e.next().then(e => {
      t.removeEventListener("abort", onAbort);
      n(t.aborted ? null : e);
    });
  });
}

function resetAbortTimer(e, t, n) {
  clearTimeout(e);
  return setAbortTimer(t, n);
}

function setAbortTimer(e, t) {
  const n = setTimeout(() => e.abort(), t);
  n.unref?.();
  return n;
}

function sessionSearchValues(e) {
  return [ e.id, String(e.pid), e.sessionDir, e.cwd, e.command, e.origin?.cwd, e.origin?.argv.join(" "), e.origin?.execPath, e.origin?.env?.npmLifecycleEvent, e.origin?.env?.npmPackageName ].filter(e => !!e);
}

function tryConnect(e, t) {
  return new Promise(i => {
    const o = n.connect(e);
    const cleanup = () => {
      o.off("connect", onConnect);
      o.off("error", onError);
      t?.removeEventListener("abort", onAbort);
    };
    const onConnect = () => {
      cleanup();
      i(o);
    };
    const onError = () => {
      cleanup();
      o.destroy();
      i(null);
    };
    const onAbort = () => {
      cleanup();
      o.destroy();
      i(null);
    };
    o.once("connect", onConnect);
    o.once("error", onError);
    t?.addEventListener("abort", onAbort, {
      once: !0
    });
  });
}

class EventCapture {
  #e;
  #t;
  #n;
  #i=!1;
  #o=!1;
  #s=[];
  #r=[];
  constructor(e = {}) {
    this.#e = e;
    this.#t = o.compileEventFilter(e.filter);
  }
  spawnOptions(e) {
    const t = Array.isArray(e?.stdio) ? [ ...e.stdio ] : null != e?.stdio ? [ e.stdio, e.stdio, e.stdio ] : [];
    while (t.length < 3) {
      t.push("inherit");
    }
    this.#n = t.length;
    t.push("pipe");
    const n = {
      ...e?.env ?? process.env,
      [o.LOG_EVENTS_ENV]: `${this.#n}`
    };
    if (!0 === this.#e.debug) {
      n[o.INTERNAL_DEBUG_ENV] = "1";
    }
    return {
      ...e,
      stdio: t,
      env: n
    };
  }
  attach(e) {
    if (null == this.#n) {
      throw new Error("captureEvents: spawnOptions() must create the pipe before attach()");
    }
    if (this.#i) {
      throw new Error("captureEvents: a child is already attached");
    }
    const t = e.stdio[this.#n];
    if (!t || "function" != typeof t.setEncoding) {
      throw new Error(`captureEvents: child stdio[${this.#n}] is not a readable pipe`);
    }
    this.#i = !0;
    let n = "";
    t.setEncoding("utf8");
    t.on("data", e => {
      const t = n ? n + e : e;
      let i = 0;
      let o;
      while (-1 !== (o = t.indexOf("\n", i))) {
        const e = t.slice(i, o);
        i = o + 1;
        if (e) {
          this.#c(e);
        }
      }
      n = i ? t.slice(i) : t;
    });
    const finish = () => {
      if (this.#o) {
        return;
      }
      this.#o = !0;
      if (n) {
        this.#c(n);
      }
      this.#a(null);
    };
    t.on("end", finish);
    t.on("close", finish);
    t.on("error", finish);
    return this;
  }
  async collect() {
    const e = [];
    for await (const t of this) {
      e.push(t);
    }
    return e;
  }
  async* [Symbol.asyncIterator]() {
    let e;
    while (e = await this.#l()) {
      yield e;
    }
  }
  #c(e) {
    const t = o.parseEventLine(e, this.#e, this.#t);
    if (t) {
      this.#a(t);
    }
  }
  #a(e) {
    const t = this.#r.shift();
    if (t) {
      t(e);
    } else if (e) {
      this.#s.push(e);
    }
  }
  #l() {
    if (this.#s.length) {
      return Promise.resolve(this.#s.shift());
    }
    if (this.#o) {
      return Promise.resolve(null);
    }
    if (!this.#i) {
      throw new Error("captureEvents: attach() a child before iterating");
    }
    return new Promise(e => this.#r.push(e));
  }
}

exports.captureEvents = function captureEvents(e) {
  return new EventCapture(e);
};

exports.detectRotationLoss = async function detectRotationLoss(n) {
  const i = o.readMetaSync(n);
  for (let s = (i?.maxSegments ?? o.DEFAULT_SEGMENTS) - 1; s >= 0; s--) {
    const i = await e.open(t.join(n, `${s}.jsonl`), "r").catch(() => null);
    if (!i) {
      continue;
    }
    try {
      const e = await readFirstLine(i);
      return null != e && !isSessionStartLine(e);
    } finally {
      await i.close().catch(() => {});
    }
  }
  return !1;
};

exports.listSessions = listSessions;

exports.resolveSession = async function resolveSession(e) {
  const t = await listSessions({
    selector: e
  });
  const n = t.filter(e => e.alive);
  const i = 1 === t.length ? t[0] : 1 === n.length ? n[0] : null;
  if (!i) {
    throw new Error(null != e ? t.length ? `Ambiguous 2g session "${e}"; specify one of: ${t.map(formatSessionSelector).join(", ")}` : `No 2g session matching "${e}"` : t.length ? `Ambiguous 2g session; specify one of: ${t.map(formatSessionSelector).join(", ")}` : "No 2g sessions found");
  }
  return i;
};

exports.tap = async function* tap(n, s = {}) {
  const r = !0 === s.follow;
  const c = o.parseSince(s.since);
  const a = o.compileEventFilter(s.filter);
  const l = function createTapAbortController(e, t) {
    if (!t || null == e.timeout && null == e.idleTimeout) {
      return;
    }
    const n = new AbortController;
    e.signal?.addEventListener("abort", () => n.abort(), {
      once: !0
    });
    if (null != e.timeout) {
      setAbortTimer(n, e.timeout);
    }
    return n;
  }(s, r);
  const u = l?.signal ?? s.signal;
  const f = o.readMetaSync(n);
  const d = r ? await async function connectLive(e, t, n) {
    const s = o.resolveSocketPath(e, t?.socket ?? o.SESSION_FILES.liveSocket);
    const r = await async function connectWithRetry(e, t) {
      for (let n = 0; n < 20 && !t?.aborted; n++) {
        const n = await tryConnect(e, t);
        if (n) {
          return n;
        }
        await new Promise(e => setTimeout(e, 50));
      }
      return null;
    }(s, n);
    if (!r) {
      return;
    }
    const c = i.createInterface({
      input: r
    });
    const a = [];
    const l = [];
    let u = !1;
    const push = e => {
      const t = l.shift();
      if (t) {
        t(e);
      } else if (null != e) {
        a.push(e);
      }
    };
    c.on("line", e => push(e));
    c.on("close", () => {
      u = !0;
      push(null);
    });
    r.on("error", () => {
      u = !0;
      push(null);
    });
    n?.addEventListener("abort", () => r.destroy(), {
      once: !0
    });
    return {
      buffer: a,
      next() {
        if (a.length) {
          return Promise.resolve(a.shift());
        }
        if (u) {
          return Promise.resolve(null);
        }
        return new Promise(e => l.push(e));
      }
    };
  }(n, f, u) : void 0;
  const h = await async function openHistoryFiles(n, i) {
    const s = i?.maxSegments ?? o.DEFAULT_SEGMENTS;
    const r = await Promise.all(Array.from({
      length: s
    }, (i, o) => e.open(t.join(n, `${o}.jsonl`), "r").catch(() => null)));
    return r.filter(e => null != e).reverse();
  }(n, f);
  let m;
  try {
    for (const e of h) {
      for await (const t of readLines(e)) {
        const e = o.parseEventLine(t, s, a, c);
        if (e) {
          yield e;
        }
      }
    }
  } finally {
    await Promise.all(h.map(e => e.close().catch(() => {})));
  }
  if (!d) {
    return;
  }
  if (null != s.idleTimeout && l) {
    m = setAbortTimer(l, s.idleTimeout);
  }
  for (const e of d.buffer.splice(0)) {
    const t = o.parseEventLine(e, s, a, c);
    if (t) {
      if (m) {
        m = resetAbortTimer(m, l, s.idleTimeout);
      }
      yield t;
    }
  }
  while (!u?.aborted) {
    const e = await readLiveLine(d, u);
    if (null == e) {
      break;
    }
    if (m) {
      m = resetAbortTimer(m, l, s.idleTimeout);
    }
    const t = o.parseEventLine(e, s, a, c);
    if (t) {
      yield t;
    }
  }
  if (m) {
    clearTimeout(m);
  }
};
//# sourceMappingURL=api-chunk.js.map
