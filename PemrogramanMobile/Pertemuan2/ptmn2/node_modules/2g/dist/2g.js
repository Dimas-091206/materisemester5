Object.defineProperty(exports, "__esModule", {
  value: !0
});

var e = require("node:path");

var t = require("node:perf_hooks");

var i = require("./chunks/sessionSockets-chunk.js");

var n = require("node:fs");

var s = require("node:net");

var r = require("./chunks/pretty-chunk.js");

var o = require("node:os");

var c = require("node:buffer");

var l = require("node:events");

var h = require("node:worker_threads");

const u = Symbol.for(`2g/state-${i.EVENT_LOG_STATE_VERSION}`);

const a = function getEventLogState() {
  const e = globalThis;
  return e[u] ??= {
    logPath: process.cwd(),
    eventLoggerInfo: null,
    debug: !1
  };
}();

const d = t.performance.timeOrigin;

const f = Array.from({
  length: 1e3
}, (e, t) => `.${`${t}`.padStart(3, "0")}`);

let p = -1;

let g = "";

let y;

let m = "";

function writeEvent(e, t, i, n, s, r) {
  try {
    e._writeln(function serializeEvent(e, t, i, n, s, r) {
      const o = stringifyTimestamp();
      if (!n?._w && !r) {
        const n = i ? JSON.stringify(i).slice(1) : "";
        return n.length > 1 ? `{"_e":"${e}:${t}","_t":${o},${n}\n` : `{"_e":"${e}:${t}","_t":${o}}\n`;
      }
      let c = `{"_e":"${e}:${t}","_t":${o}`;
      if (r) {
        c += `,"_l":${r}`;
      }
      if (n?._w) {
        c += `,"_w":${stringifyWorkerId(n._w)}`;
      }
      return `${appendPayload(c, i)}}\n`;
    }(t, i, n, s, 0, r));
  } catch {}
}

function writeCompleteEvent(e, t, i, n, s, r, o, c) {
  try {
    let l = `{"_e":"${t}:${i}","_t":${stringifyTimestamp()},"_d":${r}`;
    if (c) {
      l += `,"_l":${c}`;
    }
    if (o?._w) {
      l += `,"_w":${stringifyWorkerId(o._w)}`;
    }
    e._writeln(`${function appendMergedPayload(e, t, i) {
      if (!t) {
        return appendPayload(e, i);
      }
      return appendPayload(e, t);
    }(l, n, s)}}\n`);
  } catch {}
}

function stringifyTimestamp() {
  const e = d + t.performance.now();
  const i = Math.floor(e);
  if (i !== p) {
    p = i;
    g = `${i}`;
  }
  return g + f[1e3 * (e - i) | 0];
}

function stringifyWorkerId(e) {
  if (e !== y) {
    y = e;
    m = JSON.stringify(e);
  }
  return m;
}

function appendPayload(e, t) {
  if (!t) {
    return e;
  }
  const i = JSON.stringify(t);
  return i && i.length > 2 ? `${e},${i.slice(1, -1)}` : e;
}

const NOOP_DONE = () => {};

function serializeError(e, t = 0) {
  if (null == e) {
    return null;
  }
  if (!(e instanceof Error)) {
    return String(e);
  }
  const i = {
    name: e.name,
    message: e.message
  };
  const n = e.code;
  if ("string" == typeof n) {
    i.code = n;
  }
  const s = e.stack;
  if (s) {
    i.stack = s;
  }
  if (null != e.cause && t < 4) {
    i.cause = serializeError(e.cause, t + 1);
  }
  return i;
}

function createEventLogger(i, n) {
  function log(e, t) {
    if (a.primarySink && (!n || a.debug)) {
      writeEvent(a.primarySink, i, e, t, a.eventMeta, n);
    }
  }
  log.span = function span() {
    if (!a.primarySink || n && !a.debug) {
      return NOOP_DONE;
    }
    const e = t.performance.now();
    return function done(s, r) {
      if (!a.primarySink) {
        return;
      }
      writeCompleteEvent(a.primarySink, i, s, r, void 0, t.performance.now() - e, a.eventMeta, n);
    };
  };
  log.path = function relativePath(t) {
    return {
      toJSON() {
        try {
          return null != t && e.isAbsolute(t) ? e.relative(a.logPath, t).replace(/\\/g, "/") || "." : t ?? null;
        } catch {
          return t || null;
        }
      }
    };
  };
  log.error = function error(error) {
    return {
      toJSON: () => serializeError(error)
    };
  };
  log.category = i;
  return log;
}

function events(e) {
  return createEventLogger(e, void 0);
}

events.debug = function debug(e) {
  return createEventLogger(e, i.EVENT_LEVEL_DEBUG);
};

function createDebugSink(e = process.env[i.LOG_DEBUG_ENV]) {
  const t = function compileLogDebugFilter(e) {
    const t = e?.split(/[\s,]+/).map(e => e.trim()).filter(Boolean);
    if (!t?.length) {
      return null;
    }
    return i.compileEventFilter(t);
  }(e);
  if (!t) {
    return null;
  }
  const n = r.shouldColorizeStream(process.stderr);
  return {
    get writable() {
      return !0;
    },
    _writeln(e) {
      let s = 0;
      let o;
      while (-1 !== (o = e.indexOf("\n", s))) {
        const c = e.slice(s, o);
        s = o + 1;
        if (!c) {
          continue;
        }
        const l = i.parseEventLine(c, {
          debug: !0
        });
        if (!l || !i.matchesTapOptions(l, {
          debug: !0
        }, t)) {
          continue;
        }
        process.stderr.write(`${r.formatPrettyEvent(l, {
          colorize: n,
          stream: process.stderr
        })}\n`);
      }
      return !0;
    },
    end(e) {
      e?.();
      return this;
    },
    destroy() {}
  };
}

const S = Symbol.for("2g/process-cleanup");

function registerProcessCleanup(e) {
  const t = getProcessCleanupState();
  t.callbacks.add(e);
  if (t.registered) {
    return;
  }
  t.registered = !0;
  process.once("exit", runProcessCleanup);
  for (const e of [ "SIGINT", "SIGTERM", "SIGHUP" ]) {
    process.once(e, () => {
      if (process.listenerCount(e) > 0) {
        return;
      }
      runProcessCleanup();
      process.kill(process.pid, e);
    });
  }
}

function getProcessCleanupState() {
  const e = globalThis;
  return e[S] ??= {
    registered: !1,
    callbacks: new Set
  };
}

function runProcessCleanup() {
  for (const e of getProcessCleanupState().callbacks) {
    try {
      e();
    } catch {}
  }
}

function listenIpcSink(e, t) {
  const n = s.createServer(t => ingestIpcSocket(t, e));
  const r = i.listenSocket(n, t);
  const o = function publishIpcPath(e) {
    process.env[i.INTERNAL_IPC_ENV] = e;
    return () => {
      if (process.env[i.INTERNAL_IPC_ENV] === e) {
        delete process.env[i.INTERNAL_IPC_ENV];
      }
    };
  }(t);
  const c = a.debug;
  if (c) {
    process.env[i.INTERNAL_DEBUG_ENV] = "1";
  }
  delete process.env[i.LOG_DEBUG_ENV];
  delete process.env[i.LOG_EVENTS_ENV];
  return () => {
    r();
    o();
    if (c) {
      delete process.env[i.INTERNAL_DEBUG_ENV];
    }
  };
}

function publishTempIpcSink(t) {
  const n = function createTempIpcPath() {
    const t = Date.now().toString(36);
    const i = Math.random().toString(36).slice(2, 10);
    const n = `${process.pid}-${t}-${i}`;
    return "win32" === process.platform ? `\\\\.\\pipe\\event-log-${n}-ipc` : e.join(o.tmpdir(), `event-log-${n}.sock`);
  }();
  const s = listenIpcSink(t, n);
  let r = !1;
  const cleanup = () => {
    if (r) {
      return;
    }
    r = !0;
    s();
    i.removeSocket(n);
  };
  const c = t.destroy.bind(t);
  t.destroy = () => {
    cleanup();
    c();
  };
  t.once("close", cleanup);
  registerProcessCleanup(cleanup);
}

function openIpc(e, t = {}) {
  const i = t.retryMs ?? 50;
  const n = t.maxRetryMs ?? 750;
  return (t, r) => {
    const o = Date.now();
    const attempt = () => {
      const c = s.connect(e);
      let l = !1;
      c.once("connect", () => {
        const e = c._handle?.fd;
        l = !0;
        c.unref();
        c.once("close", () => r.destroy());
        const drain = (e, t) => {
          c.ref();
          c.write(e, e => {
            c.unref();
            t(e);
          });
        };
        if ("number" == typeof e && e >= 0) {
          t(null, e, drain);
        } else {
          t(null, null, drain);
        }
      });
      c.once("error", e => {
        if (l) {
          return;
        }
        c.destroy();
        ((e, s) => {
          if (Date.now() - o > n) {
            t(e);
          } else {
            setTimeout(s, i);
          }
        })(e, attempt);
      });
    };
    attempt();
  };
}

function ingestIpcSocket(e, t) {
  let i = "";
  e.unref();
  e.setEncoding("utf8");
  e.on("data", e => {
    const n = i ? i + e : e;
    const s = n.lastIndexOf("\n");
    if (s === n.length - 1) {
      i = "";
      t._writeln(n);
    } else if (-1 === s) {
      i = n;
    } else {
      i = n.slice(s + 1);
      t._writeln(n.slice(0, s + 1));
    }
  });
  e.on("error", () => {});
}

class LogStream extends l.EventEmitter {
  #e=-1;
  #t=null;
  #i=null;
  #n=!0;
  #s=!1;
  #r=!1;
  #o=!1;
  #c=!1;
  #l=!1;
  #h=!1;
  #u=!1;
  #a="";
  #d=0;
  #f=[];
  #p=0;
  #g=0;
  #y=0;
  #m=!1;
  #S=!1;
  #E;
  #v=(e, t) => {
    this.#_(e, t);
  };
  #k=e => {
    if (null != e) {
      this.#w(e);
    } else {
      this.#_(null, c.Buffer.byteLength(this.#a));
    }
  };
  #L=() => {
    this.#S = !1;
    if (!this.#s && !this.#l && (this.#f.length - this.#p > this.#g || this.#a)) {
      this.#b();
    }
  };
  constructor(e, t = {}) {
    super();
    this.#n = !1 !== t.closeFd;
    if ("number" == typeof e) {
      try {
        n.fsyncSync(e);
      } catch {}
      this.#e = e;
      process.nextTick(() => this.emit("ready"));
    } else if ("string" == typeof e) {
      this.#I(e);
    } else {
      this.#N(e, null);
    }
  }
  get file() {
    return this.#t;
  }
  get fd() {
    return this.#e;
  }
  get writing() {
    return this.#s;
  }
  get buffered() {
    return this.#d;
  }
  get writable() {
    return !this.#l && !this.#r;
  }
  reopen(e = this.#t) {
    if (null == this.#t) {
      throw new Error("Cannot reopen an fd-only LogStream");
    }
    if (this.#l || this.#r) {
      return;
    }
    if (e) {
      this.#t = e;
    }
    if (this.#h) {
      this.once("ready", () => this.reopen(e));
      return;
    }
    this.#u = !0;
    if (!this.#s) {
      this.#P();
    }
  }
  #P() {
    if (null == this.#t) {
      return;
    }
    this.#u = !1;
    this.#i = this.#e;
    this.#I(this.#t);
  }
  #O(e) {
    if ("EAGAIN" === e.code && null != this.#E) {
      this.#m = !0;
      this.#E(this.#a, this.#k);
    } else if (0 === this.#y) {
      this.#y = 1;
      setImmediate(() => this.#b());
    } else {
      this.#y = Math.min(2 * this.#y, 100);
      setTimeout(() => this.#b(), this.#y);
    }
  }
  #_(e, t) {
    if (e) {
      if ("EAGAIN" === e.code || "EBUSY" === e.code) {
        this.#O(e);
      } else {
        this.#w(e);
      }
      return;
    }
    this.#y = 0;
    this.emit("write", t);
    if (t === this.#a.length) {
      this.#d -= this.#a.length;
      this.#a = "";
    } else {
      if (c.Buffer.byteLength(this.#a) > t) {
        const e = c.Buffer.from(this.#a).toString("utf8", t);
        this.#d -= this.#a.length - e.length;
        this.#a = e;
      } else {
        this.#d -= this.#a.length;
        this.#a = "";
      }
    }
    if (this.#a) {
      this.#b();
    } else if (this.#o && !this.#r) {
      this.#s = !1;
      this.#T();
    } else if (this.#f.length - this.#p > this.#g) {
      this.#b();
    } else if (this.#u) {
      this.#s = !1;
      this.#P();
    } else if (this.#r) {
      this.#s = !1;
      this.#T();
    } else {
      this.#s = !1;
      this.#m = !1;
      if (this.#c) {
        this.emit("drain");
      }
    }
  }
  #I(t) {
    this.#N(i => {
      n.mkdir(e.dirname(t), {
        recursive: !0
      }, () => {
        n.open(t, "a", 384, (e, t) => {
          if (e || null == t) {
            i(e);
            return;
          }
          n.fchmod(t, 384, () => {
            i(null, t);
          });
        });
      });
    }, t);
  }
  #N(e, t) {
    this.#h = !0;
    this.#s = !0;
    e((e, i, s) => {
      const r = this.#i;
      this.#i = null;
      if (e) {
        this.#h = !1;
        this.#w(e);
      } else {
        this.#e = i ?? -1;
        this.#t = t;
        this.#E = s;
        this.#h = !1;
        this.#s = !1;
        this.emit("ready");
        if (null != r && r !== this.#e && !isStdFd(r)) {
          fsFsync(r, () => n.close(r, () => {}));
        }
        if (this.#l) {
          return;
        }
        if (this.#o) {
          this.#T();
        } else if (!this.#s && this.#f.length - this.#p > this.#g || this.#c) {
          this.#b();
        }
      }
    }, this);
  }
  #T() {
    if (-1 === this.#e && this.#h) {
      this.once("ready", () => this.#T());
      return;
    }
    this.#l = !0;
    this.#g = 0;
    this.#f.length = 0;
    this.#p = 0;
    const onClose = e => {
      if (e) {
        this.emit("error", e);
        this.emit("close", e);
      } else {
        if (this.#r && !this.#s) {
          this.emit("finish");
        }
        this.emit("close");
      }
    };
    if (this.#e < 0) {
      onClose();
      return;
    }
    fsFsync(this.#e, e => {
      if (!e && this.#n && !isStdFd(this.#e)) {
        n.close(this.#e, onClose);
      } else {
        onClose();
      }
    });
  }
  #w(e) {
    this.#s = !1;
    if (this.listenerCount("error")) {
      this.emit("error", e);
    }
    this.#T();
  }
  #$() {
    if (!this.#S) {
      this.#S = !0;
      setImmediate(this.#L);
    }
  }
  #b() {
    this.#s = !0;
    if (!this.#a) {
      const e = this.#f.length - this.#g;
      if (e > this.#p) {
        this.#a = this.#f[this.#p++] || "";
        while (this.#p < e && this.#a.length < 65536) {
          this.#a += this.#f[this.#p++];
        }
        if (this.#p === this.#f.length) {
          this.#f.length = 0;
          this.#p = 0;
        }
      }
    }
    if (null != this.#E && (this.#m || this.#e < 0)) {
      this.#E(this.#a, this.#k);
    } else {
      n.write(this.#e, this.#a, this.#v);
    }
  }
  _end() {
    if (!this.#l && !this.#r) {
      this.#r = !0;
      if (this.#h) {
        this.once("ready", () => this._end());
      } else if (!this.#s && this.#e >= 0) {
        if (this.#f.length - this.#p > this.#g) {
          this.#b();
        } else {
          this.#T();
        }
      }
    }
    return this;
  }
  end(e, t, i) {
    const n = i || t || e;
    const s = "function" != typeof e ? e : void 0;
    const r = "function" == typeof n ? n : void 0;
    if ("string" == typeof s) {
      this.write(s, "string" == typeof t ? t : "utf8");
    } else if (null != s) {
      this.write(s);
    }
    if (r) {
      this.once("close", r);
    }
    return this._end();
  }
  destroy() {
    if (this.#l || this.#o) {
      return;
    }
    if (this.#s || this.#h) {
      this.#o = !0;
    } else if (this.#f.length - this.#p > this.#g) {
      this.#o = !0;
      this.#b();
    } else {
      this.#T();
    }
  }
  flush(e) {
    if (this.#l) {
      e?.();
    } else {
      const onDrain = () => {
        if (!this.#l) {
          fsFsync(this.#e, t => {
            this.#c = !1;
            if ("EBADF" === t?.code) {
              e?.();
            } else {
              e?.(t);
            }
          });
        } else {
          this.#c = !1;
          e?.();
        }
        this.off("error", onError);
      };
      const onError = t => {
        this.#c = !1;
        this.off("drain", onDrain);
        e?.(t);
      };
      this.#c = !0;
      this.once("drain", onDrain);
      this.once("error", onError);
      if (!this.#s) {
        if (this.#f.length - this.#p > this.#g || this.#a) {
          this.#b();
        } else {
          process.nextTick(() => this.emit("drain"));
        }
      }
    }
  }
  _writeln(e) {
    if (this.#l) {
      return !1;
    }
    this.#d += e.length;
    this.#f.push(e);
    if (!this.#s) {
      this.#$();
    }
    return this.#d < 16384;
  }
  _write(e) {
    if (this.#l) {
      return !1;
    }
    if (0 === this.#g && 10 === e.charCodeAt(e.length - 1)) {
      return this._writeln(e);
    }
    this.#d += e.length;
    let t = 0;
    let i = -1;
    while ((i = e.indexOf("\n", t)) > -1) {
      const n = e.slice(t, i + 1);
      if (this.#g > 0) {
        this.#f[this.#f.length - 1] += n;
      } else {
        this.#f.push(n);
      }
      this.#g = 0;
      t = ++i;
    }
    if (t < e.length) {
      const i = e.slice(t);
      if (this.#g > 0) {
        this.#f[this.#f.length - 1] += i;
      } else {
        this.#f.push(e.slice(t));
      }
      this.#g = 1;
    }
    if (!this.#s && this.#f.length - this.#p > this.#g) {
      this.#$();
    }
    return this.#d < 16384;
  }
  write(e, t, i) {
    const n = i || t;
    const s = "string" == typeof t ? t : "utf8";
    const r = "string" == typeof e ? e : c.Buffer.from(e).toString(s);
    const o = "function" == typeof n ? n : void 0;
    try {
      return this._write(r);
    } finally {
      o?.();
    }
  }
  [Symbol.dispose]() {
    this.destroy();
  }
}

const isStdFd = e => {
  switch (e) {
   case 1:
   case 2:
   case process.stdout.fd:
   case process.stderr.fd:
    return !0;

   default:
    return !1;
  }
};

const fsFsync = (e, t) => {
  try {
    n.fsync(e, t);
  } catch (e) {
    t(e);
  }
};

class SocketSink {
  #R;
  constructor(e) {
    this.#R = e;
    e.on("error", () => {});
  }
  get writable() {
    return this.#R.writable;
  }
  get buffered() {
    return this.#R.writableLength;
  }
  _writeln(e) {
    return this.#R.write(e);
  }
  end(e) {
    this.#R.end(e);
    return this;
  }
  destroy() {
    this.#R.destroy();
  }
}

class BroadcastChannel {
  #D;
  #V=[];
  #l=!1;
  constructor(e) {
    this.#D = e;
    this.#V.push({
      sink: e,
      dropOnBackpressure: !1,
      destroy: () => e.destroy()
    });
  }
  get writable() {
    if (this.#l) {
      return !1;
    }
    for (let e = 0; e < this.#V.length; e++) {
      if (this.#V[e].sink.writable) {
        return !0;
      }
    }
    return !1;
  }
  add(e) {
    if (!e) {
      return;
    }
    if (this.#l) {
      e.destroy();
    } else {
      this.#V.push({
        sink: e,
        dropOnBackpressure: !1,
        destroy: () => e.destroy()
      });
    }
  }
  attach(e) {
    if (this.#l) {
      e.destroy();
      return;
    }
    e.unref();
    const t = (e => {
      const t = e._handle?.fd;
      if ("number" == typeof t && t >= 0) {
        return new LogStream(i => i(null, t, (t, i) => {
          e.write(t, i);
        }), {
          closeFd: !1
        });
      }
      return new SocketSink(e);
    })(e);
    const i = {
      sink: t,
      dropOnBackpressure: !0,
      destroy: () => {
        t.destroy();
        e.destroy();
      }
    };
    this.#V.push(i);
    const detach = () => {
      const e = this.#V.indexOf(i);
      if (e > -1) {
        this.#V.splice(e, 1);
      }
      i.destroy();
    };
    e.on("close", detach);
    e.on("error", detach);
  }
  ingest(e) {
    ingestIpcSocket(e, this);
  }
  _writeln(e) {
    const t = this.#V;
    let i = !1;
    for (let n = t.length - 1; n >= 0; n--) {
      const s = t[n];
      const r = s.sink._writeln(e);
      if (!r && s.dropOnBackpressure && (s.sink.buffered ?? 1 / 0) > 4194304) {
        t.splice(n, 1);
        s.destroy();
      }
      i = r || i;
    }
    return i;
  }
  flush(e) {
    if (this.#D.flush) {
      this.#D.flush(e);
    } else {
      e();
    }
  }
  end(e) {
    for (const t of this.#V) {
      t.sink.end(t.sink === this.#D ? e : void 0);
    }
    return this;
  }
  destroy() {
    this.#l = !0;
    for (const e of this.#V) {
      e.destroy();
    }
    this.#V.length = 0;
  }
}

const E = Symbol.for("2g/process-origin");

function setLocalProcessOrigin(e) {
  const t = globalThis;
  if (e) {
    t[E] = e;
  } else {
    delete t[E];
  }
}

function getProcessOrigin() {
  if (!h.isMainThread) {
    return {
      kind: "worker_thread",
      id: String(h.threadId)
    };
  }
  const e = globalThis[E];
  const t = process.env[i.INTERNAL_PROCESS_ORIGIN_ENV];
  if (t && t !== e) {
    return {
      kind: "event_log_child",
      id: String(process.pid)
    };
  }
  if (process.env.NODE_UNIQUE_ID) {
    return {
      kind: "child_process",
      id: process.env.NODE_UNIQUE_ID
    };
  }
  return null;
}

function createSession(t) {
  const r = i.getSessionBaseDir();
  const o = Date.now();
  const c = `${process.pid}-${o.toString(36)}-${Math.random().toString(36).slice(2, 8).padEnd(6, "0")}`;
  const l = e.join(r, c);
  try {
    n.mkdirSync(l, {
      recursive: !0,
      mode: 448
    });
    n.chmodSync(r, 448);
  } catch {}
  const h = t.maxSegments ?? (a.debug ? i.DEBUG_SEGMENTS : i.DEFAULT_SEGMENTS);
  const u = t.maxSegmentSize ?? (a.debug ? i.DEBUG_SEGMENT_SIZE : i.DEFAULT_SEGMENT_SIZE);
  const d = i.createSocketAddress(l, c, "live");
  const f = i.createSocketAddress(l, c, "ipc");
  const p = new LogStream(e.join(l, "0.jsonl"));
  const g = new BroadcastChannel(p);
  g.add(createDebugSink());
  !function trackSegmentRotation(t, i, s, r) {
    let o = 0;
    t.on("write", c => {
      o += c;
      if (o < r) {
        return;
      }
      o = 0;
      !function rotateSegments(t, i) {
        n.rmSync(e.join(t, i - 1 + ".jsonl"), {
          force: !0
        });
        for (let s = i - 1; s >= 1; s--) {
          const i = e.join(t, s - 1 + ".jsonl");
          const r = e.join(t, `${s}.jsonl`);
          try {
            n.renameSync(i, r);
          } catch {}
        }
      }(i, s);
      t.reopen();
    });
  }(p, l, h, u);
  const y = s.createServer(e => g.attach(e));
  const m = i.listenSocket(y, d.path);
  const S = listenIpcSink(g, f.path);
  const E = function publishProcessOrigin() {
    const e = String(process.pid);
    process.env[i.INTERNAL_PROCESS_ORIGIN_ENV] = e;
    setLocalProcessOrigin(e);
    return () => {
      if (process.env[i.INTERNAL_PROCESS_ORIGIN_ENV] === e) {
        delete process.env[i.INTERNAL_PROCESS_ORIGIN_ENV];
      }
      setLocalProcessOrigin(void 0);
    };
  }();
  const v = {
    pid: process.pid,
    formatVersion: i.EVENT_LOG_FORMAT_VERSION,
    startedAt: o,
    command: t.command ?? process.argv.slice(1).join(" "),
    cwd: process.cwd(),
    maxSegments: h,
    version: t.version,
    socket: d.name,
    ipcSocket: f.name,
    origin: createSessionOrigin()
  };
  try {
    !function writeJsonAtomic(e, t) {
      const i = `${e}.${process.pid}.tmp`;
      n.writeFileSync(i, JSON.stringify(t, null, 2), {
        mode: 384
      });
      n.renameSync(i, e);
    }(e.join(l, i.SESSION_FILES.meta), v);
  } catch {}
  let _ = !1;
  const destroy = () => {
    if (_) {
      return;
    }
    _ = !0;
    g.destroy();
    m();
    S();
    E();
    i.removeSocket(d.path);
    i.removeSocket(f.path);
  };
  registerProcessCleanup(destroy);
  setImmediate(() => {
    try {
      i.cleanStaleSessionsSync();
    } catch {}
  });
  return {
    sessionDir: l,
    meta: v,
    sink: g,
    destroy
  };
}

function createSessionOrigin() {
  const e = {
    npmLifecycleEvent: process.env.npm_lifecycle_event,
    npmExecPath: process.env.npm_execpath,
    npmPackageName: process.env.npm_package_name
  };
  return {
    argv: process.argv.slice(1),
    execPath: process.execPath,
    cwd: process.cwd(),
    ppid: process.ppid || void 0,
    env: Object.values(e).some(Boolean) ? e : void 0
  };
}

const v = events("root");

function installChildEventLogger(e) {
  if (a.primarySink) {
    return !0;
  }
  const t = function getProcessWorkerId() {
    const e = getProcessOrigin();
    return e ? `${e.kind}:${e.id}` : void 0;
  }();
  if (t) {
    a.eventMeta = {
      _w: t
    };
  }
  const n = function getParentIpcPath() {
    return process.env[i.INTERNAL_IPC_ENV];
  }();
  if (n) {
    a.debug = e?.debug ?? (function isParentDebugEnabled() {
      return "1" === process.env[i.INTERNAL_DEBUG_ENV];
    }() || !!process.env[i.LOG_DEBUG_ENV]);
    !function connectToParent(e) {
      a.primarySink = createPrimarySink(openIpc(e), {
        closeFd: !1
      });
      a.eventLoggerInfo = {
        destination: "ipc",
        isUserVisibleOutput: !1,
        debug: a.debug
      };
      v("init", getInitMetadata());
    }(n);
    return !0;
  } else {
    return !1;
  }
}

function parseLogTarget(t) {
  if ("number" == typeof t) {
    return Number.isSafeInteger(t) && t > 0 ? t : void 0;
  }
  if (!t) {
    return;
  }
  const i = parseInt(t, 10);
  if (`${i}` === t && i > 0 && Number.isSafeInteger(i)) {
    return i;
  }
  try {
    const i = e.parse(t);
    const n = e.format(i);
    a.logPath = i.dir || process.cwd();
    return n;
  } catch {
    return;
  }
}

function createPrimarySink(e, t) {
  const i = new LogStream(e, t);
  i.once("error", () => {
    a.primarySink = void 0;
    a.eventLoggerInfo = null;
  });
  return i;
}

function getInitMetadata(e) {
  return {
    format: "v0-jsonl",
    formatVersion: i.EVENT_LOG_FORMAT_VERSION,
    version: e ?? "UNVERSIONED",
    processOrigin: getProcessOrigin() ?? void 0
  };
}

function activateSink(e, t) {
  a.primarySink = e;
  v("init", getInitMetadata(t));
}

installChildEventLogger();

exports.events = events;

exports.flushEventLogger = function flushEventLogger() {
  return new Promise(e => {
    const t = a.primarySink;
    if (t?.flush) {
      t.flush(() => e());
    } else {
      e();
    }
  });
};

exports.getEventLoggerInfo = () => a.primarySink?.writable ? a.eventLoggerInfo : null;

exports.installChildEventLogger = installChildEventLogger;

exports.installEventLogger = function installEventLogger(e) {
  const t = e && "object" == typeof e ? e : void 0;
  if (installChildEventLogger(t)) {
    return;
  }
  const n = parseLogTarget(process.env[i.LOG_EVENTS_ENV]) ?? parseLogTarget("string" == typeof e || "number" == typeof e ? e : void 0);
  if (null != n) {
    if ("number" == typeof n) {
      r.redirectConsoleForFd(n);
    }
    a.debug = t?.debug ?? !0;
    a.eventLoggerInfo = function getExplicitTargetInfo(e) {
      if ("number" == typeof e) {
        return {
          destination: 1 === e ? "stdout" : 2 === e ? "stderr" : "fd",
          isUserVisibleOutput: 1 === e || 2 === e,
          debug: a.debug,
          fd: e
        };
      }
      return {
        destination: "file",
        isUserVisibleOutput: !1,
        debug: a.debug,
        file: e
      };
    }(n);
    const e = createPrimarySink(n);
    publishTempIpcSink(e);
    activateSink(e);
    return;
  }
  if (t && !1 !== t.session) {
    a.debug = t.debug ?? !!process.env[i.LOG_DEBUG_ENV];
    const e = createSession(t);
    a.logPath = e.sessionDir;
    a.eventLoggerInfo = {
      destination: "session",
      isUserVisibleOutput: !1,
      debug: a.debug,
      sessionDir: e.sessionDir
    };
    activateSink(e.sink, t.version);
    return;
  }
};
//# sourceMappingURL=2g.js.map
