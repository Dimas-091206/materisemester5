"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "fixHermesV1AsyncArrowNonSimpleParams", {
    enumerable: true,
    get: function() {
        return fixHermesV1AsyncArrowNonSimpleParams;
    }
});
function fixHermesV1AsyncArrowNonSimpleParams({ types: t }) {
    return {
        name: 'fix-hermes-v1-async-arrow-non-simple-params',
        visitor: {
            ArrowFunctionExpression (path) {
                const { node } = path;
                if (!node.async || node.params.every((p)=>t.isIdentifier(p))) {
                    return;
                }
                // Hermes v1 rejects any rest param on async arrows
                // Bail with wrapping sync arrow function closing over async arrow function
                if (node.params.some((p)=>t.isRestElement(p))) {
                    const body = !t.isBlockStatement(node.body) ? t.blockStatement([
                        t.returnStatement(node.body)
                    ]) : node.body;
                    const innerAsync = t.arrowFunctionExpression([], body, true);
                    node.async = false;
                    node.body = t.callExpression(innerAsync, []);
                    return;
                }
                const newParams = [];
                const init = [];
                for(let idx = 0; idx < node.params.length; idx++){
                    const param = node.params[idx];
                    if (t.isIdentifier(param)) {
                        newParams.push(param);
                        continue;
                    }
                    const sym = path.scope.generateUidIdentifier('p');
                    if (t.isAssignmentPattern(param)) {
                        newParams.push(sym);
                        init.push(t.variableDeclaration('var', [
                            t.variableDeclarator(param.left, t.conditionalExpression(t.binaryExpression('===', t.cloneNode(sym), t.identifier('undefined')), param.right, t.cloneNode(sym)))
                        ]));
                    } else {
                        newParams.push(sym);
                        init.push(t.variableDeclaration('var', [
                            t.variableDeclarator(param, t.cloneNode(sym))
                        ]));
                    }
                }
                const body = !t.isBlockStatement(node.body) ? t.blockStatement([
                    t.returnStatement(node.body)
                ]) : node.body;
                body.body.unshift(...init);
                node.params = newParams;
                node.body = body;
            }
        }
    };
}

//# sourceMappingURL=fix-hermes-v1-async-arrow-non-simple-params.js.map
