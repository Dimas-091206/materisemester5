/** Observes and reports file changes. */
export declare class FileNotifier {
    /** Project root to resolve the module IDs relative to. */
    private projectRoot;
    /** List of module IDs sorted by priority. Only the first file that exists will be observed. */
    private moduleIds;
    private settings;
    static instances: FileNotifier[];
    static stopAll(): void;
    private unsubscribe;
    constructor(
    /** Project root to resolve the module IDs relative to. */
    projectRoot: string, 
    /** List of module IDs sorted by priority. Only the first file that exists will be observed. */
    moduleIds: string[], settings?: {
        /** An additional warning message to add to the notice. */
        additionalWarning?: string;
    });
    /** Get the file in the project. */
    private resolveFilePath;
    startObserving(callback?: (cur: any, prev: any) => void): string | null;
    stopObserving(): void;
    /** Watch the file and warn to reload the CLI if it changes. */
    watchFile: (filePath: string, callback?: (cur: any, prev: any) => void) => string;
    private startWatchingFile;
}
