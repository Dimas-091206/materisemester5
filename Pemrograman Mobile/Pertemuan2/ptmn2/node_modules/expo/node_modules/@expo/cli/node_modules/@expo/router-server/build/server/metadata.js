"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "resolveMetadata", {
    enumerable: true,
    get: function() {
        return resolveMetadata;
    }
});
function _ctx() {
    const data = require("expo-router/_ctx");
    _ctx = function() {
        return data;
    };
    return data;
}
const _serialize = require("../utils/metadata/serialize");
function getGenerateMetadata(moduleExports) {
    return typeof moduleExports.generateMetadata === 'function' ? moduleExports.generateMetadata : null;
}
async function resolveMetadata(options) {
    const routeModule = await (0, _ctx().ctx)(options.route.file);
    if (!routeModule) {
        return null;
    }
    const generateMetadata = getGenerateMetadata(routeModule);
    if (!generateMetadata) {
        return null;
    }
    const metadata = await generateMetadata(options.request, options.params);
    if (metadata == null) {
        return null;
    }
    return {
        metadata,
        headNodes: (0, _serialize.serializeMetadataToReact)(metadata)
    };
}

//# sourceMappingURL=metadata.js.map
