export interface AdbDeviceRecord {
    serial: string;
    state: string;
    metadata: string[];
    transportId: string | undefined;
}
export declare function isAdbDeviceStateUsable(state: string): boolean;
export declare function parseAdbDeviceList(output: string): AdbDeviceRecord[];
