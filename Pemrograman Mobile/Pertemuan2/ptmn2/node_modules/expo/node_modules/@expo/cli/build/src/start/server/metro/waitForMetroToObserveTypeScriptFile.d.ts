import type { ChangeEvent } from '@expo/metro/metro-file-map';
import type MetroServer from '@expo/metro/metro/Server';
import type { ServerLike } from '../BundlerDevServer';
/**
 * Use the native file watcher / Metro ruleset to detect if a
 * TypeScript file is added to the project during development.
 */
export declare function waitForMetroToObserveTypeScriptFile(projectRoot: string, runner: {
    metro: MetroServer;
    server: ServerLike;
}, callback: () => Promise<void>): () => void;
export declare function observeFileChanges(runner: {
    metro: MetroServer;
    server: ServerLike;
}, files: string[], callback: () => void | Promise<void>): () => void;
export declare function observeAnyFileChanges(runner: {
    metro: MetroServer;
    server: ServerLike;
}, callback: (events: ChangeEvent) => void | Promise<void>): () => void;
