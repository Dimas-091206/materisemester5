import type { CodeFrame, StackFrame as MetroStackFrame } from './log-box/LogBoxSymbolication';
export declare function logMetroErrorWithStack(projectRoot: string, { stack, codeFrame, error, }: {
    stack: MetroStackFrame[];
    codeFrame?: CodeFrame;
    error: Error;
}): Promise<void>;
export declare function getStackAsFormattedLog(projectRoot: string, { stack, codeFrame, error, showCollapsedFrames, }: {
    stack: MetroStackFrame[];
    codeFrame?: CodeFrame;
    error?: Error;
    showCollapsedFrames?: boolean;
}): {
    isFallback: boolean;
    stack: string;
};
export declare const IS_METRO_BUNDLE_ERROR_SYMBOL: unique symbol;
declare const HAS_LOGGED_SYMBOL: unique symbol;
export declare function logMetroError(projectRoot: string, { error, }: {
    error: Error & {
        [HAS_LOGGED_SYMBOL]?: boolean;
    };
}): Promise<void>;
/** @returns the html required to render the static metro error as an SPA. */
export declare function logMetroErrorAsync({ error, projectRoot, }: {
    error: Error;
    projectRoot: string;
}): Promise<void>;
/** @returns the html required to render the static metro error as an SPA. */
export declare function getErrorOverlayHtmlAsync({ error, projectRoot, routerRoot, }: {
    error: Error;
    projectRoot: string;
    routerRoot: string;
}): Promise<string>;
export declare function dropStackIfContainsCodeFrame(err: unknown): void;
/**
 * Tests given string on presence of ` [num] |` at the start of any line.
 * Returns `false` for undefined or empty strings.
 */
export declare function likelyContainsCodeFrame(message: string | undefined): boolean;
/**
 * Walks thru the error cause chain and attaches the import stack to the root error message.
 * Removes the error stack for import and syntax errors.
 */
export declare const attachImportStackToRootMessage: (err: unknown, importStack?: string | undefined) => void;
/**
 * Walks thru the error cause chain and returns the nearest import stack.
 * If the import stack is not found, it returns `undefined`.
 */
export declare const nearestImportStack: (err: unknown, root?: unknown) => string | undefined;
export {};
