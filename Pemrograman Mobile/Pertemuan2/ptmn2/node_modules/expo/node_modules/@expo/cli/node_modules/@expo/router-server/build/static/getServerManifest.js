/**
 * Copyright © 2024 650 Industries.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */ "use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
0 && (module.exports = {
    getBuildTimeServerManifestAsync: null,
    getManifest: null
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: Object.getOwnPropertyDescriptor(all, name).get
    });
}
_export(exports, {
    get getBuildTimeServerManifestAsync () {
        return getBuildTimeServerManifestAsync;
    },
    get getManifest () {
        return getManifest;
    }
});
function _ctx() {
    const data = require("expo-router/_ctx");
    _ctx = function() {
        return data;
    };
    return data;
}
function _routing() {
    const data = require("expo-router/internal/routing");
    _routing = function() {
        return data;
    };
    return data;
}
const _getServerManifest = require("../getServerManifest");
const _loadStaticParamsAsync = require("../loadStaticParamsAsync");
async function getBuildTimeServerManifestAsync(options = {}) {
    const routeTree = (0, _routing().getRoutes)(_ctx().ctx, {
        platform: 'web',
        ...options
    });
    // Evaluate all static params; skip for SSR mode where routes are matched at runtime
    if (routeTree && !options.skipStaticParams) {
        await (0, _loadStaticParamsAsync.loadStaticParamsAsync)(routeTree);
    }
    // NOTE(@kitten): The route tree can be `null` and should be accepted if the app
    // has no route tree set up. This can happen when we build against a project that
    // isn't an expo-router project or not fully set up yet, but has expo-router options
    // in the app.json already
    return (0, _getServerManifest.getServerManifest)(routeTree, options);
}
async function getManifest(options = {}) {
    const routeTree = (0, _routing().getRoutes)(_ctx().ctx, {
        preserveApiRoutes: true,
        preserveRedirectAndRewrites: true,
        platform: 'web',
        ...options
    });
    if (routeTree) {
        // Evaluate all static params
        await (0, _loadStaticParamsAsync.loadStaticParamsAsync)(routeTree);
    }
    // NOTE(@kitten): The route tree can be `null` and should be accepted if the app
    // has no route tree set up. This can happen when we build against a project that
    // isn't an expo-router project or not fully set up yet, but has expo-router options
    // in the app.json already
    return (0, _routing().getReactNavigationConfig)(routeTree, false);
}

//# sourceMappingURL=getServerManifest.js.map
