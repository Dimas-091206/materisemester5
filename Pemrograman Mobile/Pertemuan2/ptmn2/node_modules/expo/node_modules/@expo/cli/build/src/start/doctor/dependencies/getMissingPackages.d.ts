export type ResolvedPackage = {
    /** Module ID pointing to the library `package.json`. */
    file: string;
    /** NPM package name. */
    pkg: string;
    /** Required version range. */
    version?: string;
    /** If the dependency should be installed as a `devDependency` */
    dev?: boolean;
};
/** Given a set of required packages, this method returns a list of missing packages. */
export declare function collectMissingPackages(projectRoot: string, requiredPackages: ResolvedPackage[]): {
    missing: ResolvedPackage[];
    resolutions: Record<string, string>;
};
export declare function versionSatisfiesRequiredPackage(packageJsonFilePath: string, resolvedPackage: Pick<ResolvedPackage, 'version' | 'pkg'>): boolean;
/**
 * Collect missing packages given a list of required packages.
 * Any missing packages will be versioned to the known versions for the current SDK.
 *
 * @param projectRoot
 * @param props.requiredPackages list of required packages to check for
 * @returns list of missing packages and resolutions to existing packages.
 */
export declare function getMissingPackagesAsync(projectRoot: string, { sdkVersion, requiredPackages, }: {
    sdkVersion?: string;
    requiredPackages: ResolvedPackage[];
}): Promise<{
    missing: ResolvedPackage[];
    resolutions: Record<string, string>;
}>;
export declare function mutatePackagesWithKnownVersionsAsync(projectRoot: string, sdkVersion: string | undefined, packages: ResolvedPackage[]): Promise<ResolvedPackage[]>;
