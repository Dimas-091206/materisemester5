export type Options = {
    privateKeyPath: string | null;
    android: boolean;
    web: boolean;
    ios: boolean;
    offline: boolean;
    clear: boolean;
    dev: boolean;
    https: boolean;
    maxWorkers: number;
    port: number;
    /** Should instruct the bundler to create minified bundles. */
    minify: boolean;
    devClient: boolean;
    scheme: string | null;
    host: 'localhost' | 'lan' | 'tunnel';
};
export declare function resolveOptionsAsync(projectRoot: string, args: any): Promise<Options>;
export declare function resolveSchemeAsync(projectRoot: string, options: {
    scheme?: string;
    devClient?: boolean;
}): Promise<string | null>;
/** Resolve and assert host type options. */
export declare function resolveHostType(options: {
    host?: string;
    offline?: boolean;
    lan?: boolean;
    localhost?: boolean;
    tunnel?: boolean;
}): 'lan' | 'tunnel' | 'localhost';
/** Resolve the port options for all supported bundlers. */
export declare function resolvePortsAsync(projectRoot: string, options: Partial<Pick<Options, 'port' | 'devClient'>>, bundlers: ('metro' | 'webpack')[]): Promise<{
    metroPort: number;
    webpackPort?: number;
}>;
