import type { ProjectInfo } from '../XcodeBuild.types';
/** Return the path and type of Xcode project in the given folder. */
export declare function resolveXcodeProject(projectRoot: string): ProjectInfo;
