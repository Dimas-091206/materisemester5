import { glob, type GlobOptions } from 'glob';
/**
 * Finds all matching files.
 * @deprecated Use `glob` directly instead.
 */
export declare const everyMatchAsync: typeof glob;
/** Bails out early after finding the first matching file. */
export declare function anyMatchAsync(pattern: string, options: Omit<GlobOptions, 'withFileTypes' | 'signal'>): Promise<string[]>;
