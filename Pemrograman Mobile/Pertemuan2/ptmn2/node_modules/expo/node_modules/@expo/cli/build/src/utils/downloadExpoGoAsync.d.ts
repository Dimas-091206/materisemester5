import type { SDKVersion } from '../api/getVersions';
declare const platformSettings: {
    readonly ios: {
        readonly versionsKey: "iosClientUrl";
        readonly getFilePath: (filename: string) => string;
        readonly shouldExtractResults: true;
    };
    readonly android: {
        readonly versionsKey: "androidClientUrl";
        readonly getFilePath: (filename: string) => string;
        readonly shouldExtractResults: false;
    };
};
/**
 * @internal exposed for testing.
 * @returns the matching `SDKVersion` object from the Expo API.
 */
export declare function getExpoGoVersionEntryAsync(sdkVersion: string): Promise<SDKVersion>;
/** Remove cached Expo Go apps (.apk / .app) that are older than `maxAge` from the given directory. */
export declare function cleanupOldExpoGoCacheEntriesAsync(cacheDirectory: string, maxAgeMs?: number): Promise<void>;
/** Download the Expo Go app from the Expo servers (if only it was this easy for every app). */
export declare function downloadExpoGoAsync(platform: keyof typeof platformSettings, { url, sdkVersion, }: {
    url?: string;
    sdkVersion: string;
}): Promise<string>;
export {};
