import type { ExpoConfig, PackageJSONConfig } from '@expo/config';
export type IncorrectDependency = {
    packageName: string;
    packageType: 'dependencies' | 'devDependencies';
    expectedVersionOrRange: string;
    actualVersion: string;
};
/**
 * Print a list of incorrect dependency versions.
 * This only checks dependencies when not running in offline mode.
 *
 * @param projectRoot Expo project root.
 * @param exp Expo project config.
 * @param pkg Project's `package.json`.
 * @param packagesToCheck A list of packages to check, if undefined or empty, all will be checked.
 * @returns `true` if there are no incorrect dependencies.
 */
export declare function validateDependenciesVersionsAsync(projectRoot: string, exp: Pick<ExpoConfig, 'sdkVersion'>, pkg: PackageJSONConfig, packagesToCheck?: string[]): Promise<boolean | null>;
export declare function logIncorrectDependencies(incorrectDeps: IncorrectDependency[]): boolean;
/**
 * Return a list of versioned dependencies for the project SDK version.
 *
 * @param projectRoot Expo project root.
 * @param exp Expo project config.
 * @param pkg Project's `package.json`.
 * @param packagesToCheck A list of packages to check, if undefined or empty, all will be checked.
 * @returns A list of incorrect dependencies.
 */
export declare function getVersionedDependenciesAsync(projectRoot: string, exp: Pick<ExpoConfig, 'sdkVersion'>, pkg: PackageJSONConfig, packagesToCheck?: string[]): Promise<IncorrectDependency[]>;
export declare function isDependencyVersionIncorrect(packageName: string, actualVersion: string, expectedVersionOrRange?: string): boolean;
