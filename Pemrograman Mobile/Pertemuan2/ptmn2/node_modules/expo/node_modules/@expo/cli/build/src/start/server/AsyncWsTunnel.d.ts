export interface AsyncWsTunnelOptions {
    useExpoAccount?: boolean;
}
export declare class AsyncWsTunnel {
    private projectRoot;
    private port;
    private options;
    private serverUrl;
    constructor(projectRoot: string, port: number, options?: AsyncWsTunnelOptions);
    getActiveUrl(): string | null;
    startAsync(): Promise<void>;
    private getTunnelOptionsAsync;
    private getExpoAccountTunnelOptionsAsync;
    stopAsync(): Promise<void>;
}
export declare function getExpoAccountTunnelUrlAsync(projectRoot: string): Promise<string | null>;
