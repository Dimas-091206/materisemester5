import type { ExpoConfig } from '@expo/config';
import type { ServerRequest, ServerResponse } from './server.types';
/** Check if hostname matches "localhost" exactly, the local IPv6,
 * a local IPV4 in the 127.0.0.0/8 range, or an IPv6-to-4 range
 * (starting with ::ffff: and ending in an IPv4) */
export declare const _isLocalHostname: (hostname: string) => boolean;
export declare function createCorsMiddleware(exp: ExpoConfig): (req: ServerRequest, res: ServerResponse, next: (err?: Error) => void) => void;
