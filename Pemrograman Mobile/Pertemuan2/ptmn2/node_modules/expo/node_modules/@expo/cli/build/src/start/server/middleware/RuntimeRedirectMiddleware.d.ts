import { ExpoMiddleware } from './ExpoMiddleware';
import type { ForwardedRequestInfo } from './resolveForwarded';
import type { RuntimePlatform } from './resolvePlatform';
import type { ServerRequest, ServerResponse } from './server.types';
export declare const LinkEndpoint = "/_expo/link";
/** Runtime to target: expo = Expo Go, custom = Dev Client. */
type RuntimeTarget = 'expo' | 'custom';
export type DeepLinkHandler = (props: {
    runtime: RuntimeTarget;
    platform: RuntimePlatform;
}) => void | Promise<void>;
export declare class RuntimeRedirectMiddleware extends ExpoMiddleware {
    protected projectRoot: string;
    protected options: {
        onDeepLink?: DeepLinkHandler;
        getLocation: (props: {
            runtime: RuntimeTarget;
            forwarded: ForwardedRequestInfo | null;
        }) => string | null | undefined;
    };
    constructor(projectRoot: string, options: {
        onDeepLink?: DeepLinkHandler;
        getLocation: (props: {
            runtime: RuntimeTarget;
            forwarded: ForwardedRequestInfo | null;
        }) => string | null | undefined;
    });
    handleRequestAsync(req: ServerRequest, res: ServerResponse): Promise<void>;
}
export {};
