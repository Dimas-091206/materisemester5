import { CommandError } from '../../utils/errors';
/** An error that is memoized and asserted whenever a Prerequisite.assertAsync is subsequently called. */
export declare class PrerequisiteCommandError extends CommandError {
    constructor(code: string, message?: string);
}
export declare class Prerequisite<T = void, TProps = void> {
    /** Memoized results of `assertImplementation` */
    private _assertAsync;
    constructor();
    /** An optional warning to call before running the memoized assertion.  */
    protected cachedError?: PrerequisiteCommandError;
    /** Reset the assertion memo and warning message. */
    resetAssertion(props: TProps): void;
    assertAsync(props: TProps): Promise<T>;
    /** Exposed for testing. */
    assertImplementation(props: TProps): Promise<T>;
}
/** A prerequisite that is project specific. */
export declare class ProjectPrerequisite<T = void, TProps = void> extends Prerequisite<T, TProps> {
    protected projectRoot: string;
    constructor(projectRoot: string);
}
