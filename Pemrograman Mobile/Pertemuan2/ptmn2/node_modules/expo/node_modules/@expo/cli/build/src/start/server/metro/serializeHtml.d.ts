import type { SerialAsset } from '@expo/metro-config/build/serializer/serializerAssets';
import { type StaticContentAssets } from '@expo/router-server/build/utils/html';
import type { RouteNode } from 'expo-router/build/Route';
export declare function serializeHtmlWithAssets({ resources, template, devBundleUrl, baseUrl, route, isExporting, hydrate, }: {
    resources: SerialAsset[];
    template: string;
    /** asset prefix used for deploying to non-standard origins like GitHub pages. */
    baseUrl: string;
    devBundleUrl?: string;
    route?: RouteNode;
    isExporting: boolean;
    hydrate?: boolean;
}): string;
export declare function serialAssetsToStaticContentAssets(assets: SerialAsset[], { isExporting, baseUrl, bundleUrl, route, favicon, }: {
    isExporting: boolean;
    baseUrl: string;
    bundleUrl?: string;
    route?: RouteNode;
    favicon?: string;
}): StaticContentAssets;
/**
 * Sorts matched async assets by their matching `entryPoint` in the route's `entryPoints` array.
 * This ensures layout chunks come before page chunks.
 */
export declare function sortMatchedAssetsByEntryPoints(matchedAssets: SerialAsset[], entryPoints: string[]): SerialAsset[];
/**
 * Sorts assets based on the requires tree. DFS order.
 */
export declare function assetsRequiresSort(assets: SerialAsset[]): SerialAsset[];
