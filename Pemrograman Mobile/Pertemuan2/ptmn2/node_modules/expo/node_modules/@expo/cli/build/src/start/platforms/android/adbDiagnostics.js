"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
0 && (module.exports = {
    createAdbOperationError: null,
    formatAdbDeviceError: null,
    formatAdbDiscoveryError: null,
    isAdbDeviceDisconnectedError: null,
    shouldProbeAdbHost: null
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: Object.getOwnPropertyDescriptor(all, name).get
    });
}
_export(exports, {
    get createAdbOperationError () {
        return createAdbOperationError;
    },
    get formatAdbDeviceError () {
        return formatAdbDeviceError;
    },
    get formatAdbDiscoveryError () {
        return formatAdbDiscoveryError;
    },
    get isAdbDeviceDisconnectedError () {
        return isAdbDeviceDisconnectedError;
    },
    get shouldProbeAdbHost () {
        return shouldProbeAdbHost;
    }
});
const _errors = require("../../../utils/errors");
const _adbEndpoint = require("./adbEndpoint");
const _adbProcess = require("./adbProcess");
function formatAdbError(error, endpoint, hostProbe, device) {
    const details = [
        error instanceof Error ? error.message : String(error)
    ];
    // NOTE(@kitten): Side-effect marker from `hasSideEffects` in ./adbProcess.ts
    if (typeof error === 'object' && error != null && 'remoteCompletionUnknown' in error && error.remoteCompletionUnknown === true) {
        details.push('The operation may have completed on the device. Check before trying again.');
    }
    if (hostProbe && !(error instanceof _adbProcess.AdbProcessError && error.spawnFailed)) {
        const hostProbeAdvice = formatHostProbeAdvice(endpoint, hostProbe);
        if (hostProbeAdvice) {
            details.push(hostProbeAdvice);
        }
    }
    details.push(...getAdvice(error, device));
    return details.join('\n');
}
function formatAdbDeviceError(error, device) {
    return formatAdbError(error, undefined, undefined, device);
}
function formatAdbDiscoveryError(error, endpoint, hostProbe) {
    return formatAdbError(error, endpoint, hostProbe);
}
function shouldProbeAdbHost(error) {
    return error instanceof _adbProcess.AdbProcessWaitError || error instanceof Error && /(?:cannot connect|connection refused|server didn't ACK|daemon (?:not running|still not running)|smartsocket)/i.test(error.message);
}
function createAdbOperationError(code, error, device) {
    const commandError = new _errors.CommandError(code, formatAdbError(error, undefined, undefined, device));
    commandError.cause = error;
    return commandError;
}
function formatHostProbeAdvice(endpoint, result) {
    const formattedEndpoint = endpoint ? (0, _adbEndpoint.formatAdbEndpoint)(endpoint) : 'the configured endpoint';
    switch(result.kind){
        case 'version':
            // A successful probe after an operation failed cannot explain the earlier failure.
            return null;
        case 'connected-no-reply':
            return `The ADB server at ${formattedEndpoint} is not responding. Check or restart that server, then try again.`;
        case 'connection-refused':
        case 'connection-failure':
            return `Could not connect to the ADB server at ${formattedEndpoint}. Check the server configuration and try again.`;
        case 'adb-failure':
            return `The ADB server rejected Expo's health check: ${result.message}. Check the server configuration and try again.`;
        case 'invalid-protocol':
            return `The configured endpoint at ${formattedEndpoint} is not an ADB server. Check ADB_SERVER_SOCKET and try again.`;
        case 'unsupported':
            return 'Expo could not check the configured ADB server. Check ADB_SERVER_SOCKET and try again.';
    }
}
function getAdvice(error, device) {
    const advice = [];
    if (error instanceof _adbProcess.AdbProcessError && error.spawnFailed) {
        advice.push('Check that Android SDK Platform-Tools is installed and ADB is available.');
    }
    if (device?.state === 'offline' && device.type === 'emulator') {
        advice.push('Wait until ADB reports the emulator as ready, then try again.');
    } else if (device?.state === 'offline') {
        advice.push(`Reconnect ${device.pid ? `device ${device.pid}` : 'the device'} and try again.`);
    } else if (device?.state === 'unauthorized') {
        advice.push('Authorize this computer on the device, then try again: https://expo.fyi/authorize-android-device');
    } else if (device?.state === 'no permissions') {
        advice.push('Set up permission to access the device, then reconnect it. On Linux, configure the appropriate udev rules; on Windows, install the appropriate USB driver: https://developer.android.com/studio/run/device.html');
    } else if (device?.state && device.state !== 'device') {
        advice.push('Wait until ADB reports the device as ready, then try again.');
    }
    if ((!device?.state || device.state === 'device') && isAdbDeviceDisconnectedError(error)) {
        advice.push('The device disconnected. Reconnect it and try again.');
    }
    return advice;
}
function isAdbDeviceDisconnectedError(error) {
    return error instanceof Error && /device (?:not found|offline|still authorizing)|no devices\/emulators found|transport (?:is closed|error)/i.test(error.message);
}

//# sourceMappingURL=adbDiagnostics.js.map
