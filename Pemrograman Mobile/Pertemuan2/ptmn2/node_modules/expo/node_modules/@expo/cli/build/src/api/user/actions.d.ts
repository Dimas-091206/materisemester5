import type { Actor } from './user';
/** Show login prompt while prompting for missing credentials. */
export declare function showLoginPromptAsync({ printNewLine, otp, ...options }?: {
    printNewLine?: boolean;
    username?: string;
    password?: string;
    otp?: string;
    sso?: boolean | undefined;
    browser?: boolean | undefined;
}): Promise<void>;
export declare function tryGetUserAsync(): Promise<Actor | null>;
