/**
 * Copyright © 2024 650 Industries.
 * Copyright © 2024 2023 Daishi Kato
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */ "use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
0 && (module.exports = {
    REQUEST_HEADERS: null,
    defineEntries: null,
    getContext: null,
    rerender: null,
    runWithRenderStore: null
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: Object.getOwnPropertyDescriptor(all, name).get
    });
}
_export(exports, {
    get REQUEST_HEADERS () {
        return REQUEST_HEADERS;
    },
    get defineEntries () {
        return defineEntries;
    },
    get getContext () {
        return getContext;
    },
    get rerender () {
        return rerender;
    },
    get runWithRenderStore () {
        return runWithRenderStore;
    }
});
function _nodeasync_hooks() {
    const data = require("node:async_hooks");
    _nodeasync_hooks = function() {
        return data;
    };
    return data;
}
const REQUEST_HEADERS = '__expo_requestHeaders';
function defineEntries(renderEntries, getBuildConfig, getSsrConfig) {
    return {
        renderEntries,
        getBuildConfig,
        getSsrConfig
    };
}
// Stashed on globalThis so separately-loaded copies of this module (e.g. the renderer and a
// server-action module loaded via Metro's ssrLoadModule) share one storage instance.
function getRenderStorage() {
    return globalThis.__EXPO_RSC_STORE__ ??= new (_nodeasync_hooks()).AsyncLocalStorage();
}
const runWithRenderStore = (renderStore, fn)=>{
    return getRenderStorage().run(renderStore, fn);
};
async function rerender(input, params) {
    const renderStore = getRenderStorage().getStore();
    if (!renderStore) {
        throw new Error('Render store is not available for rerender');
    }
    renderStore.rerender(input, params);
}
function getContext() {
    const renderStore = getRenderStorage().getStore();
    if (!renderStore) {
        throw new Error('Render store is not available for accessing context');
    }
    return renderStore.context;
}

//# sourceMappingURL=server.js.map
