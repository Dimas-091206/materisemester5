import type { ServerNext, ServerRequest, ServerResponse } from './server.types';
/**
 * Create a web-only middleware which redirects to the index middleware without losing the path component.
 * This is useful for things like React Navigation which need to render the index.html and then direct the user in-memory.
 */
export declare class HistoryFallbackMiddleware {
    private indexMiddleware;
    constructor(indexMiddleware: (req: ServerRequest, res: ServerResponse, next: ServerNext) => Promise<void>);
    getHandler(): (req: ServerRequest, res: ServerResponse, next: any) => any;
}
