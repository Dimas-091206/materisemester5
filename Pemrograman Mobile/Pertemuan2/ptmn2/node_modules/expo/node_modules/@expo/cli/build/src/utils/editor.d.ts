interface Editor {
    id: string;
    name: string;
    binary: string;
    isTerminalEditor: boolean;
    isOSXOnly?: boolean;
    paths: string[];
    keywords: string[];
}
/** Guess what the default editor is and default to VSCode. */
export declare function guessEditor(): Editor | null;
export declare function guessFallbackVisualEditor(): Promise<Editor | null>;
/** Open a file path in a given editor. */
export declare function openInEditorAsync(path: string, lineNumber?: number): Promise<boolean>;
export {};
