import type { ModPlatform } from '@expo/config-plugins';
export declare function validateTemplatePlatforms({ templateDirectory, platforms, }: {
    templateDirectory: string;
    platforms: ModPlatform[];
}): (keyof import("@expo/config-plugins").ModConfig)[];
