import { Prerequisite } from '../Prerequisite';
export declare class XcrunPrerequisite extends Prerequisite {
    static instance: XcrunPrerequisite;
    /**
     * Ensure Xcode CLI is installed.
     */
    assertImplementation(): Promise<void>;
}
