import type { Device } from './adb';
/** Get a list of all devices including offline emulators. Asserts if no devices are available. */
export declare function getDevicesAsync(): Promise<Device[]>;
export declare function mergeDevices(attachedDevices: Device[], avds: Device[]): Device[];
