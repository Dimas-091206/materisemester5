"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
0 && (module.exports = {
    copyAsync: null,
    copySync: null,
    directoryExistsAsync: null,
    directoryExistsSync: null,
    ensureDirectory: null,
    ensureDirectoryAsync: null,
    fileExistsAsync: null,
    fileExistsSync: null,
    isPathInside: null,
    maybeRealpathSync: null,
    removeAsync: null
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: Object.getOwnPropertyDescriptor(all, name).get
    });
}
_export(exports, {
    get copyAsync () {
        return copyAsync;
    },
    get copySync () {
        return copySync;
    },
    get directoryExistsAsync () {
        return directoryExistsAsync;
    },
    get directoryExistsSync () {
        return directoryExistsSync;
    },
    get ensureDirectory () {
        return ensureDirectory;
    },
    get ensureDirectoryAsync () {
        return ensureDirectoryAsync;
    },
    get fileExistsAsync () {
        return fileExistsAsync;
    },
    get fileExistsSync () {
        return fileExistsSync;
    },
    get isPathInside () {
        return isPathInside;
    },
    get maybeRealpathSync () {
        return maybeRealpathSync;
    },
    get removeAsync () {
        return removeAsync;
    }
});
function _fs() {
    const data = /*#__PURE__*/ _interop_require_default(require("fs"));
    _fs = function() {
        return data;
    };
    return data;
}
function _path() {
    const data = /*#__PURE__*/ _interop_require_default(require("path"));
    _path = function() {
        return data;
    };
    return data;
}
function _interop_require_default(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
function fileExistsSync(file) {
    return !!_fs().default.statSync(file, {
        throwIfNoEntry: false
    })?.isFile();
}
function directoryExistsSync(file) {
    return !!_fs().default.statSync(file, {
        throwIfNoEntry: false
    })?.isDirectory();
}
async function directoryExistsAsync(file) {
    return (await _fs().default.promises.stat(file).catch(()=>null))?.isDirectory() ?? false;
}
async function fileExistsAsync(file) {
    return (await _fs().default.promises.stat(file).catch(()=>null))?.isFile() ?? false;
}
const ensureDirectoryAsync = (path)=>_fs().default.promises.mkdir(path, {
        recursive: true
    });
const ensureDirectory = (path)=>{
    _fs().default.mkdirSync(path, {
        recursive: true
    });
};
const copySync = (src, dest)=>{
    const destParent = _path().default.dirname(dest);
    if (!_fs().default.existsSync(destParent)) ensureDirectory(destParent);
    _fs().default.cpSync(src, dest, {
        recursive: true,
        force: true
    });
};
const copyAsync = async (src, dest)=>{
    const destParent = _path().default.dirname(dest);
    if (!_fs().default.existsSync(destParent)) {
        await _fs().default.promises.mkdir(destParent, {
            recursive: true
        });
    }
    await _fs().default.promises.cp(src, dest, {
        recursive: true,
        force: true
    });
};
const removeAsync = (path)=>{
    return _fs().default.promises.rm(path, {
        recursive: true,
        force: true
    });
};
function maybeRealpathSync(target) {
    try {
        return _fs().default.realpathSync(target);
    } catch  {
        return null;
    }
}
function isPathInside(child, parent) {
    const relative = _path().default.relative(parent, child);
    return !!relative && !relative.startsWith('..') && !_path().default.isAbsolute(relative);
}

//# sourceMappingURL=dir.js.map
