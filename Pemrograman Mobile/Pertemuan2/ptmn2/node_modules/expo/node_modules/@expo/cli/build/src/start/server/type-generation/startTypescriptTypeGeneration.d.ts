import type Server from '@expo/metro/metro/Server';
import type { ServerLike } from '../BundlerDevServer';
export interface TypeScriptTypeGenerationOptions {
    server?: ServerLike;
    metro?: Server | null;
    projectRoot: string;
}
declare module '2g' {
    interface EventRegistry {
        'typegen:done': {
            enabled: boolean;
        };
    }
}
/** Setup all requisite features for statically typed routes in Expo Router v2 / SDK +49. */
export declare function startTypescriptTypeGenerationAsync({ metro, projectRoot, server, }: TypeScriptTypeGenerationOptions): Promise<void>;
