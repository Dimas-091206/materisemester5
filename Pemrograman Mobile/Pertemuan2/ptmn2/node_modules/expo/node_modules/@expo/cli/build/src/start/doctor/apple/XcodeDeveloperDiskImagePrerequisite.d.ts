import { Prerequisite } from '../Prerequisite';
export declare class XcodeDeveloperDiskImagePrerequisite extends Prerequisite<string, {
    version: string;
}> {
    static instance: XcodeDeveloperDiskImagePrerequisite;
    assertImplementation({ version }: {
        version: string;
    }): Promise<string>;
}
