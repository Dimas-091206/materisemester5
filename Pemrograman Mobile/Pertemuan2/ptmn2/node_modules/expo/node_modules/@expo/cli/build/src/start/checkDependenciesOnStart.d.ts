import type { ExpoConfig, PackageJSONConfig } from '@expo/config';
export interface DependencyCheckResult {
    expo?: {
        actualVersion: string;
        expectedVersionOrRange: string;
    };
    otherCount: number;
}
export interface DependencyCheckRef {
    result: DependencyCheckResult | null;
    promise: Promise<DependencyCheckResult | null>;
}
export declare function checkDependencies(projectRoot: string, exp: Pick<ExpoConfig, 'sdkVersion'>, pkg: PackageJSONConfig): DependencyCheckRef;
/** Print the condensed dependency check messages to the terminal. */
export declare function getDependencyCheckMessage(result: DependencyCheckResult | null | undefined): string[];
/** Print the condensed dependency check messages to the terminal. */
export declare function printDependencyCheckResult(result: DependencyCheckResult): void;
