"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
0 && (module.exports = {
    event: null,
    getConfigEnvMode: null,
    getEnvFiles: null,
    loadEnvFiles: null,
    reloadEnvFiles: null,
    setNodeEnv: null
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: Object.getOwnPropertyDescriptor(all, name).get
    });
}
_export(exports, {
    get event () {
        return event;
    },
    get getConfigEnvMode () {
        return getConfigEnvMode;
    },
    get getEnvFiles () {
        return getEnvFiles;
    },
    get loadEnvFiles () {
        return loadEnvFiles;
    },
    get reloadEnvFiles () {
        return reloadEnvFiles;
    },
    get setNodeEnv () {
        return setNodeEnv;
    }
});
function _2g() {
    const data = require("2g");
    _2g = function() {
        return data;
    };
    return data;
}
function _env() {
    const data = /*#__PURE__*/ _interop_require_wildcard(require("@expo/env"));
    _env = function() {
        return data;
    };
    return data;
}
function _nodepath() {
    const data = /*#__PURE__*/ _interop_require_default(require("node:path"));
    _nodepath = function() {
        return data;
    };
    return data;
}
const _env1 = require("./env");
const _errors = require("./errors");
const _interactive = require("./interactive");
function _interop_require_default(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
function _getRequireWildcardCache(nodeInterop) {
    if (typeof WeakMap !== "function") return null;
    var cacheBabelInterop = new WeakMap();
    var cacheNodeInterop = new WeakMap();
    return (_getRequireWildcardCache = function(nodeInterop) {
        return nodeInterop ? cacheNodeInterop : cacheBabelInterop;
    })(nodeInterop);
}
function _interop_require_wildcard(obj, nodeInterop) {
    if (!nodeInterop && obj && obj.__esModule) {
        return obj;
    }
    if (obj === null || typeof obj !== "object" && typeof obj !== "function") {
        return {
            default: obj
        };
    }
    var cache = _getRequireWildcardCache(nodeInterop);
    if (cache && cache.has(obj)) {
        return cache.get(obj);
    }
    var newObj = {
        __proto__: null
    };
    var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
    for(var key in obj){
        if (key !== "default" && Object.prototype.hasOwnProperty.call(obj, key)) {
            var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
            if (desc && (desc.get || desc.set)) {
                Object.defineProperty(newObj, key, desc);
            } else {
                newObj[key] = obj[key];
            }
        }
    }
    newObj.default = obj;
    if (cache) {
        cache.set(obj, newObj);
    }
    return newObj;
}
const event = (0, _2g().events)('env');
/** Defer relativizing a list of paths until the event is written. */ function relativeFiles(files) {
    return {
        toJSON: ()=>files.map((file)=>event.path(file).toJSON())
    };
}
function setNodeEnv(mode) {
    _env().setNodeEnv(mode);
    process.env.BABEL_ENV = process.env.BABEL_ENV || mode;
    globalThis.__DEV__ = mode === 'development';
    event('mode', {
        nodeEnv: mode,
        babelEnv: process.env.BABEL_ENV,
        mode
    });
}
function getConfigEnvMode() {
    try {
        // Older EAS Build versions do not pass __EXPO_CONFIG_MODE.
        return _env().consumeConfigEnvMode() ?? (_env1.env.EAS_BUILD ? 'production' : 'development');
    } catch (error) {
        throw new _errors.CommandError('BAD_ARGS', error instanceof Error ? error.message : 'Invalid __EXPO_CONFIG_MODE value.');
    }
}
let prevEnvKeys;
function loadEnvFiles(projectRoot, options) {
    setNodeEnv(options.mode);
    const params = {
        ...options,
        silent: !!options.silent || (0, _interactive.shouldReduceLogs)(),
        force: !!options.force,
        systemEnv: process.env
    };
    const envInfo = _env().loadProjectEnv(projectRoot, params);
    const envOutput = {};
    if (envInfo.result === 'loaded') {
        prevEnvKeys = new Set();
        for (const key of envInfo.loaded){
            envOutput[key] = envInfo.env[key] ?? undefined;
            prevEnvKeys.add(key);
        }
    }
    if (envInfo.result === 'loaded') {
        event('load', {
            mode: params.mode,
            files: relativeFiles(envInfo.files),
            keys: Object.keys(envOutput)
        });
    }
    if (!params.silent) {
        _env().logLoadedEnv(envInfo, params);
    }
    return process.env;
}
function getEnvFiles(projectRoot, mode) {
    return _env().getEnvFiles({
        mode
    }).map((fileName)=>_nodepath().default.join(projectRoot, fileName));
}
function reloadEnvFiles(projectRoot, mode) {
    setNodeEnv(mode);
    const isEnabled = _env().isEnabled();
    if (isEnabled) {
        const params = {
            force: true,
            silent: true,
            mode,
            systemEnv: process.env
        };
        // We use a global tracker to allow overwrites of env vars we set ourselves
        const envInfo = _env().parseProjectEnv(projectRoot, params);
        const envOutput = {};
        for(const key in envInfo.env){
            const value = envInfo.env[key];
            if (process.env[key] !== value) {
                if (typeof process.env[key] === 'undefined' || (!prevEnvKeys || prevEnvKeys.has(key)) && process.env[key] !== value) {
                    (prevEnvKeys ||= new Set()).add(key);
                    process.env[key] = envInfo.env[key];
                    envOutput[key] = value ?? undefined;
                }
            }
        }
        event('load', {
            mode: params.mode,
            files: relativeFiles(envInfo.files),
            keys: Object.keys(envOutput)
        });
    }
}

//# sourceMappingURL=nodeEnv.js.map
