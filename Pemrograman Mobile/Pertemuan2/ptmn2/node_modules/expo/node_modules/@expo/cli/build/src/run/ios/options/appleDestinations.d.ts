import type { OSType } from '../../../start/platforms/ios/simctl';
import type { BuildProps } from '../XcodeBuild.types';
export declare function resolveDestinationsAsync(props: Pick<BuildProps, 'configuration' | 'scheme' | 'xcodeProject'>): Promise<{
    name: string;
    osType: OSType;
    osVersion: string;
    udid: string;
}[]>;
