import type { OSType } from '../../../start/platforms/ios/simctl';
interface AnyDevice {
    name: string;
    osType: OSType;
    osVersion: string;
    udid: string;
    deviceType?: string;
}
/** Format a device for the prompt list. Exposed for testing. */
export declare function formatDeviceChoice(item: AnyDevice): {
    title: string;
    value: string;
};
/** Prompt to select a device from a searchable list of devices. */
export declare function promptDeviceAsync<T extends AnyDevice>(devices: T[]): Promise<T>;
export {};
