import type { SerializedError } from '2g';
import { DevToolsPlugin } from './DevToolsPlugin';
export declare const DevToolsPluginEndpoint = "/_expo/plugins";
declare module '2g' {
    interface EventRegistry {
        'expo:dev-tools-plugin:load': {
            plugins: {
                packageName: string;
                bannerTitle: string;
                cliBanner: boolean;
                webpageEndpoint: string | undefined;
            }[];
        };
        'expo:dev-tools-plugin:error': {
            packageName: string;
            error: SerializedError;
        };
    }
}
export declare const event: import("2g").EventLogger<"expo">;
export default class DevToolsPluginManager {
    private projectRoot;
    private plugins;
    constructor(projectRoot: string);
    queryPluginsAsync(): Promise<DevToolsPlugin[]>;
    queryPluginAsync(pluginName: string): Promise<DevToolsPlugin | null>;
    private queryAutolinkedPluginsAsync;
}
