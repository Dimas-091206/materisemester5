import type { DeviceManager } from './DeviceManager';
/** Given a platform, appId, and sdkVersion, this module will ensure that Expo Go is up-to-date on the provided device. */
export declare class ExpoGoInstaller<IDevice> {
    private platform;
    private appId;
    private sdkVersion;
    static cache: Record<string, boolean>;
    constructor(platform: 'ios' | 'android', appId: string, sdkVersion: string);
    /** Returns true if the installed app matching the previously provided `appId` is outdated. */
    isInstalledClientVersionMismatched(installedVersion: string | null, expectedExpoGoVersion: string | null): boolean;
    /** Returns the expected version of Expo Go given the project SDK Version. Exposed for testing. */
    getExpectedExpoGoClientVersionAsync(): Promise<string | null>;
    /** Returns a boolean indicating if Expo Go should be installed. Returns `true` if the app was uninstalled. */
    promptForUninstallExpoGoIfInstalledClientVersionMismatchedAndReturnShouldInstallAsync(deviceManager: DeviceManager<IDevice>, { containerPath }?: {
        containerPath?: string;
    }): Promise<boolean>;
    /** Check if a given device has Expo Go installed, if not then download and install it. */
    ensureAsync(deviceManager: DeviceManager<IDevice>): Promise<boolean>;
}
