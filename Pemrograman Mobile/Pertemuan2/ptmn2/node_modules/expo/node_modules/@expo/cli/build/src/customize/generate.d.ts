import type { DestinationResolutionProps } from './templates';
export declare function queryAndGenerateAsync(projectRoot: string, { files, props, extras, }: {
    files: string[];
    props: DestinationResolutionProps;
    /** Any extra props to pass to the install command. */
    extras: any[];
}): Promise<void>;
/** Select templates to generate then generate and install. */
export declare function selectAndGenerateAsync(projectRoot: string, { props, extras, }: {
    props: DestinationResolutionProps;
    /** Any extra props to pass to the install command. */
    extras: any[];
}): Promise<void>;
