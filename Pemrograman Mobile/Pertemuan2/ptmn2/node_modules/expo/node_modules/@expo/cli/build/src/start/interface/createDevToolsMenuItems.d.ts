import { openBrowserAsync } from '../../utils/open';
import type { ExpoChoice } from '../../utils/prompts';
import type { DevToolsPlugin } from '../server/DevToolsPlugin';
import type { DevToolsPluginCommand } from '../server/DevToolsPlugin.schema';
export interface MoreToolMenuItem extends ExpoChoice<string> {
    action?: () => unknown;
}
type MenuItemWithSubmenu = MoreToolMenuItem & {
    children?: MenuItemWithSubmenu[];
};
type CliExtensionMenuItemHandler = (plugin: DevToolsPlugin, cmd: DevToolsPluginCommand, metroServerOrigin: string) => Promise<void>;
/**
 * Creates the menu items for the DevTools interface.
 * @param plugins The list of DevTools plugins.
 * @param defaultServerUrl The default server URL.
 * @param metroServerOrigin The Metro server origin.
 * @param cliExtensionMenuItemHandlerFunc The function to handle CLI extension menu items.
 * @param openBrowserAsyncFunc The function to open the browser.
 * @returns The menu items for the DevTools interface.
 */
export declare const createDevToolsMenuItems: (plugins: DevToolsPlugin[], defaultServerUrl: string, metroServerOrigin: string, cliExtensionMenuItemHandlerFunc?: CliExtensionMenuItemHandler, // Used for injection when testing
openBrowserAsyncFunc?: typeof openBrowserAsync) => MenuItemWithSubmenu[];
export {};
