import type { AppIdResolver } from '../AppIdResolver';
import type { BaseOpenInCustomProps, BaseResolveDeviceProps } from '../PlatformManager';
import { PlatformManager } from '../PlatformManager';
import type { Device } from './simctl';
/** Manages launching apps on Apple simulators. */
export declare class ApplePlatformManager extends PlatformManager<Device> {
    protected projectRoot: string;
    protected port: number;
    constructor(projectRoot: string, port: number, options: {
        /** Get the base URL for the dev server hosting this platform manager. */
        getDevServerUrl: () => string | null;
        /** Expo Go URL. */
        getExpoGoUrl: () => string;
        /** Get redirect URL for native disambiguation. */
        getRedirectUrl: () => string | null;
        /** Dev Client */
        getCustomRuntimeUrl: (props?: {
            scheme?: string;
        }) => string | null;
    });
    openAsync(options: {
        runtime: 'expo' | 'web';
    } | {
        runtime: 'custom';
        props?: Partial<BaseOpenInCustomProps>;
    }, resolveSettings?: BaseResolveDeviceProps<Device>): Promise<{
        url: string;
    }>;
    _getAppIdResolver(): AppIdResolver;
    _resolveAlternativeLaunchUrl(applicationId: string, props?: Partial<BaseOpenInCustomProps>): string;
}
