import { NgrokResolver } from '../doctor/ngrok/NgrokResolver';
export declare class AsyncNgrok {
    private projectRoot;
    private port;
    /** Resolves the best instance of ngrok, exposed for testing. */
    resolver: NgrokResolver;
    /** Info about the currently running instance of ngrok. */
    private serverUrl;
    constructor(projectRoot: string, port: number);
    getActiveUrl(): string | null;
    /** Exposed for testing. */
    _getIdentifyingUrlSegmentsAsync(): Promise<string[]>;
    /** Exposed for testing. */
    _getProjectHostnameAsync(): Promise<string>;
    /** Exposed for testing. */
    _getProjectSubdomainAsync(): Promise<string>;
    /** Start ngrok on the given port for the project. */
    startAsync({ timeout }?: {
        timeout?: number;
    }): Promise<void>;
    /** Stop the ngrok process if it's running. */
    stopAsync(): Promise<void>;
    /** Exposed for testing. */
    _connectToNgrokAsync(options?: {
        timeout?: number;
    }, attempts?: number): Promise<string>;
    private _getConnectionPropsAsync;
    private connectToNgrokInternalAsync;
    private getProjectRandomnessAsync;
    _resetProjectRandomnessAsync(): Promise<string>;
}
