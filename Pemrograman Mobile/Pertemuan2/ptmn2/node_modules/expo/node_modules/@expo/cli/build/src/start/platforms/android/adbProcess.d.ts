import spawnAsync from '@expo/spawn-async';
type AdbProcessPhase = 'host-request' | 'device-service';
export declare class AdbProcessError extends Error {
    readonly operation: string;
    readonly phase: AdbProcessPhase;
    constructor(message: string, operation: string, phase: AdbProcessPhase, remoteCompletionUnknown?: true, spawnFailed?: true);
    remoteCompletionUnknown?: boolean;
    spawnFailed?: boolean;
    stdout?: string;
    stderr?: string;
    status?: number;
    signal?: NodeJS.Signals | null;
}
export declare class AdbProcessWaitError extends AdbProcessError {
}
export declare const runAdbHostQueryAsync: (command: string, args: string[], operation: string, signal?: AbortSignal) => Promise<spawnAsync.SpawnResult>;
export declare const runBoundedAdbHostQueryAsync: (command: string, args: string[], operation: string, waitLimitMs: number, signal?: AbortSignal) => Promise<spawnAsync.SpawnResult>;
export declare const runAdbDeviceQueryAsync: (command: string, args: string[], operation: string, signal?: AbortSignal) => Promise<spawnAsync.SpawnResult>;
export declare const runBoundedAdbDeviceQueryAsync: (command: string, args: string[], operation: string, waitLimitMs: number, signal?: AbortSignal) => Promise<spawnAsync.SpawnResult>;
export declare const runAdbDeviceMutationAsync: (command: string, args: string[], operation: string, signal?: AbortSignal) => Promise<spawnAsync.SpawnResult>;
export declare const runBoundedAdbDeviceMutationAsync: (command: string, args: string[], operation: string, waitLimitMs: number, signal?: AbortSignal) => Promise<spawnAsync.SpawnResult>;
export declare function isAdbTimeoutReason(reason: unknown): boolean;
export {};
