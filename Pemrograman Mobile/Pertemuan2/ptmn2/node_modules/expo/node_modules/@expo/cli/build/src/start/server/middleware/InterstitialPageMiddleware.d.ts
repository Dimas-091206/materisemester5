import { ExpoMiddleware } from './ExpoMiddleware';
import type { RuntimePlatform } from './resolvePlatform';
import type { ServerRequest, ServerResponse } from './server.types';
type ProjectVersion = {
    type: 'sdk' | 'runtime';
    version: string | null;
};
export declare const LoadingEndpoint = "/_expo/loading";
export declare class InterstitialPageMiddleware extends ExpoMiddleware {
    protected options: {
        scheme: string | null;
    };
    constructor(projectRoot: string, options?: {
        scheme: string | null;
    });
    /** Get the template HTML page and inject values. */
    _getPageAsync({ appName, projectVersion, }: {
        appName: string;
        projectVersion: ProjectVersion;
    }): Promise<string>;
    /** Get settings for the page from the project config. */
    _getProjectOptionsAsync(platform: RuntimePlatform): Promise<{
        appName: string;
        projectVersion: ProjectVersion;
    }>;
    handleRequestAsync(req: ServerRequest, res: ServerResponse): Promise<void>;
}
export {};
