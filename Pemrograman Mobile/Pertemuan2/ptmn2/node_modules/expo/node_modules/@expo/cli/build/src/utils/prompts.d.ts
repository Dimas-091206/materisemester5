import type { Choice, Options, PromptObject } from 'prompts';
import prompts from 'prompts';
export type Question<V extends string = string> = PromptObject<V> & {
    optionsPerPage?: number;
};
export interface ExpoChoice<T> extends Choice {
    value: T;
}
type PromptOptions = {
    nonInteractiveHelp?: string;
} & Options;
export type NamelessQuestion = Omit<Question<'value'>, 'name' | 'type'>;
type InteractionOptions = {
    pause: boolean;
    canEscape?: boolean;
};
type InteractionCallback = (options: InteractionOptions) => void;
export default function prompt(questions: Question | Question[], { nonInteractiveHelp, ...options }?: PromptOptions): Promise<prompts.Answers<string>>;
/**
 * Create a standard yes/no confirmation that can be cancelled.
 *
 * @param questions
 * @param options
 */
export declare function confirmAsync(questions: NamelessQuestion, options?: PromptOptions): Promise<boolean>;
/** Select an option from a list of options. */
export declare function selectAsync<T>(message: string, choices: ExpoChoice<T>[], options?: PromptOptions): Promise<T>;
export declare const promptAsync: typeof prompt;
/** Used to pause/resume interaction observers while prompting (made for TerminalUI). */
export declare function addInteractionListener(callback: InteractionCallback): void;
export declare function removeInteractionListener(callback: InteractionCallback): void;
/** Notify all listeners that keypress observations must pause. */
export declare function pauseInteractions(options?: Omit<InteractionOptions, 'pause'>): void;
/** Notify all listeners that keypress observations can start. */
export declare function resumeInteractions(options?: Omit<InteractionOptions, 'pause'>): void;
export declare function createSelectionFilter(): (input: any, choices: Choice[]) => Promise<any>;
export {};
