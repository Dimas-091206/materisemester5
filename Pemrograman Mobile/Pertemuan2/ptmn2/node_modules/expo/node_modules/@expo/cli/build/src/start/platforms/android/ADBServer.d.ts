export declare class ADBServer {
    /** Returns the command line reference to ADB. */
    getAdbExecutablePath(): string;
    /** Execute an ADB command with given args. */
    runDeviceQueryAsync(args: string[], operation: string, signal?: AbortSignal, waitLimitMs?: number): Promise<string>;
    runDeviceMutationAsync(args: string[], operation: string, signal?: AbortSignal, waitLimitMs?: number): Promise<string>;
    runHostQueryAsync(args: string[], operation: string, signal?: AbortSignal, waitLimitMs?: number): Promise<string>;
    /** Get ADB file output. Useful for reading device state/settings. */
    getFileOutputAsync(args: string[], { signal, waitLimitMs }?: {
        signal?: AbortSignal;
        waitLimitMs?: number;
    }): Promise<string>;
    /** Formats error info. */
    resolveAdbPromise<T>(promise: T | Promise<T>): Promise<T>;
}
