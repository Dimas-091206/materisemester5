import type { Options } from '../resolveOptions';
import type { DevServerManager } from './DevServerManager';
/** Launch the app on various platforms in parallel. */
export declare function openPlatformsAsync(devServerManager: DevServerManager, options: Pick<Options, 'ios' | 'android' | 'web'>): Promise<boolean>;
