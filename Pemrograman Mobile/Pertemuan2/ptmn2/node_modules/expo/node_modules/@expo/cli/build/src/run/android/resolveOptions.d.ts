import type { BuildCacheProvider } from '@expo/config';
import type { AndroidDeviceManager } from '../../start/platforms/android/AndroidDeviceManager';
import type { BundlerProps } from '../resolveBundlerProps';
import type { GradleProps } from './resolveGradlePropsAsync';
import type { LaunchProps } from './resolveLaunchProps';
export type Options = {
    variant?: string;
    device?: boolean | string;
    port?: number;
    bundler?: boolean;
    install?: boolean;
    buildCache?: boolean;
    allArch?: boolean;
    binary?: string;
    appId?: string;
};
export type ResolvedOptions = GradleProps & BundlerProps & LaunchProps & {
    variant: string;
    buildCache: boolean;
    device: AndroidDeviceManager;
    install: boolean;
    architectures?: string;
    appId?: string;
    buildCacheProvider?: BuildCacheProvider;
};
export declare function resolveOptionsAsync(projectRoot: string, options: Options): Promise<ResolvedOptions>;
