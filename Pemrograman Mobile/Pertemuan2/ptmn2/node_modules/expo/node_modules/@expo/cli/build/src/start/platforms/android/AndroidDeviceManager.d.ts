import { DeviceManager } from '../DeviceManager';
import type { BaseResolveDeviceProps } from '../PlatformManager';
import * as AndroidDebugBridge from './adb';
export declare class AndroidDeviceManager extends DeviceManager<AndroidDebugBridge.Device> {
    static resolveFromNameAsync(query: string): Promise<AndroidDeviceManager>;
    static resolveAsync({ device, shouldPrompt, }?: BaseResolveDeviceProps<AndroidDebugBridge.Device>): Promise<AndroidDeviceManager>;
    get name(): string;
    get identifier(): string;
    getAppVersionAsync(applicationId: string): Promise<string | null>;
    protected attemptToStartAsync(): Promise<AndroidDebugBridge.Device | null>;
    private assertDeviceStateIsUsable;
    private createDeviceStateError;
    private mapDeviceOperationError;
    private runDeviceOperationAsync;
    startAsync(): Promise<AndroidDebugBridge.Device>;
    installAppAsync(binaryPath: string): Promise<void>;
    uninstallAppAsync(appId: string): Promise<void>;
    /**
     * @param launchActivity Activity to launch `[application identifier]/.[main activity name]`, ex: `com.bacon.app/.MainActivity`
     */
    launchActivityAsync(launchActivity: string, url?: string): Promise<string>;
    isAppInstalledAndIfSoReturnContainerPathForIOSAsync(applicationId: string): Promise<boolean>;
    openUrlAsync(url: string): Promise<void>;
    activateWindowAsync(): Promise<void>;
    getExpoGoAppId(): string;
    ensureExpoGoAsync(sdkVersion: string): Promise<boolean>;
}
