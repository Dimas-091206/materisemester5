import type { ExpoConfig } from '@expo/config';
/**
 * Create a static HTML for SPA styled websites.
 * This method attempts to reuse the same patterns as `@expo/webpack-config`.
 */
export declare function createTemplateHtmlFromExpoConfigAsync(projectRoot: string, { scripts, cssLinks, extraHead, exp, }: {
    scripts: string[];
    cssLinks?: string[];
    /** Pre-rendered HTML to inject before `</head>`. Used for caller-owned head assets like favicons. */
    extraHead?: string;
    exp?: ExpoConfig;
}): Promise<string>;
/** Return an `index.html` string with template values added. */
export declare function createTemplateHtmlAsync(projectRoot: string, { scripts, cssLinks, extraHead, description, langIsoCode, title, themeColor, }: {
    scripts: string[];
    cssLinks?: string[];
    /** Pre-rendered HTML to inject before `</head>`. */
    extraHead?: string;
    description?: string;
    langIsoCode: string;
    title: string;
    themeColor?: string;
}): Promise<string>;
