import type { ExpoConfig } from '@expo/config';
import type { Asset, BundleAssetWithFileHashes, BundleOutput, ExportAssetMap } from './saveAssets';
export declare function assetPatternsToBeBundled(exp: ExpoConfig & {
    extra?: {
        updates?: {
            assetPatternsToBeBundled?: string[];
        };
    };
}): string[] | undefined;
/**
 * Resolves the assetBundlePatterns from the manifest and returns the set of assets to bundle.
 *
 * @modifies {exp}
 */
export declare function resolveAssetPatternsToBeBundled<T extends ExpoConfig>(projectRoot: string, exp: T, assets: Asset[]): Set<string> | undefined;
export declare function exportAssetsAsync(projectRoot: string, { exp, outputDir, bundles: { web, ...bundles }, baseUrl, files, hostedNative, }: {
    exp: ExpoConfig;
    bundles: Partial<Record<string, BundleOutput>>;
    outputDir: string;
    baseUrl: string;
    files?: ExportAssetMap;
    hostedNative?: boolean;
}): Promise<{
    exp: ExpoConfig;
    assets: BundleAssetWithFileHashes[];
    embeddedHashSet: Set<string>;
    files: ExportAssetMap;
}>;
