/**
 * Copyright © 2026 650 Industries.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */ // NOTE(@hassankhan): disable when this file is its own entrypoint
// import '@expo/metro-runtime';
"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
0 && (module.exports = {
    getStreamingContent: null,
    resolveMetadata: null
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: Object.getOwnPropertyDescriptor(all, name).get
    });
}
_export(exports, {
    get getStreamingContent () {
        return getStreamingContent;
    },
    get resolveMetadata () {
        return _metadata.resolveMetadata;
    }
});
function _jsxruntime() {
    const data = require("react/jsx-runtime");
    _jsxruntime = function() {
        return data;
    };
    return data;
}
function _server() {
    const data = /*#__PURE__*/ _interop_require_wildcard(require("expo-font/build/server"));
    _server = function() {
        return data;
    };
    return data;
}
function _exporouter() {
    const data = require("expo-router");
    _exporouter = function() {
        return data;
    };
    return data;
}
function _ctx() {
    const data = require("expo-router/_ctx");
    _ctx = function() {
        return data;
    };
    return data;
}
function _head() {
    const data = /*#__PURE__*/ _interop_require_default(require("expo-router/head"));
    _head = function() {
        return data;
    };
    return data;
}
function _server1() {
    const data = require("expo-router/internal/server");
    _server1 = function() {
        return data;
    };
    return data;
}
function _static() {
    const data = require("expo-router/internal/static");
    _static = function() {
        return data;
    };
    return data;
}
function _react() {
    const data = /*#__PURE__*/ _interop_require_default(require("react"));
    _react = function() {
        return data;
    };
    return data;
}
function _server2() {
    const data = /*#__PURE__*/ _interop_require_default(require("react-dom/server"));
    _server2 = function() {
        return data;
    };
    return data;
}
const _getRootComponent = require("../static/getRootComponent");
const _debug = require("../utils/debug");
const _react1 = require("../utils/react");
const _metadata = require("./metadata");
function _interop_require_default(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
function _getRequireWildcardCache(nodeInterop) {
    if (typeof WeakMap !== "function") return null;
    var cacheBabelInterop = new WeakMap();
    var cacheNodeInterop = new WeakMap();
    return (_getRequireWildcardCache = function(nodeInterop) {
        return nodeInterop ? cacheNodeInterop : cacheBabelInterop;
    })(nodeInterop);
}
function _interop_require_wildcard(obj, nodeInterop) {
    if (!nodeInterop && obj && obj.__esModule) {
        return obj;
    }
    if (obj === null || typeof obj !== "object" && typeof obj !== "function") {
        return {
            default: obj
        };
    }
    var cache = _getRequireWildcardCache(nodeInterop);
    if (cache && cache.has(obj)) {
        return cache.get(obj);
    }
    var newObj = {
        __proto__: null
    };
    var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
    for(var key in obj){
        if (key !== "default" && Object.prototype.hasOwnProperty.call(obj, key)) {
            var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
            if (desc && (desc.get || desc.set)) {
                Object.defineProperty(newObj, key, desc);
            } else {
                newObj[key] = obj[key];
            }
        }
    }
    newObj.default = obj;
    if (cache) {
        cache.set(obj, newObj);
    }
    return newObj;
}
const debug = (0, _debug.createDebug)('expo:router:server:renderStreamingContent');
function resetReactNavigationContexts() {
    // https://github.com/expo/router/discussions/588
    // https://github.com/react-navigation/react-navigation/blob/9fe34b445fcb86e5666f61e144007d7540f014fa/packages/elements/src/getNamedContext.tsx#LL3C1-L4C1
    // React Navigation is storing providers in a global, this is fine for the first static render
    // but subsequent static renders of Stack or Tabs will cause React to throw a warning. To prevent this warning, we'll reset the globals before rendering.
    const contexts = '__react_navigation__elements_contexts';
    globalThis[contexts] = new Map();
}
/**
 * Shared setup for both `getStaticContent()` and `getStreamingContent()`. Creates the React element
 * tree, resets server contexts, and computes loader data.
 */ function prepareRenderContext(location, options) {
    const headContext = {};
    const Root = (0, _getRootComponent.getRootComponent)();
    const { // NOTE: The `element` that's returned adds two extra Views and
    // the seemingly unused `RootTagContext.Provider`.
    element, getStyleElement } = (0, _static().registerStaticRootComponent)(_exporouter().ExpoRoot, {
        location,
        context: _ctx().ctx,
        wrapper: ({ children })=>/*#__PURE__*/ (0, _jsxruntime().jsx)(Root, {
                children: /*#__PURE__*/ (0, _jsxruntime().jsx)("div", {
                    id: "root",
                    children: children
                })
            })
    });
    // This MUST be run before `ReactDOMServer.renderToString` to prevent
    // "Warning: Detected multiple renderers concurrently rendering the same context provider. This is currently unsupported."
    resetReactNavigationContexts();
    const loaderKey = options?.loader ? options.loader.key + location.search : null;
    const loadedData = loaderKey ? {
        [loaderKey]: options?.loader?.data ?? null
    } : null;
    return {
        headContext,
        element,
        getStyleElement,
        loadedData
    };
}
function FontResources() {
    // NOTE(@hassankhan): runs once during the shell pass; fonts loaded inside late-resolving
    // Suspense boundaries register after this and won't be emitted.
    const descriptors = _server().getServerResourceDescriptors();
    debug(`Pushing fonts: (count: ${descriptors.length})`, descriptors);
    return (0, _react1.createInjectedFontsAsNodes)(descriptors);
}
async function getStreamingContent(location, options) {
    return _server().withServerContext(()=>{
        const { headContext, element, getStyleElement, loadedData } = prepareRenderContext(location, options);
        const { headNodes: headCssNodes } = (0, _react1.createInjectedCssAsNodes)(options?.assets?.css ?? []);
        const { headNodes: externalCssNodes } = (0, _react1.createInjectedExternalCssAsNodes)(options?.assets?.externalCss);
        const { headNodes: inlineCssNodes } = (0, _react1.createInjectedInlineCssAsNodes)(options?.assets?.inlineCss);
        const faviconNode = options?.assets?.favicon ? (0, _react1.createFaviconAsNode)(options?.assets?.favicon) : undefined;
        const serverDocumentData = {
            headNodes: [
                ...options?.metadata?.headNodes ?? [],
                faviconNode,
                getStyleElement({
                    key: 'rnw-style-element'
                }),
                ...headCssNodes ?? [],
                ...externalCssNodes ?? [],
                ...inlineCssNodes ?? []
            ].filter(Boolean),
            bodyNodes: [
                /*#__PURE__*/ (0, _jsxruntime().jsx)(FontResources, {}, "font-resources")
            ]
        };
        return _server2().default.renderToReadableStream(/*#__PURE__*/ (0, _jsxruntime().jsx)(_server1().ServerDocument, {
            data: serverDocumentData,
            children: /*#__PURE__*/ (0, _jsxruntime().jsx)(_head().default.Provider, {
                context: headContext,
                children: /*#__PURE__*/ (0, _jsxruntime().jsx)(_static().InnerRoot, {
                    loadedData: loadedData,
                    children: element
                })
            })
        }), {
            // TODO(@hassankhan): Experiment and see if we can calculate a better default
            // We're doubling the default here so non-JavaScript renders show some content
            progressiveChunkSize: 12800 * 2,
            bootstrapScriptContent: (0, _react1.getBootstrapContents)({
                hydrate: true,
                loadedData
            }),
            bootstrapScripts: options?.assets?.js,
            signal: options?.request?.signal,
            onError (error) {
                if (options?.request?.signal.aborted) {
                    return;
                }
                console.error('SSR streaming render error:', error);
            }
        });
    });
}

//# sourceMappingURL=renderStreamingContent.js.map
