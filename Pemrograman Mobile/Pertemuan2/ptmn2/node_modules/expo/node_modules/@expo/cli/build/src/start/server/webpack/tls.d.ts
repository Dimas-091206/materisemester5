/** Ensure TLS is setup and environment variables are set. */
export declare function ensureEnvironmentSupportsTLSAsync(projectRoot: string): Promise<void>;
/** Create TLS and write to files in the temporary directory. Exposed for testing. */
export declare function getTLSCertAsync(projectRoot: string): Promise<{
    keyPath: string;
    certPath: string;
} | false>;
