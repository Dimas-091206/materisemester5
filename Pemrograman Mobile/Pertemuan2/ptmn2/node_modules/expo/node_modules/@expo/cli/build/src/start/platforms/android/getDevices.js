"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
0 && (module.exports = {
    getDevicesAsync: null,
    mergeDevices: null
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: Object.getOwnPropertyDescriptor(all, name).get
    });
}
_export(exports, {
    get getDevicesAsync () {
        return getDevicesAsync;
    },
    get mergeDevices () {
        return mergeDevices;
    }
});
const _errors = require("../../../utils/errors");
const _adb = require("./adb");
const _emulator = require("./emulator");
async function getDevicesAsync() {
    const bootedDevices = await (0, _adb.waitForAttachedDevicesAsync)();
    // NOTE(@kitten): We don't assume AVD must succeed or be present, and still allow
    // devices to be discovered and move on
    let data;
    try {
        data = await (0, _emulator.listAvdsAsync)();
    } catch (error) {
        if (!bootedDevices.length) {
            throw new _errors.CommandError('ANDROID_AVD_DISCOVERY', formatNoDevicesMessage(error instanceof Error ? error.message : String(error)));
        }
        data = [];
    }
    const allDevices = mergeDevices(bootedDevices, data);
    if (!allDevices.length) {
        throw new _errors.CommandError('ANDROID_NO_DEVICES', formatNoDevicesMessage());
    }
    return allDevices;
}
function formatNoDevicesMessage(cause) {
    return [
        `No Android connected device found, and no emulators could be started automatically.`,
        cause ? `Could not list Android emulators: ${cause}` : null,
        `Connect a device or create an emulator (https://docs.expo.dev/workflow/android-studio-emulator).`,
        `Then follow the instructions here to enable USB debugging:`,
        `https://developer.android.com/studio/run/device.html#developer-device-options. If you are using Genymotion go to Settings -> ADB, select "Use custom Android SDK tools", and point it at your Android SDK directory.`
    ].filter((line)=>line != null).join('\n');
}
function mergeDevices(attachedDevices, avds) {
    const connectedNames = new Set(attachedDevices.map(({ name })=>name));
    return attachedDevices.concat(avds.filter(({ name })=>!connectedNames.has(name)));
}

//# sourceMappingURL=getDevices.js.map
