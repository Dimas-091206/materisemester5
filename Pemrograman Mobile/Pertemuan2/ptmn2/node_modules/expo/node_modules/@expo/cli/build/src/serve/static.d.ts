import http from 'node:http';
import type { StaticManifest } from '../export/static';
export declare function loadStaticManifestAsync(dist: string): Promise<StaticManifest<RegExp> | null>;
/** Mirrors `resolveRouteHeaders()` in `expo-server/src/vendor/abstract.ts`. */
export declare function resolveStaticHeaders(manifest: StaticManifest<RegExp>, pathname: string): Record<string, string | string[]>;
/**
 * Applies headers from a static export manifest to a response, except
 * `Content-Type` which must come from the served file.
 */
export declare function applyStaticHeaders(res: http.ServerResponse, headers: Record<string, string | string[]>): void;
