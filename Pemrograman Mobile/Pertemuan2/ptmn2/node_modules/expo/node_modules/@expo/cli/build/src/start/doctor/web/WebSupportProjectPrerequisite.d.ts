import type { AppJSONConfig, ExpoConfig, PackageJSONConfig, ProjectConfig } from '@expo/config';
import { ProjectPrerequisite } from '../Prerequisite';
/** Ensure the project has the required web support settings. */
export declare class WebSupportProjectPrerequisite extends ProjectPrerequisite {
    /** Ensure a project that hasn't explicitly disabled web support has all the required packages for running in the browser. */
    assertImplementation(): Promise<void>;
    /** Exposed for testing. */
    _shouldSetupWebSupportAsync(): Promise<ProjectConfig>;
    /** Exposed for testing. */
    _ensureWebDependenciesInstalledAsync({ exp, pkg, }: {
        exp: ExpoConfig;
        pkg: PackageJSONConfig;
    }): Promise<boolean>;
}
/** Return `true` if the `web` platform is purposefully excluded from the project Expo config. */
export declare function isWebPlatformExcluded(rootConfig: AppJSONConfig): boolean;
