export declare const ProjectSettings: {
    getFile: (projectRoot: string) => import("@expo/json-file").default<{
        urlRandomness: string | null;
    }>;
    readAsync: (projectRoot: string) => Promise<{
        urlRandomness: string | null;
    }>;
    setAsync: (projectRoot: string, json: Partial<{
        urlRandomness: string | null;
    }>) => Promise<{
        urlRandomness: string | null;
    }>;
};
