import { spawn } from 'child_process';
import type { DevToolsPluginExecutorArguments, DevToolsPluginInfo, DevToolsPluginOutput } from './DevToolsPlugin.schema';
/**
 * Class that executes CLI Extension commands for a given plugin
 *
 * ## Responsibilities:
 * - Verifies that requested commands exist and have correct parameters
 * - Validates that provided arguments match expected parameter schema
 * - Spawns and manages child processes for command execution
 *  - Captures and streams stdout/stderr data from executed commands
 *  - Manages process errors, timeouts, and unexpected failures
 *  - Enforces execution time limits to prevent hanging processes
 *  - Configures proper execution environment with color support
 *  - Ensures proper cleanup of processes and timeouts
 *  - Provides structured output with exit codes and error states
 */
export declare class DevToolsPluginCliExtensionExecutor {
    private plugin;
    private projectRoot;
    private spawnFunc;
    private timeoutMs;
    private readonly resolvedEntryPoint;
    constructor(plugin: DevToolsPluginInfo, projectRoot: string, spawnFunc?: typeof spawn, // Used for injection when testing,
    timeoutMs?: number);
    validate({ command, args }: Omit<DevToolsPluginExecutorArguments, 'metroServerOrigin'>): void;
    /** this function is used for testing and showing the command in UI */
    getCommandString: ({ command, args, }: Omit<DevToolsPluginExecutorArguments, "metroServerOrigin">) => string;
    execute: ({ command, args, metroServerOrigin, onOutput, }: DevToolsPluginExecutorArguments) => Promise<DevToolsPluginOutput>;
}
