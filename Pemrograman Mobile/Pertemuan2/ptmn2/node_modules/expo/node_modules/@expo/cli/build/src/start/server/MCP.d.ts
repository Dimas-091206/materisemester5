import type { McpServerProxy } from '@expo/mcp-tunnel' with { 'resolution-mode': 'import' };
/**
 * The MCP server
 */
export type McpServer = Omit<McpServerProxy, 'close'> & {
    /**
     * Close the server
     */
    closeAsync: () => Promise<void>;
};
/**
 * Create the MCP server
 */
export declare function maybeCreateMCPServerAsync({ projectRoot, devServerUrl, }: {
    projectRoot: string;
    devServerUrl: string;
}): Promise<McpServer | null>;
