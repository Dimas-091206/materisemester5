import type { ExpoConfig } from '@expo/config';
/** Create the expected session info. */
export declare function createSessionInfo({ exp, runtime, url, }: {
    exp: Pick<ExpoConfig, 'name' | 'description' | 'slug' | 'primaryColor'>;
    runtime: 'native' | 'web';
    url: string;
}): {
    session: {
        description: string;
        hostname: string;
        platform: "native" | "web";
        config: {
            description: string | undefined;
            name: string;
            slug: string;
            primaryColor: string | undefined;
        };
        url: string;
        source: string;
    };
};
/** Send a request to Expo API to keep the 'development session' alive for the provided devices. */
export declare function updateDevelopmentSessionAsync({ deviceIds, exp, runtime, url, signal, }: {
    deviceIds: string[];
    exp: Pick<ExpoConfig, 'name' | 'description' | 'slug' | 'primaryColor'>;
    runtime: 'native' | 'web';
    url: string;
    signal?: AbortSignal;
}): Promise<void>;
/** Send a request to Expo API to close the 'development session' for the provided devices. */
export declare function closeDevelopmentSessionAsync({ deviceIds, url, }: {
    deviceIds: string[];
    url: string;
}): Promise<void>;
