import type { IncomingHttpHeaders } from 'node:http';
import type { Socket } from 'node:net';
export declare const isLocalSocket: (socket: Socket) => boolean;
interface AbstractIncomingMessage {
    headers: IncomingHttpHeaders | Record<string, string | string[]>;
}
export declare const isMatchingOrigin: (request: AbstractIncomingMessage, serverBaseUrl: string) => boolean;
/** Process-wide throttle. Returns `true` if another call fired within the cooldown window. */
export declare const shouldThrottleRemoteDevCall: () => boolean;
export {};
