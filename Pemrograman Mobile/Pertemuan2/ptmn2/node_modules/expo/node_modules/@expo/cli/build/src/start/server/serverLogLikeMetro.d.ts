import * as stackTraceParser from 'stacktrace-parser';
import type { StackFrame } from './metro/log-box/LogBoxSymbolication';
declare const CONSOLE_METHODS: readonly ["trace", "info", "error", "warn", "log", "group", "groupCollapsed", "groupEnd", "debug"];
type ConsoleMethod = (typeof CONSOLE_METHODS)[number];
export declare function logLikeMetro(originalLogFunction: (...args: any[]) => void, level: ConsoleMethod, platform: string | null, ...data: any[]): void;
export declare function maybeSymbolicateAndFormatJSErrorStackLogAsync(projectRoot: string, level: 'error' | 'warn' | (string & {}), error: {
    message: string;
    stack: StackFrame[];
}): Promise<{
    isFallback: boolean;
    stack: string;
}>;
/**
 * Attempt to parse an error message string to an unsymbolicated stack.
 */
export declare function parseErrorStringToObject(errorString: string): {
    message: string;
    stack: (stackTraceParser.StackFrame & {
        collapse?: boolean;
    })[];
} | null;
declare function augmentLogsInternal(projectRoot: string): void;
export declare function formatStackLikeMetro(projectRoot: string, stack: string): string;
/** Augment console logs to check the stack trace and format like Metro logs if we think the log came from the SSR renderer or an API route. */
export declare const augmentLogs: typeof augmentLogsInternal;
export {};
