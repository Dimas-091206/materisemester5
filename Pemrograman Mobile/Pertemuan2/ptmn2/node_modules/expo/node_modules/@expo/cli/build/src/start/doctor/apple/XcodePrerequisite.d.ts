import { Prerequisite } from '../Prerequisite';
export declare const getXcodeVersionAsync: ({ silent, force, }?: {
    silent?: boolean;
    force?: boolean;
}) => Promise<string | null | false>;
export declare class XcodePrerequisite extends Prerequisite {
    static instance: XcodePrerequisite;
    /**
     * Ensure Xcode is installed and recent enough to be used with Expo.
     */
    assertImplementation(): Promise<void>;
}
