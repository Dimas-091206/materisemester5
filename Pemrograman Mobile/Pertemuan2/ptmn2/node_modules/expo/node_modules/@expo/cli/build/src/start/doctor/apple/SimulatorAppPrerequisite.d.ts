import { Prerequisite } from '../Prerequisite';
export declare class SimulatorAppPrerequisite extends Prerequisite {
    static instance: SimulatorAppPrerequisite;
    assertImplementation(): Promise<void>;
}
