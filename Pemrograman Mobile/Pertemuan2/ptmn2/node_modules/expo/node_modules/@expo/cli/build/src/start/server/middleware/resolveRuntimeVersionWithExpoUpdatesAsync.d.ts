import type { RuntimePlatform } from './resolvePlatform';
export declare function resolveRuntimeVersionWithExpoUpdatesAsync({ projectRoot, platform, }: {
    projectRoot: string;
    platform: RuntimePlatform;
}): Promise<string | null>;
