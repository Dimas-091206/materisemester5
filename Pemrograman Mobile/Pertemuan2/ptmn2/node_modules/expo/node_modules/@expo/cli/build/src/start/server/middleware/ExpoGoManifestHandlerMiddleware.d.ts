import type { ManifestRequestInfo } from './ManifestMiddleware';
import { ManifestMiddleware } from './ManifestMiddleware';
import type { ServerRequest } from './server.types';
declare module '2g' {
    interface EventRegistry {
        'manifest:served': {
            type: 'expo-go' | 'dev-client';
            runtimeVersion: string;
            sdkVersion: string | null;
        };
    }
}
export declare enum ResponseContentType {
    TEXT_PLAIN = 0,
    APPLICATION_JSON = 1,
    APPLICATION_EXPO_JSON = 2,
    MULTIPART_MIXED = 3
}
interface ExpoGoManifestRequestInfo extends ManifestRequestInfo {
    responseContentType: ResponseContentType;
    expectSignature: string | null;
}
export declare class ExpoGoManifestHandlerMiddleware extends ManifestMiddleware<ExpoGoManifestRequestInfo> {
    getParsedHeaders(req: ServerRequest): ExpoGoManifestRequestInfo;
    private getDefaultResponseHeaders;
    _getManifestResponseAsync(requestOptions: ExpoGoManifestRequestInfo): Promise<Response>;
    private static getContentTypeForResponseContentType;
    private encodeFormDataAsync;
    private static getScopeKeyAsync;
}
export {};
