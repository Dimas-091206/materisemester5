import type { DevServerManager } from '../server/DevServerManager';
import type { DevToolsPlugin } from '../server/DevToolsPlugin';
import type { StartOptions } from './commandsTable';
export declare function getDevToolsPluginCliBannerItems(plugins: DevToolsPlugin[], defaultServerUrl: string): {
    title: string;
    url: string;
}[];
export declare function printDevToolsPluginCliBannersAsync(devServerManager: DevServerManager): Promise<number>;
/** Wraps the DevServerManager and adds an interface for user actions. */
export declare class DevServerManagerActions {
    private devServerManager;
    private options;
    constructor(devServerManager: DevServerManager, options: Pick<StartOptions, 'devClient' | 'platforms'>);
    printDevServerInfoAsync(options: Pick<StartOptions, 'devClient' | 'isWebSocketsEnabled' | 'platforms' | 'dependencyCheckRef'>): Promise<void>;
    openJsInspectorAsync(): Promise<void>;
    reloadApp(): void;
    openMoreToolsAsync(): Promise<void>;
    toggleDevMenu(): void;
}
