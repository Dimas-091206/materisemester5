import type { Socket } from 'net';
import type { AFCResponse } from '../protocol/AFCProtocol';
import { AFCProtocolClient } from '../protocol/AFCProtocol';
import { ServiceClient } from './ServiceClient';
export declare class AFCClient extends ServiceClient<AFCProtocolClient> {
    socket: Socket;
    constructor(socket: Socket);
    getFileInfo(path: string): Promise<string[]>;
    writeFile(fd: Buffer, data: Buffer): Promise<AFCResponse>;
    protected openFile(path: string): Promise<Buffer>;
    protected closeFile(fd: Buffer): Promise<AFCResponse>;
    protected uploadFile(srcPath: string, destPath: string): Promise<void>;
    makeDirectory(path: string): Promise<AFCResponse>;
    uploadDirectory(srcPath: string, destPath: string): Promise<void>;
}
