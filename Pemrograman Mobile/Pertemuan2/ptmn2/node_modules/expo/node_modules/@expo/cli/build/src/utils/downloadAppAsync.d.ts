import type { ProgressCallback } from '../api/rest/client.types';
export declare function downloadAppAsync({ url, outputPath, extract, cacheDirectory, onProgress, }: {
    url: string;
    outputPath: string;
    extract?: boolean;
    cacheDirectory?: string;
    onProgress?: ProgressCallback;
}): Promise<void>;
