import type { ExpoConfig } from '@expo/config';
import type { MetroBundlerDevServer } from '../start/server/metro/MetroBundlerDevServer';
import type { PlatformMetadata } from './createMetadataJson';
import { type BundleOutput, type ExportAssetMap } from './saveAssets';
export declare function exportDomComponentAsync({ filePath, projectRoot, dev, devServer, isHermes, includeSourceMaps, exp, files, useMd5Filename, }: {
    filePath: string;
    projectRoot: string;
    dev: boolean;
    devServer: MetroBundlerDevServer;
    isHermes: boolean;
    includeSourceMaps: boolean;
    exp: ExpoConfig;
    files: ExportAssetMap;
    useMd5Filename?: boolean;
}): Promise<{
    bundle: BundleOutput;
    htmlOutputName: string;
}>;
/**
 * Add the DOM component bundle to the metadata for updates.
 */
export declare function addDomBundleToMetadataAsync(bundle: BundleOutput): PlatformMetadata['assets'];
/**
 * Transform the DOM component entry (*.html) to use MD5 filename by its content.
 */
export declare function transformDomEntryForMd5Filename({ files, htmlOutputName, }: {
    files: ExportAssetMap;
    htmlOutputName: string;
}): PlatformMetadata['assets'];
/**
 * Post-transform the native bundle to use MD5 filename based on DOM component entry content.
 */
export declare function transformNativeBundleForMd5Filename({ domComponentReference, nativeBundle, files, htmlOutputName, }: {
    domComponentReference: string;
    nativeBundle: BundleOutput;
    files: ExportAssetMap;
    htmlOutputName: string;
}): void;
