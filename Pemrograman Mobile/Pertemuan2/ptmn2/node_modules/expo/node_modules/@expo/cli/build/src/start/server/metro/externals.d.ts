declare global {
    namespace NodeJS {
        interface Process {
            binding?(key: 'natives'): Record<string, unknown>;
        }
    }
}
export declare const NODE_STDLIB_MODULES: string[];
export declare function shouldCreateVirtualShim(normalName: string): string | null;
export declare function isNodeExternal(moduleName: string): string | null;
