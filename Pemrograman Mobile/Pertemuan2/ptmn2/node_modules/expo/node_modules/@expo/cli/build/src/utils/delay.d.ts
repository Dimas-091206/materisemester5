/** Await for a given duration of milliseconds. */
export declare function delayAsync(timeout: number): Promise<void>;
/** Wait for a given action to return a truthy value. */
export declare function waitForActionAsync<T>({ action, interval, maxWaitTime, }: {
    action: () => T | Promise<T>;
    interval?: number;
    maxWaitTime?: number;
}): Promise<T>;
/** Resolves a given function or rejects if the provided timeout is passed. */
export declare function resolveWithTimeout<T>(action: () => Promise<T>, { timeout, errorMessage, }: {
    /** Duration in milliseconds to wait before asserting a timeout. */
    timeout: number;
    /** Optional error message to use in the assertion. */
    errorMessage?: string;
}): Promise<T>;
