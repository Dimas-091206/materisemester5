"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
0 && (module.exports = {
    DeviceABI: null,
    adbArgs: null,
    adbShellArgs: null,
    getAdbNameForDeviceIdAsync: null,
    getAttachedDevicesAsync: null,
    getDeviceABIsAsync: null,
    getPackageInfoAsync: null,
    getPropertyDataForDeviceAsync: null,
    getServer: null,
    installAsync: null,
    isBootAnimationCompleteAsync: null,
    isDeviceBootedAsync: null,
    isPackageInstalledAsync: null,
    launchActivityAsync: null,
    logUnauthorized: null,
    openUrlAsync: null,
    sanitizeAdbDeviceName: null,
    uninstallAsync: null,
    waitForAttachedDevicesAsync: null
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: Object.getOwnPropertyDescriptor(all, name).get
    });
}
_export(exports, {
    get DeviceABI () {
        return DeviceABI;
    },
    get adbArgs () {
        return adbArgs;
    },
    get adbShellArgs () {
        return adbShellArgs;
    },
    get getAdbNameForDeviceIdAsync () {
        return getAdbNameForDeviceIdAsync;
    },
    get getAttachedDevicesAsync () {
        return getAttachedDevicesAsync;
    },
    get getDeviceABIsAsync () {
        return getDeviceABIsAsync;
    },
    get getPackageInfoAsync () {
        return getPackageInfoAsync;
    },
    get getPropertyDataForDeviceAsync () {
        return getPropertyDataForDeviceAsync;
    },
    get getServer () {
        return getServer;
    },
    get installAsync () {
        return installAsync;
    },
    get isBootAnimationCompleteAsync () {
        return isBootAnimationCompleteAsync;
    },
    get isDeviceBootedAsync () {
        return isDeviceBootedAsync;
    },
    get isPackageInstalledAsync () {
        return isPackageInstalledAsync;
    },
    get launchActivityAsync () {
        return launchActivityAsync;
    },
    get logUnauthorized () {
        return logUnauthorized;
    },
    get openUrlAsync () {
        return openUrlAsync;
    },
    get sanitizeAdbDeviceName () {
        return sanitizeAdbDeviceName;
    },
    get uninstallAsync () {
        return uninstallAsync;
    },
    get waitForAttachedDevicesAsync () {
        return waitForAttachedDevicesAsync;
    }
});
function _chalk() {
    const data = /*#__PURE__*/ _interop_require_default(require("chalk"));
    _chalk = function() {
        return data;
    };
    return data;
}
const _log = /*#__PURE__*/ _interop_require_wildcard(require("../../../log"));
const _env = require("../../../utils/env");
const _errors = require("../../../utils/errors");
const _interactive = require("../../../utils/interactive");
const _link = require("../../../utils/link");
const _ora = require("../../../utils/ora");
const _events = require("../events");
const _ADBServer = require("./ADBServer");
const _adbDeviceList = require("./adbDeviceList");
const _adbDiagnostics = require("./adbDiagnostics");
const _adbEndpoint = require("./adbEndpoint");
function _interop_require_default(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
function _getRequireWildcardCache(nodeInterop) {
    if (typeof WeakMap !== "function") return null;
    var cacheBabelInterop = new WeakMap();
    var cacheNodeInterop = new WeakMap();
    return (_getRequireWildcardCache = function(nodeInterop) {
        return nodeInterop ? cacheNodeInterop : cacheBabelInterop;
    })(nodeInterop);
}
function _interop_require_wildcard(obj, nodeInterop) {
    if (!nodeInterop && obj && obj.__esModule) {
        return obj;
    }
    if (obj === null || typeof obj !== "object" && typeof obj !== "function") {
        return {
            default: obj
        };
    }
    var cache = _getRequireWildcardCache(nodeInterop);
    if (cache && cache.has(obj)) {
        return cache.get(obj);
    }
    var newObj = {
        __proto__: null
    };
    var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
    for(var key in obj){
        if (key !== "default" && Object.prototype.hasOwnProperty.call(obj, key)) {
            var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
            if (desc && (desc.get || desc.set)) {
                Object.defineProperty(newObj, key, desc);
            } else {
                newObj[key] = obj[key];
            }
        }
    }
    newObj.default = obj;
    if (cache) {
        cache.set(obj, newObj);
    }
    return newObj;
}
var DeviceABI = /*#__PURE__*/ function(DeviceABI) {
    // The arch specific android target platforms are soft-deprecated.
    // Instead of using TargetPlatform as a combination arch + platform
    // the code will be updated to carry arch information in [DarwinArch]
    // and [AndroidArch].
    DeviceABI["arm"] = "arm";
    DeviceABI["arm64"] = "arm64";
    DeviceABI["x64"] = "x64";
    DeviceABI["x86"] = "x86";
    DeviceABI["x8664"] = "x86_64";
    DeviceABI["arm64v8a"] = "arm64-v8a";
    DeviceABI["armeabiV7a"] = "armeabi-v7a";
    DeviceABI["armeabi"] = "armeabi";
    DeviceABI["universal"] = "universal";
    return DeviceABI;
}({});
const CANT_START_ACTIVITY_ERROR = 'Activity not started, unable to resolve Intent';
// http://developer.android.com/ndk/guides/abis.html
const PROP_CPU_NAME = 'ro.product.cpu.abi';
const PROP_CPU_ABI_LIST_NAME = 'ro.product.cpu.abilist';
// Can sometimes be null
// http://developer.android.com/ndk/guides/abis.html
const PROP_BOOT_ANIMATION_STATE = 'init.svc.bootanim';
const DEVICE_DISCOVERY_WAIT_LIMIT_MS = 10_000;
const DEVICE_DISCOVERY_RETRY_DELAY_MS = 200;
const DEVICE_DISCOVERY_RETRY_WINDOW_MS = 2_000;
let _server;
function getServer() {
    _server ??= new _ADBServer.ADBServer();
    return _server;
}
function logUnauthorized(device) {
    _log.warn(`\nThis computer is not authorized for developing on ${_chalk().default.bold(device.name)}. ${_chalk().default.dim((0, _link.learnMore)('https://expo.fyi/authorize-android-device'))}`);
}
async function isPackageInstalledAsync(device, androidPackage, signal) {
    const packages = await getServer().runDeviceQueryAsync(adbShellArgs(device.pid, 'pm', 'list', 'packages', '--user', _env.env.EXPO_ADB_USER, androidPackage), 'package query', signal);
    const lines = packages.split(/\r?\n/);
    for(let i = 0; i < lines.length; i++){
        const line = lines[i].trim();
        if (line === `package:${androidPackage}`) {
            return true;
        }
    }
    return false;
}
async function launchActivityAsync(device, { launchActivity, url }, signal) {
    const command = [
        'am',
        'start',
        // FLAG_ACTIVITY_SINGLE_TOP -- If set, the activity will not be launched if it is already running at the top of the history stack.
        '-f',
        '0x20000000',
        // Activity to open first: com.bacon.app/.MainActivity
        '-n',
        launchActivity
    ];
    if (url) {
        command.push('-d', url);
    }
    return openAsync(adbShellArgs(device.pid, ...command), 'activity launch', signal);
}
async function openUrlAsync(device, { url }, signal) {
    return openAsync(adbShellArgs(device.pid, 'am', 'start', '-a', 'android.intent.action.VIEW', '-d', url), 'URL launch', signal);
}
/** Runs a generic command watches for common errors in order to throw with an expected code. */ async function openAsync(args, operation, signal) {
    let results;
    try {
        results = await getServer().runDeviceMutationAsync(args, operation, signal);
    } catch (error) {
        const output = error && typeof error === 'object' ? [
            'stdout' in error ? error.stdout : undefined,
            'stderr' in error ? error.stderr : undefined,
            error instanceof Error ? error.message : undefined
        ].filter((value)=>typeof value === 'string').join('\n') : String(error);
        if (isMissingActivityOutput(output)) {
            throw new _errors.CommandError('APP_NOT_INSTALLED', extractActivityError(output));
        }
        throw error;
    }
    if (isMissingActivityOutput(results)) {
        throw new _errors.CommandError('APP_NOT_INSTALLED', extractActivityError(results));
    }
    return results;
}
function isMissingActivityOutput(output) {
    return output.includes(CANT_START_ACTIVITY_ERROR) || /Error: Activity class .* does not exist\./.test(output);
}
function extractActivityError(output) {
    const errorIndex = output.indexOf('Error: ');
    return errorIndex >= 0 ? output.substring(errorIndex) : output;
}
async function uninstallAsync(device, { appId }, signal) {
    return await getServer().runDeviceMutationAsync(adbArgs(device.pid, 'uninstall', '--user', _env.env.EXPO_ADB_USER, appId), 'app uninstall', signal);
}
async function getPackageInfoAsync(device, { appId }, signal) {
    return await getServer().runDeviceQueryAsync(adbShellArgs(device.pid, 'dumpsys', 'package', appId), 'package info query', signal);
}
async function installAsync(device, { filePath }, signal) {
    // TODO: Handle the `INSTALL_FAILED_INSUFFICIENT_STORAGE` error.
    return await getServer().runDeviceMutationAsync(adbArgs(device.pid, 'install', '-r', '-d', '--user', _env.env.EXPO_ADB_USER, filePath), 'app install', signal);
}
function adbArgs(pid, ...options) {
    const args = [];
    if (pid) {
        args.push('-s', pid);
    }
    return args.concat(options);
}
function adbShellArgs(pid, ...command) {
    return adbArgs(pid, 'shell', ...command.map(shellQuote));
}
function shellQuote(value) {
    return `'${value.replace(/'/g, `'\\''`)}'`;
}
async function getAttachedDevicesAsync(options = {}) {
    return getAttachedDevicesWithOptionsAsync(options, false);
}
async function waitForAttachedDevicesAsync() {
    return getAttachedDevicesWithOptionsAsync({
        shouldShowWaitingMessage: true
    }, true);
}
async function getAttachedDevicesWithOptionsAsync({ server = getServer(), signal, waitLimitMs, probeWaitLimitMs = _adbEndpoint.ADB_HOST_PROBE_WAIT_LIMIT_MS, shouldShowWaitingMessage = false }, shouldRetryEmptyResult) {
    const discoveryWaitLimitMs = waitLimitMs ?? DEVICE_DISCOVERY_WAIT_LIMIT_MS;
    const discoveryDeadline = Date.now() + discoveryWaitLimitMs;
    const retryDeadline = Math.min(discoveryDeadline, Date.now() + DEVICE_DISCOVERY_RETRY_WINDOW_MS);
    const waitSignal = AbortSignal.timeout(discoveryWaitLimitMs);
    const operationSignal = signal ? AbortSignal.any([
        signal,
        waitSignal
    ]) : waitSignal;
    const endpoint = (0, _adbEndpoint.resolveAdbEndpoint)();
    const spinner = shouldShowWaitingMessage && (0, _interactive.isInteractive)() ? (0, _ora.ora)('Waiting for ADB device discovery').start() : null;
    let retryError;
    try {
        while(true){
            try {
                const output = await server.runHostQueryAsync([
                    'devices',
                    '-l'
                ], 'device discovery', signal, Math.max(1, discoveryDeadline - Date.now()));
                const devices = await parseAttachedDevicesAsync(output, operationSignal);
                if (devices.length || !shouldRetryEmptyResult) {
                    return devices;
                }
                retryError = undefined;
            } catch (error) {
                if (operationSignal.aborted && error === operationSignal.reason) throw error;
                if (!shouldRetryAdbDiscovery(error)) {
                    throw error;
                }
                retryError = error;
            }
            const retryTimeRemaining = retryDeadline - Date.now();
            if (retryTimeRemaining <= 0) {
                if (retryError) throw retryError;
                return [];
            }
            await waitForRetryAsync(Math.min(DEVICE_DISCOVERY_RETRY_DELAY_MS, retryTimeRemaining), operationSignal);
        }
    } catch (caughtError) {
        // Caller cancellation is not a discovery failure, so skip the diagnostic probe
        if (signal?.aborted && caughtError === signal.reason) {
            throw caughtError;
        }
        const error = retryError != null && operationSignal.aborted && caughtError === operationSignal.reason ? retryError : caughtError;
        let hostProbe;
        if ((0, _adbDiagnostics.shouldProbeAdbHost)(error) && !operationSignal.aborted) {
            try {
                const probeSignal = AbortSignal.any([
                    operationSignal,
                    AbortSignal.timeout(probeWaitLimitMs)
                ]);
                hostProbe = await (0, _adbEndpoint.probeAdbHostVersionAsync)(endpoint, probeSignal);
            } catch (probeError) {
                if (signal?.aborted && probeError === signal.reason) {
                    throw probeError;
                } else if (!operationSignal.aborted) {
                    hostProbe = {
                        kind: 'connection-failure'
                    };
                }
            }
        }
        throw new _errors.CommandError('ADB_DISCOVERY', (0, _adbDiagnostics.formatAdbDiscoveryError)(error, endpoint, hostProbe));
    } finally{
        spinner?.stop();
    }
}
async function parseAttachedDevicesAsync(output, signal) {
    return Promise.all((0, _adbDeviceList.parseAdbDeviceList)(output).map(async (record)=>{
        // unauthorized: ['FA8251A00719', 'unauthorized', 'usb:338690048X', 'transport_id:5']
        // authorized: ['FA8251A00719', 'device', 'usb:336592896X', 'product:walleye', 'model:Pixel_2', 'device:walleye', 'transport_id:4']
        // emulator: ['emulator-5554', 'offline', 'transport_id:1']
        const type = /^emulator-\d+$/.test(record.serial) ? 'emulator' : 'device';
        const connectionType = type !== 'device' ? undefined : record.metadata.some((field)=>field.startsWith('usb:')) ? 'USB' : record.serial.includes('_adb-tls-connect.') ? 'Network' : undefined;
        const { serial: pid, state, transportId, metadata: deviceInfo } = record;
        const isUsable = (0, _adbDeviceList.isAdbDeviceStateUsable)(state);
        let name;
        if (type === 'device') {
            // Unauthorized devices do not report model metadata.
            const model = isUsable ? deviceInfo.find((field)=>field.startsWith('model:'))?.slice('model:'.length) : undefined;
            name = model || `Device ${pid}`;
        } else {
            // Resolve names in every emulator state so booting transports merge with AVD inventory.
            // A broken emulator console must not discard otherwise healthy attached devices.
            try {
                name = await getAdbNameForDeviceIdAsync({
                    pid
                }, signal) || `Device ${pid}`;
            } catch (error) {
                if (signal.aborted && error === signal.reason) throw error;
                name = `Device ${pid}`;
            }
        }
        return {
            pid,
            name,
            type,
            isAuthorized: isUsable,
            isBooted: isUsable,
            isLaunchable: false,
            state,
            transportId,
            connectionType
        };
    }));
}
function shouldRetryAdbDiscovery(error) {
    return !(error instanceof Error && error.name === 'AbortError') && error instanceof Error && /(?:cannot connect|connection refused|server didn't ACK|daemon (?:not running|still not running)|smartsocket)/i.test(error.message);
}
function waitForRetryAsync(delayMs, signal) {
    if (signal.aborted) return Promise.reject(signal.reason);
    return new Promise((resolve, reject)=>{
        const timeout = setTimeout(()=>{
            signal.removeEventListener('abort', handleAbort);
            resolve();
        }, delayMs);
        const handleAbort = ()=>{
            clearTimeout(timeout);
            reject(signal.reason);
        };
        signal.addEventListener('abort', handleAbort, {
            once: true
        });
    });
}
async function getAdbNameForDeviceIdAsync(device, signal) {
    const results = await getServer().runDeviceQueryAsync(adbArgs(device.pid, 'emu', 'avd', 'name'), 'emulator name query', signal);
    if (results.match(/could not connect to TCP port .*: Connection refused/)) {
        // Can also occur when the emulator does not exist.
        throw new _errors.CommandError('EMULATOR_NOT_FOUND', results);
    }
    return sanitizeAdbDeviceName(results) ?? null;
}
async function isDeviceBootedAsync({ pid, name } = {}) {
    const devices = await getAttachedDevicesAsync();
    if (pid) return devices.find((device)=>device.pid === pid) ?? null;
    if (name) return devices.find((device)=>device.name === name) ?? null;
    return devices[0] ?? null;
}
async function isBootAnimationCompleteAsync(pid, signal) {
    const props = await getPropertyDataForDeviceAsync({
        pid
    }, PROP_BOOT_ANIMATION_STATE, signal);
    return !!props[PROP_BOOT_ANIMATION_STATE]?.match(/stopped/);
}
async function getDeviceABIsAsync(device) {
    const cpuAbiList = (await getPropertyDataForDeviceAsync(device, PROP_CPU_ABI_LIST_NAME))[PROP_CPU_ABI_LIST_NAME];
    if (cpuAbiList) {
        return cpuAbiList.trim().split(',');
    }
    const abi = (await getPropertyDataForDeviceAsync(device, PROP_CPU_NAME))[PROP_CPU_NAME];
    return [
        abi
    ];
}
async function getPropertyDataForDeviceAsync(device, prop, signal) {
    const propCommand = prop ? adbShellArgs(device.pid, 'getprop', prop) : adbShellArgs(device.pid, 'getprop');
    try {
        const results = await getServer().getFileOutputAsync(propCommand, {
            signal
        });
        // Like:
        // [wifi.direct.interface]: [p2p-dev-wlan0]
        // [wifi.interface]: [wlan0]
        if (prop) {
            (0, _events.event)('adb_property_data', {
                devicePid: device.pid,
                prop,
                data: results
            });
            return {
                [prop]: results
            };
        }
        const props = parseAdbDeviceProperties(results);
        (0, _events.event)('adb_parsed_properties', {
            props
        });
        return props;
    } catch (error) {
        throw (0, _adbDiagnostics.createAdbOperationError)('ADB_PROPERTY', error, device);
    }
}
function parseAdbDeviceProperties(devicePropertiesString) {
    const properties = {};
    const propertyExp = /\[(.*?)\]: \[(.*?)\]/gm;
    for (const match of devicePropertiesString.matchAll(propertyExp)){
        properties[match[1]] = match[2];
    }
    return properties;
}
function sanitizeAdbDeviceName(deviceName) {
    return deviceName.trim().split(/[\r\n]+/).shift();
}

//# sourceMappingURL=adb.js.map
