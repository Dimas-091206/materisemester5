import { ProjectPrerequisite } from '../../../doctor/Prerequisite';
import { type EnsureDependenciesOptions } from '../../../doctor/dependencies/ensureDependenciesAsync';
export declare class AtlasPrerequisite extends ProjectPrerequisite<boolean, Pick<EnsureDependenciesOptions, 'exp'>> {
    assertImplementation({ exp }?: Pick<EnsureDependenciesOptions, 'exp'>): Promise<boolean>;
    bootstrapAsync({ exp }?: Pick<EnsureDependenciesOptions, 'exp'>): Promise<void>;
    private ensureAtlasInstalled;
}
