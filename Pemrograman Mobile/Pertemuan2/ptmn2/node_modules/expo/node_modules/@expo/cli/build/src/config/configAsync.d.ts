import type { ExpoConfig, ProjectConfig } from '@expo/config';
type Options = {
    type?: string;
    full?: boolean;
    json?: boolean;
};
export declare function logConfig(config: ExpoConfig | ProjectConfig): void;
export declare function configAsync(projectRoot: string, options: Options): Promise<void>;
export {};
