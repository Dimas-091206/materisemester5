import type Server from '@expo/metro/metro/Server';
import type { ServerLike } from '../BundlerDevServer';
export declare const CAPTURE_DYNAMIC_PARAMS: RegExp;
export declare const CATCH_ALL: RegExp;
export declare const SLUG: RegExp;
export declare const ARRAY_GROUP_REGEX: RegExp;
export declare const CAPTURE_GROUP_REGEX: RegExp;
/**
 * Match:
 *  - _layout files, +html, +not-found, string+api, etc
 *  - Routes can still use `+`, but it cannot be in the last segment.
 */
export declare const TYPED_ROUTES_EXCLUSION_REGEX: RegExp;
export interface SetupTypedRoutesOptions {
    server?: ServerLike;
    metro?: Server | null;
    typesDirectory: string;
    projectRoot: string;
    /** Absolute expo router routes directory. */
    routerDirectory: string;
    plugin?: Record<string, any>;
}
export declare function setupTypedRoutes(options: SetupTypedRoutesOptions): Promise<void>;
export declare function getTemplateString(staticRoutes: Set<string>, dynamicRoutes: Set<string>, dynamicRouteTemplates: Set<string>): string;
/**
 * Utility functions for typed routes
 *
 * These are extracted for easier testing
 */
export declare function getTypedRoutesUtils(appRoot: string, filePathSeperator?: "/" | "\\"): {
    staticRoutes: Map<string, Set<string>>;
    dynamicRoutes: Map<string, Set<string>>;
    filePathToRoute: (filePath: string) => string;
    addFilePath: (filePath: string) => boolean;
    isRouteFile: (filePath: string) => boolean | "";
};
export declare const setToUnionType: <T>(set: Set<T>) => string;
/**
 * Given a route, return all possible routes that could be generated from it.
 */
export declare function extrapolateGroupRoutes(route: string, routes?: Set<string>): Set<string>;
