import type { WebSocket } from 'ws';
export type SocketId = string;
export type SocketMap = Map<string, WebSocket>;
export declare function createSocketMap(): {
    map: SocketMap;
    registerSocket: (socket: WebSocket) => {
        id: string;
        terminate: () => void;
    };
    findSocket: (id: SocketId) => WebSocket | null;
};
