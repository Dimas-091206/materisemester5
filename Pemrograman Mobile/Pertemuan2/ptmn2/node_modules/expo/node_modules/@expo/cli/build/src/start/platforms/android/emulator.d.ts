import type { Device } from './adb';
export declare const EMULATOR_MAX_WAIT_TIMEOUT: number;
export declare function whichEmulator(): string;
/** Returns a list of emulator names. */
export declare function listAvdsAsync(): Promise<Device[]>;
/** Start an Android device and wait until it is booted. */
export declare function startDeviceAsync(device: Pick<Device, 'name'>, { timeout, interval, signal, }?: {
    /** Time in milliseconds to wait before asserting a timeout error. */
    timeout?: number;
    interval?: number;
    signal?: AbortSignal;
}): Promise<Device>;
