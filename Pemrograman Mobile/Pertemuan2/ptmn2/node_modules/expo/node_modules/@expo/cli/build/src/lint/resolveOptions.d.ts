export type Options = {
    ext: string[];
    config?: string;
    cache: boolean;
    fix: boolean;
    fixType: string[];
    ignore: boolean;
    ignorePattern: string[];
    quiet: boolean;
    maxWarnings: number;
};
export declare function resolveArgsAsync(argv: string[]): Promise<{
    variadic: string[];
    options: Options;
    extras: string[];
}>;
