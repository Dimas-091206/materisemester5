"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
0 && (module.exports = {
    isAdbDeviceStateUsable: null,
    parseAdbDeviceList: null
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: Object.getOwnPropertyDescriptor(all, name).get
    });
}
_export(exports, {
    get isAdbDeviceStateUsable () {
        return isAdbDeviceStateUsable;
    },
    get parseAdbDeviceList () {
        return parseAdbDeviceList;
    }
});
function isAdbDeviceStateUsable(state) {
    return state === 'device';
}
const DEVICE_LIST_HEADER = 'List of devices attached';
function parseAdbDeviceList(output) {
    return output.split(/\r?\n/).map((line)=>line.trim()).filter((line)=>line && line !== DEVICE_LIST_HEADER && !/\.cpp:[0-9]+/.test(line)).map(parseDeviceLine).filter((record)=>record != null);
}
function parseDeviceLine(line) {
    const match = line.match(/^(\S+)\s+(.+)$/);
    if (!match) {
        return null;
    }
    const [, serial, remainder] = match;
    const fields = remainder.split(/\s+/);
    const state = fields[0] === 'no' && fields[1] === 'permissions' ? 'no permissions' : fields[0];
    const metadata = fields.slice(state === 'no permissions' ? 2 : 1);
    const transportId = metadata.find((field)=>field.startsWith('transport_id:'))?.slice('transport_id:'.length);
    return {
        serial: serial,
        state,
        metadata,
        transportId
    };
}

//# sourceMappingURL=adbDeviceList.js.map
