import { CommandError } from '../../../utils/errors';
import type { AdbEndpoint, AdbHostProbeResult } from './adbEndpoint';
export type AdbDeviceDiagnostic = {
    pid?: string;
    state?: string;
    type?: 'emulator' | 'device';
};
export declare function formatAdbDeviceError(error: unknown, device: AdbDeviceDiagnostic): string;
export declare function formatAdbDiscoveryError(error: unknown, endpoint: AdbEndpoint, hostProbe?: AdbHostProbeResult): string;
export declare function shouldProbeAdbHost(error: unknown): boolean;
export declare function createAdbOperationError(code: string, error: unknown, device?: AdbDeviceDiagnostic): CommandError;
export declare function isAdbDeviceDisconnectedError(error: unknown): boolean;
