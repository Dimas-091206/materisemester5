import type { ExpoConfig, BuildCacheProviderPlugin, BuildCacheProvider, RunOptions } from '@expo/config';
export declare const resolveBuildCacheProvider: (provider: Required<ExpoConfig>["buildCacheProvider"] | undefined, projectRoot: string) => Promise<BuildCacheProvider | undefined>;
export declare function resolveBuildCache({ projectRoot, platform, provider, runOptions, }: {
    projectRoot: string;
    platform: 'android' | 'ios';
    provider: BuildCacheProvider;
    runOptions: RunOptions;
}): Promise<string | null>;
export declare function uploadBuildCache({ projectRoot, platform, provider, buildPath, runOptions, }: {
    projectRoot: string;
    platform: 'android' | 'ios';
    provider: BuildCacheProvider;
    buildPath: string;
    runOptions: RunOptions;
}): Promise<void>;
export declare function resolvePluginFunction(projectRoot: string, pluginReference: string): BuildCacheProviderPlugin;
