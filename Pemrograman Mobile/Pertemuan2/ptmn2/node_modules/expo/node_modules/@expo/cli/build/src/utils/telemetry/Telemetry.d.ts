import type { TelemetryClientStrategy, TelemetryRecord } from './types';
type TelemetryOptions = {
    /** A locally generated ID, untraceable to an actual user */
    anonymousId?: string;
    /** A locally generated ID, per CLI invocation */
    sessionId?: string;
    /** The authenticated user ID, this is used to generate an untraceable hash */
    userId?: string;
    /** The underlying telemetry strategy to use */
    strategy?: TelemetryClientStrategy;
};
export declare class Telemetry {
    private context;
    private client;
    private actor;
    /** A list of all events, recorded before the telemetry was fully initialized */
    private earlyRecords;
    constructor({ anonymousId, sessionId, userId, strategy, }?: TelemetryOptions);
    get strategy(): TelemetryClientStrategy;
    setStrategy(strategy: TelemetryOptions['strategy']): this | undefined;
    get isInitialized(): boolean;
    initialize({ userId }: {
        userId: string | null;
    }): void;
    private flushEarlyRecords;
    private recordInternal;
    record(record: TelemetryRecord | TelemetryRecord[]): void | Promise<void>;
    flush(): void | Promise<void>;
    flushOnExit(): void | Promise<void>;
}
export {};
