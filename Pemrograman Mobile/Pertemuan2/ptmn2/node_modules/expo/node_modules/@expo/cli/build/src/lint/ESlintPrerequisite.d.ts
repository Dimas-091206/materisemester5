import { ProjectPrerequisite } from '../start/doctor/Prerequisite';
/** Ensure the project has the required ESLint config. */
export declare class ESLintProjectPrerequisite extends ProjectPrerequisite<boolean> {
    assertImplementation(): Promise<boolean>;
    bootstrapAsync(): Promise<boolean>;
    _ensureDependenciesInstalledAsync({ skipPrompt, isProjectMutable, }: {
        skipPrompt?: boolean;
        isProjectMutable?: boolean;
    }): Promise<boolean>;
}
