import type { Options, ProjectInfo, XcodeConfiguration } from '../XcodeBuild.types';
type NativeSchemeProps = {
    name: string;
    osType?: string;
};
export declare function resolveNativeSchemePropsAsync(projectRoot: string, options: Pick<Options, 'scheme' | 'configuration'>, xcodeProject: ProjectInfo): Promise<NativeSchemeProps>;
/** Resolve the native iOS build `scheme` for a given `configuration`. If the `scheme` isn't provided then the user will be prompted to select one. */
export declare function promptOrQueryNativeSchemeAsync(projectRoot: string, { scheme, configuration }: {
    scheme?: string | boolean;
    configuration?: XcodeConfiguration;
}): Promise<NativeSchemeProps | null>;
export declare function getDefaultNativeScheme(projectRoot: string, options: Pick<Options, 'configuration'>, xcodeProject: Pick<ProjectInfo, 'name'>): NativeSchemeProps;
export {};
