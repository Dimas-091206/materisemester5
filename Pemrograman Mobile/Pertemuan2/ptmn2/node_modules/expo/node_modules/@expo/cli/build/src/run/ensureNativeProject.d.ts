import type { ModPlatform } from '@expo/config-plugins';
export declare function ensureNativeProjectAsync(projectRoot: string, { platform, install }: {
    platform: ModPlatform;
    install?: boolean;
}): Promise<boolean>;
