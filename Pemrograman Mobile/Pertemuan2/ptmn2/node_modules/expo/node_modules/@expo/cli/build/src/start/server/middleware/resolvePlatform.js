"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
0 && (module.exports = {
    assertMissingRuntimePlatform: null,
    assertRuntimePlatform: null,
    parsePlatformHeader: null,
    resolvePlatformFromUserAgentHeader: null
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: Object.getOwnPropertyDescriptor(all, name).get
    });
}
_export(exports, {
    get assertMissingRuntimePlatform () {
        return assertMissingRuntimePlatform;
    },
    get assertRuntimePlatform () {
        return assertRuntimePlatform;
    },
    get parsePlatformHeader () {
        return parsePlatformHeader;
    },
    get resolvePlatformFromUserAgentHeader () {
        return resolvePlatformFromUserAgentHeader;
    }
});
function _url() {
    const data = require("url");
    _url = function() {
        return data;
    };
    return data;
}
const _errors = require("../../../utils/errors");
function parsePlatformHeader(req) {
    const url = (0, _url().parse)(req.url, /* parseQueryString */ true);
    const platform = url.query?.platform || req.headers['expo-platform'] || req.headers['exponent-platform'];
    return (Array.isArray(platform) ? platform[0] : platform) ?? null;
}
function resolvePlatformFromUserAgentHeader(req) {
    let platform = null;
    const userAgent = req.headers['user-agent'];
    if (userAgent?.match(/Android/i)) {
        platform = 'android';
    }
    if (userAgent?.match(/OculusBrowser|Quest/i)) {
        platform = 'android';
    }
    if (userAgent?.match(/iPhone|iPad/i)) {
        platform = 'ios';
    }
    return platform;
}
function assertMissingRuntimePlatform(platform) {
    if (!platform) {
        throw new _errors.CommandError('PLATFORM_HEADER', `Must specify "expo-platform" header or "platform" query parameter`);
    }
}
function assertRuntimePlatform(platform) {
    const stringifiedPlatform = String(platform);
    if (![
        'android',
        'ios',
        'web',
        'tvos',
        'macos'
    ].includes(stringifiedPlatform)) {
        throw new _errors.CommandError('PLATFORM_HEADER', `platform must be "android", "ios", "web", "tvos", or "macos". Received: "${platform}"`);
    }
}

//# sourceMappingURL=resolvePlatform.js.map
