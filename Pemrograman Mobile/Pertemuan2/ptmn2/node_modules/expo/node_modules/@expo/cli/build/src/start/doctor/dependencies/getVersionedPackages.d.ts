import type { PackageJSONConfig } from '@expo/config';
export type DependencyList = Record<string, string>;
/** Get the known versions for a given SDK, combines all sources. */
export declare function getCombinedKnownVersionsAsync({ projectRoot, sdkVersion, skipCache, }: {
    projectRoot: string;
    sdkVersion?: string;
    skipCache?: boolean;
}): Promise<{
    [x: string]: string;
}>;
/** @returns a key/value list of known dependencies and their version (including range). */
export declare function getRemoteVersionsForSdkAsync({ sdkVersion, skipCache, }?: {
    sdkVersion?: string;
    skipCache?: boolean;
}): Promise<DependencyList>;
type ExcludedNativeModules = {
    name: string;
    bundledNativeVersion: string;
    isExcludedFromValidation: boolean;
    specifiedVersion?: string;
};
/**
 * Versions a list of `packages` against a given `sdkVersion` based on local and remote versioning resources.
 *
 * @param projectRoot
 * @param param1
 * @returns
 */
export declare function getVersionedPackagesAsync(projectRoot: string, { packages, sdkVersion, pkg, }: {
    /** List of npm packages to process. */
    packages: string[];
    /** Target SDK Version number to version the `packages` for. */
    sdkVersion: string;
    pkg: PackageJSONConfig;
}): Promise<{
    packages: string[];
    messages: string[];
    excludedNativeModules: ExcludedNativeModules[];
}>;
/** Craft a set of messages regarding the install operations. */
export declare function getOperationLog({ nativeModulesCount, sdkVersion, othersCount, }: {
    nativeModulesCount: number;
    othersCount: number;
    sdkVersion: string;
}): string[];
export {};
