import type { Device } from './adb';
export declare function promptForDeviceAsync(devices: Device[]): Promise<Device>;
/**
 * Format the device for prompt list.
 * @internal - Exposed for testing.
 */
export declare function formatDeviceChoice(device: Device): {
    title: string;
    value: string;
};
