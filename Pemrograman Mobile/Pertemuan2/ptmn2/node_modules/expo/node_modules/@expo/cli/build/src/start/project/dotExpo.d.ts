import type { JSONObject } from '@expo/json-file';
import JsonFile from '@expo/json-file';
/** Create a set of functions for managing a file in the project's `.expo` directory. */
export declare function createTemporaryProjectFile<T extends JSONObject>(fileName: string, defaults: T): {
    getFile: (projectRoot: string) => JsonFile<T>;
    readAsync: (projectRoot: string) => Promise<T>;
    setAsync: (projectRoot: string, json: Partial<T>) => Promise<T>;
};
export declare function ensureDotExpoProjectDirectoryInitialized(projectRoot: string): string;
