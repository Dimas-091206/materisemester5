import type { ServerNext, ServerRequest, ServerResponse } from './server.types';
/** Base middleware creator for Expo dev servers. */
export declare abstract class ExpoMiddleware {
    protected projectRoot: string;
    protected supportedPaths: string[];
    constructor(projectRoot: string, supportedPaths: string[]);
    /**
     * Returns true when the middleware should handle the incoming server request.
     * Exposed for testing.
     */
    shouldHandleRequest(req: ServerRequest): boolean;
    abstract handleRequestAsync(req: ServerRequest, res: ServerResponse, next: ServerNext): Promise<void>;
    /** Create a server middleware handler. */
    getHandler(): {
        (req: ServerRequest, res: ServerResponse, next: ServerNext): Promise<void>;
        internal: (req: ServerRequest, res: ServerResponse, next: ServerNext) => Promise<void>;
    };
}
export declare function disableResponseCache(res: ServerResponse): ServerResponse;
