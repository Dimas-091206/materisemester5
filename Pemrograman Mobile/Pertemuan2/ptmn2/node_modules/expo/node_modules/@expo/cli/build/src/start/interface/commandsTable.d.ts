import type { ExpoConfig } from '@expo/config';
import type { DependencyCheckRef } from '../checkDependenciesOnStart';
import type { McpServer } from '../server/MCP';
export declare const BLT = "\u203A";
export type StartOptions = {
    isWebSocketsEnabled?: boolean;
    devClient?: boolean;
    reset?: boolean;
    nonPersistent?: boolean;
    maxWorkers?: number;
    platforms?: ExpoConfig['platforms'];
    mcpServer?: McpServer;
    dependencyCheckRef?: DependencyCheckRef;
};
export declare const printHelp: () => void;
export declare const getTerminalColumns: () => number;
export declare const printItem: (text: string, opts?: {
    dim: boolean;
}) => string;
export declare function printUsage(options: Pick<StartOptions, 'devClient' | 'isWebSocketsEnabled' | 'platforms'>, { verbose, rows }: {
    verbose: boolean;
    rows?: number;
}): void;
