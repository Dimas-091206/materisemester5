import { type RouteNode } from 'expo-router/internal/routing';
import type { RoutesManifest } from 'expo-server/private';
import type { Options } from './getRoutesSSR';
export interface Group {
    pos: number;
    repeat: boolean;
    optional: boolean;
}
export interface RouteRegex {
    groups: Record<string, Group>;
    re: RegExp;
}
type GetServerManifestOptions = Pick<Options, 'headers' | 'pageHeaders'>;
export declare function getServerManifest(route: RouteNode | null, options?: GetServerManifestOptions): RoutesManifest<string>;
export {};
