import type { Platform } from '@expo/config';
import type { PlatformBundlers } from '../start/server/platformBundlers';
export type Options = {
    outputDir: string;
    platforms: Platform[];
    maxWorkers?: number;
    dev: boolean;
    clear: boolean;
    minify: boolean;
    bytecode: boolean;
    dumpAssetmap: boolean;
    sourceMaps: boolean;
    inlineSourceMaps: boolean;
    skipSSG: boolean;
    hostedNative: boolean;
};
/** Returns an array of platforms based on the input platform identifier and runtime constraints. */
export declare function resolvePlatformOption(configPlatforms: Platform[], platformBundlers: PlatformBundlers, platform?: string[]): Platform[];
export declare function resolveOptionsAsync(projectRoot: string, args: any): Promise<Options>;
