import type { OutputOptions } from '@expo/metro/metro/shared/types';
import type arg from 'arg';
import type { resolveCustomBooleanArgsAsync } from '../../utils/resolveArgs';
export interface Options {
    assetsDest?: string;
    assetCatalogDest?: string;
    entryFile: string;
    resetCache: boolean;
    transformer?: string;
    minify?: boolean;
    config?: string;
    platform: string;
    dev: boolean;
    bundleOutput: string;
    bundleEncoding?: OutputOptions['bundleEncoding'];
    maxWorkers?: number;
    sourcemapOutput?: string;
    sourcemapSourcesRoot?: string;
    sourcemapUseAbsolutePath: boolean;
    verbose: boolean;
    unstableTransformProfile?: string;
    eager?: boolean;
    bytecode?: boolean;
    skipServer?: boolean;
}
export declare function resolveOptions(projectRoot: string, args: arg.Result<arg.Spec>, parsed: Awaited<ReturnType<typeof resolveCustomBooleanArgsAsync>>): Options;
/** Best effort guess of which options will be used for the export:embed invocation that is called from the native build scripts. */
export declare function resolveEagerOptionsAsync(projectRoot: string, { dev, platform, assetsDest, bundleOutput, minify, ...options }: Partial<Omit<Options, 'platform' | 'dev'>> & {
    platform: string;
    dev: boolean;
}): Options;
export declare function getExportEmbedOptionsKey({ resetCache, assetsDest, bundleOutput, verbose, maxWorkers, eager, ...options }: Options): string;
export declare function deserializeEagerKey(key: string): {
    options: Options;
    key: string;
};
