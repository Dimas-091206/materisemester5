import type { ExpoConfig } from '@expo/config';
import type { BundleAssetWithFileHashes } from '../../../export/saveAssets';
type ManifestAsset = {
    fileHashes: string[];
    files: string[];
    hash: string;
};
export type Asset = ManifestAsset | BundleAssetWithFileHashes;
/** Inline the contents of each platform's `googleServicesFile` so runtimes can access them. */
export declare function resolveGoogleServicesFile(projectRoot: string, manifest: Partial<Pick<ExpoConfig, 'android' | 'ios'>>): Promise<Partial<Pick<ExpoConfig, "android" | "ios">>>;
/**
 * Get all fields in the manifest that match assets, then filter the ones that aren't set.
 *
 * @param manifest
 * @returns Asset fields that the user has set like ["icon", "splash.image", ...]
 */
export declare function getAssetFieldPathsForManifestAsync(manifest: ExpoConfig): Promise<string[]>;
/** Resolve all assets in the app.json inline. */
export declare function resolveManifestAssets(projectRoot: string, { manifest, resolver, strict, }: {
    manifest: ExpoConfig;
    resolver: (assetPath: string) => Promise<string>;
    strict?: boolean;
}): Promise<void>;
export {};
