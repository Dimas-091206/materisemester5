import type { ExpoConfig } from '@expo/config';
export declare class Bonjour {
    /** Project root directory */
    private projectRoot;
    /** Port to advertise, if any */
    private port;
    private stopAdvertising?;
    constructor(
    /** Project root directory */
    projectRoot: string, 
    /** Port to advertise, if any */
    port: number | undefined);
    announceAsync({ exp, username, }: {
        exp?: ExpoConfig;
        username?: string;
    }): Promise<void>;
    closeAsync(): Promise<boolean>;
}
