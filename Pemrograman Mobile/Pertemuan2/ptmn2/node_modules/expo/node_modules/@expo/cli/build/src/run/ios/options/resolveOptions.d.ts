import type { BuildProps, Options } from '../XcodeBuild.types';
/** Resolve arguments for the `run:ios` command. */
export declare function resolveOptionsAsync(projectRoot: string, options: Options): Promise<BuildProps>;
