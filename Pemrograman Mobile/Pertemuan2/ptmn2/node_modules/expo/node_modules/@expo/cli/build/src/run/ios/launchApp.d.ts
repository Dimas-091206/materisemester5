import type { DevServerManager } from '../../start/server/DevServerManager';
import type { BuildProps } from './XcodeBuild.types';
type BinaryLaunchInfo = {
    bundleId: string;
    schemes: string[];
};
/** Install and launch the app binary on a device. Requires a device to be specified. */
export declare function launchAppAsync(binaryPath: string, manager: DevServerManager, props: {
    isSimulator: boolean;
    device: NonNullable<BuildProps['device']>;
    shouldStartBundler: boolean;
}, appId?: string): Promise<void>;
export declare function getLaunchInfoForBinaryAsync(binaryPath: string): Promise<BinaryLaunchInfo>;
export {};
