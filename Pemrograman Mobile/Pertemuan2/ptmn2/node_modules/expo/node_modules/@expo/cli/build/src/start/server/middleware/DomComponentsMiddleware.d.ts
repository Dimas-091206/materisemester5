import type { ExpoMetroOptions } from './metroOptions';
import type { ServerRequest, ServerResponse } from './server.types';
export type PickPartial<T, K extends keyof T> = Omit<T, K> & Partial<Pick<T, K>>;
export declare const DOM_COMPONENTS_BUNDLE_DIR = "www.bundle";
type CreateDomComponentsMiddlewareOptions = {
    /** The absolute project root, used to resolve the `expo/dom/entry.js` path */
    projectRoot: string;
};
export declare function createDomComponentsMiddleware({ projectRoot }: CreateDomComponentsMiddlewareOptions, instanceMetroOptions: PickPartial<ExpoMetroOptions, 'mainModuleName' | 'platform' | 'bytecode'>): (req: ServerRequest, res: ServerResponse, next: (err?: Error) => void) => void | import("http").ServerResponse<import("http").IncomingMessage>;
export declare function getDomComponentHtml(src?: string, { title }?: {
    title?: string;
}): string;
export {};
