import type { Device } from './simctl';
/** Open the Simulator.app and return when the system registers it as 'open'. */
export declare function ensureSimulatorAppRunningAsync(device: Partial<Pick<Device, 'udid'>>, { maxWaitTime, }?: {
    maxWaitTime?: number;
}): Promise<void>;
