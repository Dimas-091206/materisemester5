import { AppIdResolver } from '../AppIdResolver';
/** Resolves the Android package name from the Expo config or native files. */
export declare class AndroidAppIdResolver extends AppIdResolver {
    constructor(projectRoot: string);
    hasNativeProjectAsync(): Promise<boolean>;
    resolveAppIdFromNativeAsync(): Promise<string | null>;
}
