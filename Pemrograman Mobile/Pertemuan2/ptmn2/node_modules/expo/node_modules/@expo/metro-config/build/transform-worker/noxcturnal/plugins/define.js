"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "createDefinePlugin", {
    enumerable: true,
    get: function() {
        return createDefinePlugin;
    }
});
const _noxcturnaltransformer = require("../noxcturnal-transformer");
function createDefinePlugin(nox) {
    return nox.defineNativePlugin({
        name: 'expo-define-globals',
        createState: (context)=>{
            const input = (0, _noxcturnaltransformer.expoPluginInput)(context);
            const { options } = input;
            const environment = options.customTransformOptions?.environment;
            const isServer = environment === 'node' || environment === 'react-server';
            const identifiers = new Map();
            const members = new Map();
            const typeofValues = new Map();
            members.set('process.env.EXPO_OS', options.platform);
            members.set('process.env.EXPO_SERVER', environment === 'node' || environment === 'react-server');
            if (!options.dev) {
                identifiers.set('__DEV__', false);
                members.set('Platform.OS', options.platform);
                members.set('process.env.NODE_ENV', 'production');
            }
            if (process.env.NODE_ENV !== 'test') {
                const baseUrl = options.customTransformOptions?.baseUrl;
                members.set('process.env.EXPO_BASE_URL', typeof baseUrl === 'string' ? decodeURI(baseUrl) : '');
            }
            if ((0, _noxcturnaltransformer.isNodeModule)(input.filename) && process.env.EXPO_PUBLIC_USE_RN_FETCH != null) {
                members.set('process.env.EXPO_PUBLIC_USE_RN_FETCH', process.env.EXPO_PUBLIC_USE_RN_FETCH);
            }
            const minifyTypeofWindow = options.customTransformOptions?.minifyTypeofWindow;
            if (String(minifyTypeofWindow ?? isServer) !== 'false') {
                typeofValues.set('window', isServer ? 'undefined' : 'object');
            }
            return {
                identifiers,
                members,
                typeofValues
            };
        },
        visitors: [
            nox.defineVisitor('ReferencedIdentifier', {
                fields: [
                    'name',
                    'global',
                    'read',
                    'write'
                ],
                where: {
                    name: {
                        equals: '__DEV__'
                    }
                }
            }, (path, state)=>{
                const { identifiers } = state;
                const name = String(path.node.name);
                if (!identifiers.has(name) || path.node.global !== true || path.node.write === true) {
                    return;
                }
                path.replaceWith((0, _noxcturnaltransformer.mappedLiteral)(path.context, identifiers.get(name)));
            }),
            nox.defineVisitor('StaticMemberExpression|ComputedMemberExpression', {
                fields: [
                    'memberPath'
                ],
                where: {
                    memberPath: {
                        oneOf: [
                            'Platform.OS',
                            'process.env.EXPO_OS',
                            'process.env.EXPO_SERVER',
                            'process.env.NODE_ENV',
                            'process.env.EXPO_BASE_URL',
                            'process.env.EXPO_PUBLIC_USE_RN_FETCH'
                        ]
                    },
                    write: {
                        equals: false
                    }
                },
                scope: true
            }, (path, state)=>{
                const { members } = state;
                const pattern = String(path.node.memberPath ?? '');
                if (!members.has(pattern)) return;
                const root = pattern.slice(0, pattern.indexOf('.'));
                if (path.scope.hasBinding(root)) return;
                path.replaceWith((0, _noxcturnaltransformer.mappedLiteral)(path.context, members.get(pattern)));
            }),
            nox.defineVisitor('UnaryExpression', {
                fields: [
                    'operator'
                ],
                where: {
                    operator: {
                        equals: 'typeof'
                    }
                },
                scope: true,
                children: {
                    argument: {
                        route: 'Expression',
                        fields: [
                            'name'
                        ]
                    }
                }
            }, (path, state)=>{
                const { typeofValues } = state;
                if (path.node.operator !== 'typeof') return;
                const argument = path.getChild('argument');
                if (!argument || argument.node.type !== 'Identifier') return;
                const name = String(argument.node.name);
                if (!typeofValues.has(name) || path.scope.hasBinding(name)) return;
                path.replaceWith((0, _noxcturnaltransformer.mappedLiteral)(path.context, typeofValues.get(name)));
            })
        ]
    });
}

//# sourceMappingURL=define.js.map
