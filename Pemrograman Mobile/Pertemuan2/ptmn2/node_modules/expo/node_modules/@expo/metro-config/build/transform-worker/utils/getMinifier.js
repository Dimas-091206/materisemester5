"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
0 && (module.exports = {
    getMinifier: null,
    resolveMinifier: null
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: Object.getOwnPropertyDescriptor(all, name).get
    });
}
_export(exports, {
    get getMinifier () {
        return _getMinifier().default;
    },
    get resolveMinifier () {
        return resolveMinifier;
    }
});
function _path() {
    const data = /*#__PURE__*/ _interop_require_default(require("path"));
    _path = function() {
        return data;
    };
    return data;
}
const _moduleMapper = require("./moduleMapper");
function _getMinifier() {
    const data = /*#__PURE__*/ _interop_require_default(require("@expo/metro/metro-transform-worker/utils/getMinifier"));
    _getMinifier = function() {
        return data;
    };
    return data;
}
function _interop_require_default(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
function resolveMinifier(request) {
    // We have to imitate how `getMinifier` resolves the minifier
    // It does so by requiring the `minifierPath` from the `metro-transform-worker/utils/getMinifier` base path
    // This means that when we're trying to resolve the minifier, we need to redirect the resolution to `metro-transform-worker`
    const moduleMapper = (0, _moduleMapper.createModuleMapper)();
    const metroTransformWorkerPath = moduleMapper('metro-transform-worker/package.json');
    if (metroTransformWorkerPath) {
        try {
            return require.resolve(request, {
                paths: [
                    _path().default.dirname(metroTransformWorkerPath)
                ]
            });
        } catch (error) {
            if (error.code !== 'MODULE_NOT_FOUND') {
                throw error;
            }
        }
    }
    return require.resolve(request);
}

//# sourceMappingURL=getMinifier.js.map
