/**
 * Copyright 2023-present 650 Industries (Expo). All rights reserved.
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */ "use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "transform", {
    enumerable: true,
    get: function() {
        return transform;
    }
});
function _countLines() {
    const data = /*#__PURE__*/ _interop_require_default(require("@expo/metro/metro/lib/countLines"));
    _countLines = function() {
        return data;
    };
    return data;
}
function _nodepath() {
    const data = require("node:path");
    _nodepath = function() {
        return data;
    };
    return data;
}
const _filePath = require("../utils/filePath");
const _browserslist = require("./browserslist");
const _css = require("./css");
const _cssmodules = require("./css-modules");
const _dotenvdevelopment = require("./dot-env-development");
const _events = require("./events");
const _metrotransformworker = /*#__PURE__*/ _interop_require_wildcard(require("./metro-transform-worker"));
const _postcss = require("./postcss");
const _sass = require("./sass");
const _transformShim = require("./transformShim");
function _interop_require_default(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
function _getRequireWildcardCache(nodeInterop) {
    if (typeof WeakMap !== "function") return null;
    var cacheBabelInterop = new WeakMap();
    var cacheNodeInterop = new WeakMap();
    return (_getRequireWildcardCache = function(nodeInterop) {
        return nodeInterop ? cacheNodeInterop : cacheBabelInterop;
    })(nodeInterop);
}
function _interop_require_wildcard(obj, nodeInterop) {
    if (!nodeInterop && obj && obj.__esModule) {
        return obj;
    }
    if (obj === null || typeof obj !== "object" && typeof obj !== "function") {
        return {
            default: obj
        };
    }
    var cache = _getRequireWildcardCache(nodeInterop);
    if (cache && cache.has(obj)) {
        return cache.get(obj);
    }
    var newObj = {
        __proto__: null
    };
    var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
    for(var key in obj){
        if (key !== "default" && Object.prototype.hasOwnProperty.call(obj, key)) {
            var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
            if (desc && (desc.get || desc.set)) {
                Object.defineProperty(newObj, key, desc);
            } else {
                newObj[key] = obj[key];
            }
        }
    }
    newObj.default = obj;
    if (cache) {
        cache.set(obj, newObj);
    }
    return newObj;
}
function getStringArray(value) {
    if (!value) return undefined;
    if (typeof value === 'string') {
        const parsed = JSON.parse(value);
        if (Array.isArray(parsed)) {
            return parsed;
        }
        throw new Error('Expected an array of strings for the `clientBoundaries` option.');
    }
    if (Array.isArray(value)) {
        return value;
    }
    throw new Error('Expected an array of strings for the `clientBoundaries` option.');
}
async function transform(config, projectRoot, filename, data, options) {
    const done = _events.debugEvent.span();
    try {
        const result = await transformImpl(config, projectRoot, filename, data, options);
        const customTransformOptions = options.customTransformOptions;
        if (customTransformOptions?.prewarm === '1') {
            for (const output of result.output){
                output.data.skipCache = true;
            }
        }
        done('file', {
            file: (0, _filePath.toPosixPath)(filename),
            platform: options.platform ?? null,
            environment: customTransformOptions?.environment ?? null,
            type: options.type,
            deps: result.dependencies.length,
            cached: false
        });
        return result;
    } catch (error) {
        (0, _events.event)('failed', {
            file: (0, _filePath.toPosixPath)(filename),
            error: _events.event.error(error)
        });
        throw error;
    }
}
async function transformImpl(config, projectRoot, filename, data, options) {
    const posixFilename = (0, _filePath.toPosixPath)(filename);
    if (typeof options.customTransformOptions?.dom === 'string' && posixFilename.match(/expo\/dom\/entry\.js/)) {
        // TODO: Find some method to do this without invalidating the cache between different DOM components.
        // Inject source for DOM component entry.
        const relativeDomComponentEntry = JSON.stringify(decodeURI(options.customTransformOptions.dom));
        const src = `require('expo/dom/internal').registerDOMComponent(require(${relativeDomComponentEntry}).default);`;
        return _metrotransformworker.transform(config, projectRoot, filename, Buffer.from(src), options);
    }
    if (posixFilename.match(/(^|\/)expo\/virtual\/rsc\.js/)) {
        const environment = options.customTransformOptions?.environment;
        const isServer = environment === 'node' || environment === 'react-server';
        if (!isServer) {
            const clientBoundaries = getStringArray(options.customTransformOptions?.clientBoundaries);
            // Inject client boundaries into the root client bundle for production bundling.
            if (clientBoundaries) {
                (0, _events.debugEvent)('client_boundaries:parsed', {
                    boundaries: clientBoundaries
                });
                // Inject source
                const src = 'module.exports = {\n' + clientBoundaries.map((boundary)=>{
                    const serializedBoundary = JSON.stringify(boundary);
                    return `[\`$\{require.resolveWeak(${serializedBoundary})}\`]: /* ${boundary} */ () => import(${serializedBoundary}),`;
                }).join('\n') + '\n};';
                return _metrotransformworker.transform(config, projectRoot, filename, Buffer.from('/* RSC client boundaries */\n' + src), options);
            }
        }
    }
    if (options.type !== 'asset' && /\.(s?css|sass)$/.test(filename)) {
        return transformCss(config, projectRoot, filename, data, options);
    }
    // If the file is not CSS, then use the default behavior.
    const environment = options.customTransformOptions?.environment;
    const isClientEnvironment = environment !== 'node' && environment !== 'react-server';
    if (isClientEnvironment && // TODO: Ensure this works with windows.
    (filename.match(new RegExp(`^app/\\+html(\\.${options.platform})?\\.([tj]sx?|[cm]js)?$`)) || // Strip +api files.
    filename.match(/\+api(\.(native|ios|android|web))?\.[tj]sx?$/) || // Strip +middleware files.
    filename.match(/\+middleware\.[tj]sx?$/))) {
        // Remove the server-only +html file and API Routes from the bundle when bundling for a client environment.
        return _metrotransformworker.transform(config, projectRoot, filename, !options.minify ? Buffer.from(// Use a string so this notice is visible in the bundle if the user is
        // looking for it.
        '"> The server-only file was removed from the client JS bundle by Expo CLI."') : Buffer.from(''), options);
    }
    if (isClientEnvironment && !filename.match(/\/node_modules\//) && filename.match(/\+api(\.(native|ios|android|web))?\.[tj]sx?$/)) {
        // Clear the contents of +api files when bundling for the client.
        // This ensures that the client doesn't accidentally use the server-only +api files.
        return _metrotransformworker.transform(config, projectRoot, filename, Buffer.from(''), options);
    }
    // Add support for parsing env files to JavaScript objects. Stripping the non-public variables in client environments.
    if (filename.match(/(^|\/)\.env(\.(local|(development|production)(\.local)?))?$/)) {
        const envFileParsed = (0, _dotenvdevelopment.parseEnvFile)(data.toString('utf-8'), isClientEnvironment);
        return _metrotransformworker.transform(config, projectRoot, filename, Buffer.from(`export default ${JSON.stringify(envFileParsed)};`), options);
    }
    if (// Noop the streams polyfill in the server environment.
    !isClientEnvironment && filename.match(/\/expo\/virtual\/streams\.js$/)) {
        return _metrotransformworker.transform(config, projectRoot, filename, Buffer.from(''), options);
    }
    if (// Parsing the virtual env is client-only, on the server we use `process.env` directly.
    isClientEnvironment && // Finally match the virtual env file.
    filename.match(/\/expo\/virtual\/env\.js$/)) {
        if (// Variables should be inlined in production. We only use this JS object to ensure HMR in development.
        options.dev) {
            const relativePath = (0, _nodepath().relative)((0, _nodepath().dirname)(filename), projectRoot);
            const posixPath = (0, _filePath.toPosixPath)(relativePath);
            // This virtual module uses a context module to conditionally observe and load all of the possible .env files in development.
            // We then merge them in the expected order.
            // This module still depends on the `process.env` polyfill in the serializer to include EXPO_PUBLIC_ variables that are
            // defined in the script or bash, essentially all places where HMR is not possible.
            // Finally, we export with `env` to align with the babel plugin that transforms static process.env usage to the virtual module.
            // The .env regex depends `watcher.additionalExts` being set correctly (`'env', 'local', 'development'`) so that .env files aren't resolved as platform extensions.
            const contents = `const dotEnvModules = require.context(${JSON.stringify(posixPath)},false,/^\\.\\/\\.env/);

    export const env = !dotEnvModules.keys().length ? process.env : { ...process.env, ...['.env', '.env.development', '.env.local', '.env.development.local'].reduce((acc, file) => {
      return { ...acc, ...(dotEnvModules(file)?.default ?? {}) };
    }, {}) };`;
            return _metrotransformworker.transform(config, projectRoot, filename, Buffer.from(contents), options);
        } else {
            // Add a fallback in production for sanity and better errors if something goes wrong or the user manually imports the virtual module somehow.
            // Create a proxy module where a helpful error is thrown whenever a key from `process.env` is accessed.
            const contents = `
        export const env = new Proxy({}, {
          get(target, key) {
            throw new Error(\`Attempting to access internal environment variable "\${key}" is not supported in production bundles. Environment variables should be inlined in production by Babel.\`);
          },
       });`;
            return _metrotransformworker.transform(config, projectRoot, filename, Buffer.from(contents), options);
        }
    }
    return _metrotransformworker.transform(config, projectRoot, filename, data, options);
}
function isReactServerEnvironment(options) {
    return options.customTransformOptions?.environment === 'react-server';
}
async function transformCss(config, projectRoot, filename, data, options) {
    // If the platform is not web, then return an empty module.
    if (options.platform !== 'web') {
        const code = (0, _cssmodules.matchCssModule)(filename) ? 'module.exports={ unstable_styles: {} };' : '';
        // TODO: Native CSS Modules
        return (0, _transformShim.transformShim)(config, filename, code);
    }
    let code = data.toString('utf8');
    // Apply postcss transforms
    const postcssResults = await (0, _postcss.transformPostCssModule)(projectRoot, {
        src: code,
        filename
    });
    if (postcssResults.hasPostcss) {
        code = postcssResults.src;
    }
    // TODO: When native has CSS support, this will need to move higher up.
    const syntax = (0, _sass.matchSass)(filename);
    if (syntax) {
        code = (0, _sass.compileSass)(projectRoot, {
            filename,
            src: code
        }, {
            syntax
        }).src;
    }
    // If the file is a CSS Module, then transform it to a JS module
    // in development and a static CSS file in production.
    if ((0, _cssmodules.matchCssModule)(filename)) {
        const results = await (0, _cssmodules.transformCssModuleWeb)({
            // NOTE(cedric): use POSIX-formatted filename for consistent CSS module class names.
            // This affects the content hashes, which should be stable across platforms.
            filename: (0, _filePath.toPosixPath)(filename),
            src: code,
            options: {
                reactServer: isReactServerEnvironment(options),
                projectRoot,
                dev: options.dev,
                minify: options.minify,
                sourceMap: false
            }
        });
        const jsModuleResults = (0, _transformShim.transformShim)(config, filename, results.output);
        const cssCode = results.css.toString();
        const output = [
            {
                type: 'js/module',
                data: {
                    ...jsModuleResults.output[0].data,
                    skipCache: !!postcssResults.hasPostcss || undefined,
                    // Append additional css metadata for static extraction.
                    css: {
                        code: cssCode,
                        lineCount: (0, _countLines().default)(cssCode),
                        map: [],
                        functionMap: null,
                        // Disable caching for CSS files when postcss is enabled and has been run on the file.
                        // This ensures that things like tailwind can update on every change.
                        skipCache: postcssResults.hasPostcss,
                        externalImports: results.externalImports
                    }
                }
            }
        ];
        return {
            dependencies: jsModuleResults.dependencies.concat(results.dependencies),
            output
        };
    }
    // Global CSS:
    const { transform } = require('lightningcss');
    // Here we delegate bundling to lightningcss to resolve all CSS imports together.
    // TODO: Add full CSS bundling support to Metro.
    const cssResults = transform({
        filename,
        code: Buffer.from(code),
        errorRecovery: true,
        sourceMap: false,
        cssModules: false,
        projectRoot,
        minify: options.minify,
        analyzeDependencies: true,
        targets: await (0, _browserslist.getBrowserslistTargets)(projectRoot),
        // targets: pkg?.browserslist,
        // @ts-expect-error: Added for testing against virtual file system.
        resolver: options._test_resolveCss,
        // https://lightningcss.dev/transpilation.html
        include: 1
    });
    (0, _cssmodules.printCssWarnings)(filename, code, cssResults.warnings);
    const cssImports = (0, _cssmodules.collectCssImports)(filename, code, cssResults.code.toString(), cssResults);
    const cssCode = cssImports.code;
    // Append additional css metadata for static extraction.
    const cssOutput = {
        code: cssCode,
        lineCount: (0, _countLines().default)(cssCode),
        map: [],
        functionMap: null,
        // Disable caching for CSS files when postcss is enabled and has been run on the file.
        // This ensures that things like tailwind can update on every change.
        skipCache: postcssResults.hasPostcss,
        externalImports: cssImports.externalImports
    };
    const reactServer = isReactServerEnvironment(options);
    // Create a mock JS module that exports an empty object,
    // this ensures Metro dependency graph is correct.
    const jsModuleResults = (0, _transformShim.transformShim)(config, filename, options.dev ? (0, _css.wrapDevelopmentCSS)({
        src: cssCode,
        filename,
        reactServer
    }) : '');
    // In production, we export the CSS as a string and use a special type to prevent
    // it from being included in the JS bundle. We'll extract the CSS like an asset later
    // and append it to the HTML bundle.
    const output = [
        {
            type: 'js/module',
            data: {
                ...jsModuleResults.output[0].data,
                css: cssOutput,
                skipCache: !!postcssResults.hasPostcss || undefined
            }
        }
    ];
    return {
        dependencies: jsModuleResults.dependencies.concat(cssImports.dependencies),
        output
    };
}
/**
 * A custom Metro transformer that adds support for processing Expo-specific bundler features.
 * - Global CSS files on web.
 * - CSS Modules on web.
 * - TODO: Tailwind CSS on web.
 */ module.exports = {
    // Use defaults for everything that's not custom.
    ..._metrotransformworker,
    transform
};

//# sourceMappingURL=transform-worker.js.map
