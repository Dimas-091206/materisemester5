"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "transformShim", {
    enumerable: true,
    get: function() {
        return transformShim;
    }
});
function _core() {
    const data = require("@babel/core");
    _core = function() {
        return data;
    };
    return data;
}
function _generator() {
    const data = /*#__PURE__*/ _interop_require_default(require("@babel/generator"));
    _generator = function() {
        return data;
    };
    return data;
}
function _JsFileWrapping() {
    const data = /*#__PURE__*/ _interop_require_wildcard(require("@expo/metro/metro/ModuleGraph/worker/JsFileWrapping"));
    _JsFileWrapping = function() {
        return data;
    };
    return data;
}
const _packedMap = require("../serializer/packedMap");
function _interop_require_default(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
function _getRequireWildcardCache(nodeInterop) {
    if (typeof WeakMap !== "function") return null;
    var cacheBabelInterop = new WeakMap();
    var cacheNodeInterop = new WeakMap();
    return (_getRequireWildcardCache = function(nodeInterop) {
        return nodeInterop ? cacheNodeInterop : cacheBabelInterop;
    })(nodeInterop);
}
function _interop_require_wildcard(obj, nodeInterop) {
    if (!nodeInterop && obj && obj.__esModule) {
        return obj;
    }
    if (obj === null || typeof obj !== "object" && typeof obj !== "function") {
        return {
            default: obj
        };
    }
    var cache = _getRequireWildcardCache(nodeInterop);
    if (cache && cache.has(obj)) {
        return cache.get(obj);
    }
    var newObj = {
        __proto__: null
    };
    var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
    for(var key in obj){
        if (key !== "default" && Object.prototype.hasOwnProperty.call(obj, key)) {
            var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
            if (desc && (desc.get || desc.set)) {
                Object.defineProperty(newObj, key, desc);
            } else {
                newObj[key] = obj[key];
            }
        }
    }
    newObj.default = obj;
    if (cache) {
        cache.set(obj, newObj);
    }
    return newObj;
}
function transformShim(config, filename, body) {
    const parsed = (0, _core().parse)(body, {
        sourceType: 'unambiguous',
        babelrc: false,
        configFile: false,
        cloneInputAst: false
    });
    if (parsed == null) {
        throw new Error(`transformShim could not parse the synthesized module body for ${filename}. ` + `This is a bug in @expo/metro-config — the shim path is only used for hand-crafted ` + `module bodies which should always parse with default Babel options.`);
    }
    const ast = parsed;
    const wrappedAst = config.unstable_disableModuleWrapping === true ? ast : _JsFileWrapping().wrapModule(ast, '_$$_IMPORT_DEFAULT', '_$$_IMPORT_ALL', config.unstable_dependencyMapReservedName ?? 'dependencyMap', config.globalPrefix, config.unstable_renameRequire === false).ast;
    const { code } = (0, _generator().default)(wrappedAst, {
        comments: false,
        compact: config.unstable_compactOutput ?? false,
        filename,
        retainLines: false,
        sourceMaps: false
    });
    const map = (0, _packedMap.emptySourceMap)();
    const { lineCount } = (0, _packedMap.countLinesAndTerminateSourceMap)(code, map);
    const output = [
        {
            type: 'js/module',
            data: {
                code,
                functionMap: null,
                lineCount,
                map
            }
        }
    ];
    return {
        dependencies: [],
        output
    };
}

//# sourceMappingURL=transformShim.js.map
