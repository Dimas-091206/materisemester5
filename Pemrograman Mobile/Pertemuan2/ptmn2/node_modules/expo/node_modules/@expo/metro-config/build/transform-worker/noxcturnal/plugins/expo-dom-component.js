"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "createExpoDomComponentPlugin", {
    enumerable: true,
    get: function() {
        return createExpoDomComponentPlugin;
    }
});
function _nodecrypto() {
    const data = require("node:crypto");
    _nodecrypto = function() {
        return data;
    };
    return data;
}
function _nodepath() {
    const data = /*#__PURE__*/ _interop_require_default(require("node:path"));
    _nodepath = function() {
        return data;
    };
    return data;
}
function _nodeurl() {
    const data = require("node:url");
    _nodeurl = function() {
        return data;
    };
    return data;
}
const _noxcturnaltransformer = require("../noxcturnal-transformer");
function _interop_require_default(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
function createExpoDomComponentPlugin(nox) {
    return nox.defineNativePlugin({
        name: 'expo-dom-component',
        editEffect: 'bindings',
        contract: {
            metadata: {
                writes: [
                    'expoDomComponentReference'
                ]
            }
        },
        createState: (context)=>({
                input: (0, _noxcturnaltransformer.expoPluginInput)(context),
                enabled: false,
                hasDefault: false,
                displayName: 'Component'
            }),
        visitors: [
            nox.defineVisitor('Program', {
                children: {
                    directives: {
                        route: 'Directive',
                        fields: [
                            'value'
                        ]
                    }
                }
            }, (program, state)=>{
                state.enabled = state.input.options.platform !== 'web' && program.getChildList('directives').some((directive)=>directive.node.value === 'use dom');
            }),
            nox.defineVisitor('ExportNamedDeclaration', {
                fields: [
                    'exportKind'
                ],
                ancestry: {
                    mode: 'programParent'
                },
                children: {
                    declaration: {
                        route: 'Declaration',
                        fields: [
                            'nodeKind'
                        ]
                    }
                }
            }, (exportPath, state)=>{
                if (!state.enabled || exportPath.parentPath?.node.type !== 'Program') return;
                if (exportPath.node.exportKind === 'type') return;
                const kind = exportPath.getChild('declaration')?.node.type;
                if (kind === 'TSInterfaceDeclaration' || kind === 'TSTypeAliasDeclaration') return;
                exportPath.unsupported('dom-component-named-export');
            }),
            nox.defineVisitor('ExportDefaultDeclaration', {
                ancestry: {
                    mode: 'programParent'
                },
                children: {
                    declaration: {
                        route: 'Declaration|Expression',
                        fields: [
                            'nodeKind',
                            'name'
                        ]
                    }
                }
            }, (exportPath, state)=>{
                if (!state.enabled || exportPath.parentPath?.node.type !== 'Program') return;
                state.hasDefault = true;
                const declaration = exportPath.getChild('declaration');
                if (declaration?.node.nodeKind === 'FunctionDeclaration' && typeof declaration.node.name === 'string') {
                    state.displayName = declaration.node.name;
                }
            })
        ],
        post (context, state) {
            if (!state.enabled) return;
            if (!state.hasDefault) context.unsupported('dom-component-missing-default');
            const basename = _nodepath().default.basename(state.input.filename);
            if (state.input.filename.includes(state.input.projectRoot) && !(0, _noxcturnaltransformer.isNodeModule)(state.input.filename)) {
                if (/^_layout\.[jt]sx?$/.test(basename)) {
                    context.unsupported('dom-component-layout-route');
                }
                if (/\+api\.[jt]sx?$/.test(basename)) {
                    context.unsupported('dom-component-api-route');
                }
            }
            const outputKey = (0, _nodeurl().pathToFileURL)(state.input.filename).href;
            const filePath = state.input.options.dev ? `${JSON.stringify(`${basename}?file=`)} + ${JSON.stringify(outputKey)}` : JSON.stringify(`${(0, _nodecrypto().createHash)('md5').update(outputKey).digest('hex')}.html`);
            const proxy = `import React from "react";
import { WebView } from "expo/dom/internal";
const filePath = ${filePath};
const _Expo_DOMProxyComponent = React.forwardRef((props, ref) => React.createElement(WebView, { ref, ...props, filePath }));
if (__DEV__) _Expo_DOMProxyComponent.displayName = ${JSON.stringify(`DOM(${state.displayName})`)};
export default _Expo_DOMProxyComponent;`;
            context.editor.overwrite(0, context.source.length, proxy, 'sourceless');
            context.metadata.set('expoDomComponentReference', outputKey);
        }
    });
}

//# sourceMappingURL=expo-dom-component.js.map
