"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "createDeepReactNativeImportWarningsPlugin", {
    enumerable: true,
    get: function() {
        return createDeepReactNativeImportWarningsPlugin;
    }
});
const _noxcturnaltransformer = require("../noxcturnal-transformer");
function createDeepReactNativeImportWarningsPlugin(nox) {
    const record = (source, path, state)=>{
        if (typeof source !== 'string' || !source.startsWith('react-native/') || source === 'react-native/Libraries/Core/InitializeCore') return;
        const location = path.getLocation().start;
        state.warnings.push({
            source,
            line: location.line,
            column: location.column
        });
    };
    return nox.defineNativePlugin({
        name: 'expo-warn-deep-react-native-imports',
        createState: ()=>({
                warnings: []
            }),
        visitors: [
            nox.defineVisitor('ImportDeclaration', {
                fields: [
                    'source'
                ]
            }, (path, state)=>record(path.node.source, path, state)),
            nox.defineVisitor('ExportNamedDeclaration', {
                fields: [
                    'source'
                ]
            }, (path, state)=>record(path.node.source, path, state)),
            nox.defineVisitor('CallExpression', {
                fields: [
                    'calleeName',
                    'staticArgument',
                    'argumentCount'
                ]
            }, (path, state)=>{
                if (path.node.calleeName === 'require' && path.node.argumentCount === 1) {
                    record(path.node.staticArgument, path, state);
                }
            })
        ],
        post (context, state) {
            const { filename } = (0, _noxcturnaltransformer.expoPluginInput)(context);
            for (const warning of state.warnings){
                const message = `Deep imports from the 'react-native' package are deprecated ('${warning.source}').` + ` Source: ${filename} ${warning.line}:${warning.column}`;
                context.editor.prependLeft(context.source.length, `\nconsole.warn(${JSON.stringify(message)});`);
            }
        }
    });
}

//# sourceMappingURL=deep-react-native-import-warnings.js.map
