"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
0 && (module.exports = {
    assignExportHelper: null,
    defaultWrapHelper: null,
    esModuleExportTemplate: null,
    liveExportAllHelper: null,
    liveExportHelper: null,
    namespaceWrapHelper: null,
    nullBoundExpression: null,
    requireCall: null,
    sideEffectRequireCall: null,
    varDeclaratorCallHelper: null,
    varDeclaratorHelper: null,
    withLocation: null
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: Object.getOwnPropertyDescriptor(all, name).get
    });
}
_export(exports, {
    get assignExportHelper () {
        return assignExportHelper;
    },
    get defaultWrapHelper () {
        return defaultWrapHelper;
    },
    get esModuleExportTemplate () {
        return esModuleExportTemplate;
    },
    get liveExportAllHelper () {
        return liveExportAllHelper;
    },
    get liveExportHelper () {
        return liveExportHelper;
    },
    get namespaceWrapHelper () {
        return namespaceWrapHelper;
    },
    get nullBoundExpression () {
        return nullBoundExpression;
    },
    get requireCall () {
        return requireCall;
    },
    get sideEffectRequireCall () {
        return sideEffectRequireCall;
    },
    get varDeclaratorCallHelper () {
        return varDeclaratorCallHelper;
    },
    get varDeclaratorHelper () {
        return varDeclaratorHelper;
    },
    get withLocation () {
        return withLocation;
    }
});
const defaultWrapHelper = ({ statement }, name)=>statement(`
    function %%name%%(e) {
      return e && e.__esModule ? e : { default: e };
    }
  `)({
        name
    });
const namespaceWrapHelper = ({ statement }, name)=>{
    // NOTE(@kitten): A looser option than the above that matches Metro's legacy behaviour
    return statement(`
    function %%name%%(e) {
      if (e && e.__esModule) return e;
      var n = {};
      if (e) Object.keys(e).forEach(function (k) {
        if (k === 'default') return;
        var d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: function () { return e[k]; }
        });
      });
      n.default = e;
      return n;
    }
  `)({
        name
    });
};
const liveExportAllHelper = ({ statement }, id)=>{
    return statement(`
    Object.keys(%%id%%).forEach(function (k) {
      if (k !== 'default' && !Object.prototype.hasOwnProperty.call(exports, k)) {
        Object.defineProperty(exports, k, {
          enumerable: true,
          get: function () { return %%id%%[k]; }
        });
      }
    });
  `)({
        id
    });
};
const liveExportHelper = (t, exportName, expr)=>{
    return t.expressionStatement(t.callExpression(t.memberExpression(t.identifier('Object'), t.identifier('defineProperty')), [
        t.identifier('exports'),
        t.stringLiteral(exportName),
        t.objectExpression([
            t.objectProperty(t.identifier('enumerable'), t.booleanLiteral(true)),
            t.objectProperty(t.identifier('get'), t.functionExpression(null, [], t.blockStatement([
                t.returnStatement(expr)
            ])))
        ])
    ]));
};
const assignExportHelper = (t, exportName, expr)=>{
    if (exportName === '__proto__') {
        // NOTE(@kitten): `exports` is instantiated as `{}` instead of `Object.create(null)`, so the __proto__
        // assignment still carries its special meaning. We switch to the live export helper implicitly here
        // to avoid this
        return liveExportHelper(t, exportName, expr);
    }
    const member = t.isValidIdentifier(exportName) ? t.identifier(exportName) : t.stringLiteral(exportName);
    return t.expressionStatement(t.assignmentExpression('=', t.memberExpression(t.identifier('exports'), member), expr));
};
const varDeclaratorHelper = (t, name, expr)=>t.variableDeclaration('var', [
        t.variableDeclarator(t.identifier(name), expr)
    ]);
const requireCall = (t, id, source)=>t.variableDeclaration('var', [
        t.variableDeclarator(t.identifier(id), t.callExpression(t.identifier('require'), [
            source
        ]))
    ]);
const sideEffectRequireCall = (t, source)=>t.expressionStatement(t.callExpression(t.identifier('require'), [
        source
    ]));
const varDeclaratorCallHelper = (t, id, fn, arg)=>t.variableDeclaration('var', [
        t.variableDeclarator(t.identifier(id), t.callExpression(t.identifier(fn), [
            t.identifier(arg)
        ]))
    ]);
const esModuleExportTemplate = ({ statement })=>{
    return statement(`
    Object.defineProperty(exports, '__esModule', {value: true});
  `)();
};
const nullBoundExpression = (t, expr)=>t.parenthesizedExpression(t.sequenceExpression([
        t.numericLiteral(0),
        expr
    ]));
function withLocation(nodeOrArray, loc) {
    if (Array.isArray(nodeOrArray)) {
        return nodeOrArray.map((n)=>withLocation(n, loc));
    }
    const node = nodeOrArray;
    if (!node.loc) {
        node.loc = loc;
    }
    return node;
}

//# sourceMappingURL=helpers.js.map
