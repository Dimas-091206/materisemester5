"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "createMetroEsmGlobalsPlugin", {
    enumerable: true,
    get: function() {
        return createMetroEsmGlobalsPlugin;
    }
});
const _noxcturnaltransformer = require("../noxcturnal-transformer");
function pseudoGlobalName(scope, preferred) {
    return scope.hasBinding(preferred) ? scope.generateUid(preferred) : preferred;
}
function createMetroEsmGlobalsPlugin(nox) {
    return nox.defineNativePlugin({
        name: 'metro-native-esm-pseudo-global-renames',
        createState: ()=>({
                names: {
                    global: 'g',
                    module: 'm',
                    exports: 'e'
                }
            }),
        visitors: [
            nox.defineVisitor('Program', {
                scope: true
            }, (program, state)=>{
                for (const name of [
                    'global',
                    'require',
                    'module',
                    'exports'
                ]){
                    if (program.scope.hasBinding(name)) {
                        program.scope.rename(name, program.scope.generateUid(name));
                    }
                }
                if (!(0, _noxcturnaltransformer.metroPluginData)(program.context).normalizePseudoGlobals) return;
                state.names.global = pseudoGlobalName(program.scope, 'g');
                state.names.module = pseudoGlobalName(program.scope, 'm');
                state.names.exports = pseudoGlobalName(program.scope, 'e');
                (0, _noxcturnaltransformer.metroPluginData)(program.context).pseudoGlobals = state.names;
            }),
            nox.defineVisitor('Identifier', {
                fields: [
                    'name',
                    'global'
                ],
                where: {
                    name: {
                        oneOf: [
                            'global',
                            'module',
                            'exports'
                        ]
                    }
                }
            }, (identifier, state)=>{
                if (identifier.node.global !== true || !(0, _noxcturnaltransformer.metroPluginData)(identifier.context).normalizePseudoGlobals) {
                    return;
                }
                const replacement = state.names[String(identifier.node.name)];
                if (replacement) identifier.replaceWith(replacement);
            })
        ]
    });
}

//# sourceMappingURL=metro-esm-globals.js.map
