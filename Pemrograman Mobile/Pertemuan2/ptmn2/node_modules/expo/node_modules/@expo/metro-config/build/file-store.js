"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
0 && (module.exports = {
    FileStore: null,
    tryRenameAndDeleteAsync: null
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: Object.getOwnPropertyDescriptor(all, name).get
    });
}
_export(exports, {
    get FileStore () {
        return FileStore;
    },
    get tryRenameAndDeleteAsync () {
        return tryRenameAndDeleteAsync;
    }
});
function _FileStore() {
    const data = /*#__PURE__*/ _interop_require_default(require("@expo/metro/metro-cache/stores/FileStore"));
    _FileStore = function() {
        return data;
    };
    return data;
}
function _fs() {
    const data = /*#__PURE__*/ _interop_require_default(require("fs"));
    _fs = function() {
        return data;
    };
    return data;
}
function _os() {
    const data = /*#__PURE__*/ _interop_require_default(require("os"));
    _os = function() {
        return data;
    };
    return data;
}
function _path() {
    const data = /*#__PURE__*/ _interop_require_default(require("path"));
    _path = function() {
        return data;
    };
    return data;
}
const _cachestore = require("./cache-store");
const _events = require("./events");
function _interop_require_default(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
// On macOS `os.tmpdir()` returns `/var/folders/...` while its realpath is
// `/private/var/folders/...`; accept either form so callers that resolved
// symlinks aren't excluded.
function isInsideOsTmpdir(target) {
    const resolved = _path().default.resolve(target);
    const tmp = _path().default.resolve(_os().default.tmpdir());
    if (resolved !== tmp && resolved.startsWith(tmp + _path().default.sep)) {
        return true;
    }
    let tmpReal;
    try {
        tmpReal = _fs().default.realpathSync(tmp);
    } catch  {
        return false;
    }
    return resolved !== tmpReal && resolved.startsWith(tmpReal + _path().default.sep);
}
function tryRenameAndDeleteAsync(root) {
    if (!isInsideOsTmpdir(root)) {
        return false;
    }
    const tombstone = `${root}.delete-${process.pid}-${Date.now()}`;
    try {
        _fs().default.renameSync(root, tombstone);
    } catch (err) {
        if (err?.code === 'ENOENT') {
            return true;
        }
        (0, _events.event)('cache:rename_failed', {
            error: _events.event.error(err)
        });
        return false;
    }
    _fs().default.promises.rm(tombstone, {
        recursive: true,
        force: true,
        maxRetries: 3
    }).catch((err)=>{
        (0, _events.event)('cache:tombstone_remove_failed', {
            tombstone,
            error: _events.event.error(err)
        });
    });
    return true;
}
class FileStore extends _FileStore().default {
    _root;
    constructor(options){
        super(options);
        this._root = options.root;
    }
    async set(key, value) {
        if ((0, _cachestore.shouldSkipCache)(value)) {
            (0, _events.event)('cache:skipped', {});
            return;
        }
        return await super.set(key, value);
    }
    clear() {
        if (!tryRenameAndDeleteAsync(this._root)) {
            super.clear();
        }
    }
}

//# sourceMappingURL=file-store.js.map
