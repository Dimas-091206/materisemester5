"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "createDevelopmentPublicEnvPlugin", {
    enumerable: true,
    get: function() {
        return createDevelopmentPublicEnvPlugin;
    }
});
const _processenv = require("./process-env");
function createDevelopmentPublicEnvPlugin(nox) {
    return nox.defineNativePlugin({
        name: 'expo-reference-development-public-env',
        contract: {
            metadata: {
                writes: [
                    'publicEnvVars'
                ]
            }
        },
        createState: ()=>({
                binding: '',
                insertion: 0,
                names: new Set()
            }),
        visitors: [
            nox.defineVisitor('Program', {
                scope: true,
                children: {
                    directives: {
                        route: 'Directive',
                        fields: [
                            'value'
                        ]
                    }
                }
            }, (program, state)=>{
                state.binding = program.scope.generateUid('env');
                state.insertion = program.getChildList('directives').at(-1)?.end ?? 0;
            }),
            nox.defineVisitor('StaticMemberExpression|ComputedMemberExpression', {
                fields: [
                    'memberPath',
                    'property'
                ],
                where: {
                    write: {
                        equals: false
                    }
                },
                scope: true
            }, (member, state)=>{
                const name = (0, _processenv.staticProcessEnvName)(member.node);
                if (name === null || !name.startsWith('EXPO_PUBLIC_')) return;
                if (member.scope.hasBinding('process')) return;
                state.names.add(name);
                member.replaceWith({
                    code: `${state.binding}.env.${name}`,
                    mapping: 'anchor-boundaries'
                });
            })
        ],
        post (context, state) {
            context.metadata.set('publicEnvVars', [
                ...state.names
            ]);
            if (state.names.size === 0) return;
            context.editor.appendRight(state.insertion, `${state.insertion === 0 ? '' : '\n'}var ${state.binding}=require("expo/virtual/env");\n`);
        }
    });
}

//# sourceMappingURL=development-public-env.js.map
