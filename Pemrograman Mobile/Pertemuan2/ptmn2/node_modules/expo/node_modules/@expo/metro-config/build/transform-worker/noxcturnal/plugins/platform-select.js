"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "createPlatformSelectPlugin", {
    enumerable: true,
    get: function() {
        return createPlatformSelectPlugin;
    }
});
function createPlatformSelectPlugin(nox, platform) {
    return nox.defineNativePlugin({
        name: 'expo-minify-platform-select',
        visitors: [
            nox.defineVisitor('CallExpression', {
                scope: true,
                children: {
                    callee: {
                        route: 'Expression',
                        fields: [
                            'memberPath',
                            'name'
                        ]
                    },
                    arguments: {
                        route: 'ObjectExpression',
                        children: {
                            properties: {
                                route: 'ObjectProperty',
                                fields: [
                                    'key',
                                    'computed'
                                ],
                                children: {
                                    value: {
                                        route: 'Expression'
                                    }
                                }
                            }
                        }
                    }
                }
            }, (call)=>{
                const callee = call.getChild('callee');
                if (!callee?.matchesPattern('Platform.select') || call.scope.hasBinding('Platform')) return;
                const argument = call.getChildList('arguments')[0];
                if (!argument?.is('ObjectExpression')) return;
                const properties = argument.getChildList('properties');
                if (properties.some((property)=>property.node.type !== 'ObjectProperty' || property.node.computed === true)) {
                    return;
                }
                const find = (key)=>{
                    for(let index = properties.length - 1; index >= 0; index--){
                        const property = properties[index];
                        if (String(property.node.key) === key) return property;
                    }
                    return undefined;
                };
                const selected = (platform ? find(platform) : undefined) ?? (platform === 'web' ? undefined : find('native')) ?? find('default');
                const value = selected?.getChild('value');
                call.replaceWith(value ? value.getSource() : call.context.code.parseExpression('undefined'));
            })
        ]
    });
}

//# sourceMappingURL=platform-select.js.map
