"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "createExpoRouterServerExportsPlugin", {
    enumerable: true,
    get: function() {
        return createExpoRouterServerExportsPlugin;
    }
});
function _path() {
    const data = /*#__PURE__*/ _interop_require_default(require("path"));
    _path = function() {
        return data;
    };
    return data;
}
const _noxcturnaltransformer = require("../noxcturnal-transformer");
function _interop_require_default(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
function createExpoRouterServerExportsPlugin(nox) {
    return nox.defineNativePlugin({
        name: 'expo-router-server-exports',
        editEffect: 'bindings',
        contract: {
            metadata: {
                writes: [
                    'loaderReference',
                    'performConstantFolding'
                ]
            }
        },
        createState: (context)=>{
            const input = (0, _noxcturnaltransformer.expoPluginInput)(context);
            const routerRootOption = input.options.customTransformOptions?.routerRoot;
            const routerRoot = typeof routerRootOption === 'string' ? decodeURI(routerRootOption) : 'app';
            const absoluteRouterRoot = _path().default.isAbsolute(routerRoot) ? routerRoot : _path().default.join(input.projectRoot, routerRoot);
            return {
                input,
                enabled: (0, _noxcturnaltransformer.isPathInsideRoot)(absoluteRouterRoot, input.filename),
                isLoaderBundle: String(input.options.customTransformOptions?.isLoaderBundle) === 'true',
                isServer: input.options.customTransformOptions?.environment === 'node',
                performConstantFolding: false
            };
        },
        visitors: [
            nox.defineVisitor('ExportDefaultDeclaration', {}, (exportPath, state)=>{
                if (!state.enabled) return;
                if (!state.isLoaderBundle) return;
                exportPath.remove();
                state.performConstantFolding = true;
            }),
            nox.defineVisitor('ExportNamedDeclaration', {
                fields: [
                    'source',
                    'exportKind'
                ],
                children: {
                    declaration: {
                        route: 'FunctionDeclaration|VariableDeclaration',
                        fields: [
                            'nodeKind',
                            'name',
                            'kind'
                        ],
                        children: {
                            declarations: {
                                route: 'VariableDeclarator',
                                children: {
                                    id: {
                                        route: 'Identifier|ObjectPattern|ArrayPattern',
                                        fields: [
                                            'name'
                                        ]
                                    }
                                }
                            }
                        }
                    },
                    specifiers: {
                        route: 'ExportSpecifier'
                    }
                }
            }, (exportPath, state)=>{
                if (!state.enabled) return;
                if (exportPath.node.source || exportPath.node.exportKind && exportPath.node.exportKind !== 'value' || exportPath.getChildList('specifiers').length > 0) {
                    return;
                }
                const declaration = exportPath.getChild('declaration');
                if (!declaration) return;
                if (declaration.node.type === 'Function') {
                    const name = declaration.node.name;
                    if (typeof name !== 'string') {
                        throw declaration.buildCodeFrameError('FunctionDeclaration route omitted its parsed declaration name');
                    }
                    if (name === 'loader') {
                        state.loaderReference = _path().default.resolve(state.input.projectRoot, state.input.filename);
                    }
                    const remove = state.isLoaderBundle ? name !== 'loader' : name === 'loader' || !state.isServer && name === 'generateMetadata';
                    if (remove) {
                        exportPath.remove();
                        state.performConstantFolding = true;
                    }
                    return;
                }
                if (declaration.node.type !== 'VariableDeclaration') return;
                const declarators = declaration.getChildList('declarations');
                const retained = declarators.filter((declarator)=>{
                    const id = declarator.getChild('id');
                    const name = id?.node.type === 'Identifier' ? id.node.name : undefined;
                    if (name === 'loader') {
                        state.loaderReference = _path().default.resolve(state.input.projectRoot, state.input.filename);
                    }
                    return state.isLoaderBundle ? name === 'loader' : name !== 'loader' && (state.isServer || name !== 'generateMetadata');
                });
                if (retained.length === declarators.length) return;
                state.performConstantFolding = true;
                if (retained.length === 0) {
                    exportPath.remove();
                } else {
                    const kind = declaration.node.kind;
                    if (typeof kind !== 'string') {
                        throw declaration.buildCodeFrameError('VariableDeclaration route omitted its parsed declaration kind');
                    }
                    exportPath.replaceWith({
                        kind: 'composite',
                        parts: [
                            `export ${kind} `,
                            ...retained.flatMap((declarator, index)=>[
                                    ...index === 0 ? [] : [
                                        ', '
                                    ],
                                    declarator.getSource()
                                ]),
                            ';'
                        ]
                    });
                }
            })
        ],
        post (context, state) {
            if (!state.enabled) return;
            if (state.loaderReference) {
                context.metadata.set('loaderReference', state.loaderReference);
            }
            if (state.performConstantFolding) {
                context.metadata.set('performConstantFolding', true);
            }
        }
    });
}

//# sourceMappingURL=expo-router-server-exports.js.map
