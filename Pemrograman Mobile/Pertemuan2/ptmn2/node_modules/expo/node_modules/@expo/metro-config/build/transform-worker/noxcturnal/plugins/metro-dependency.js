"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
0 && (module.exports = {
    createMetroDependencyPlugin: null,
    registerNativeEsmDependency: null
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: Object.getOwnPropertyDescriptor(all, name).get
    });
}
_export(exports, {
    get createMetroDependencyPlugin () {
        return createMetroDependencyPlugin;
    },
    get registerNativeEsmDependency () {
        return registerNativeEsmDependency;
    }
});
function _nodecrypto() {
    const data = require("node:crypto");
    _nodecrypto = function() {
        return data;
    };
    return data;
}
function _noxcturnal() {
    const data = require("noxcturnal");
    _noxcturnal = function() {
        return data;
    };
    return data;
}
const _noxcturnaltransformer = require("../noxcturnal-transformer");
const OPTIONAL_DEPENDENCY_ANCESTRY = {
    mode: 'findParent',
    routes: {
        MemberExpression: {
            fields: [
                'computed',
                'property'
            ],
            children: {
                object: {
                    route: 'Expression'
                }
            }
        },
        CallExpression: {
            fields: [
                'optional',
                'argumentCount'
            ],
            children: {
                callee: {
                    route: 'Expression'
                },
                arguments: {
                    route: 'Expression',
                    fields: [
                        'name',
                        'value'
                    ]
                }
            }
        },
        Statement: {},
        TryStatement: {
            children: {
                block: {
                    route: 'BlockStatement'
                }
            }
        }
    }
};
function isOptionalMetroDependency(input, name, path, checkPromiseChain = false) {
    if (name === input.config.asyncRequireModulePath) return false;
    const setting = input.config.allowOptionalDependencies;
    if (!setting || typeof setting === 'object' && setting.exclude.includes(name)) {
        return false;
    }
    let promiseChainActive = checkPromiseChain;
    let expectedObjectId = path.id;
    let memberId = null;
    let method = null;
    let statementCount = 0;
    let tryBlockResolved = false;
    for(let ancestor = path.parentPath; ancestor; ancestor = ancestor.parentPath){
        if (promiseChainActive) {
            if (memberId == null) {
                if ((ancestor.node.type === 'StaticMemberExpression' || ancestor.node.type === 'ComputedMemberExpression') && ancestor.node.computed === false && ancestor.getChild('object')?.id === expectedObjectId) {
                    memberId = ancestor.id;
                    method = ancestor.node.property;
                } else {
                    promiseChainActive = false;
                }
            } else {
                if (ancestor.node.type === 'CallExpression' && ancestor.node.optional !== true && ancestor.getChild('callee')?.id === memberId) {
                    const callback = method === 'catch' && (ancestor.node.argumentCount ?? 0) >= 1 ? ancestor.getChildList('arguments')[0] : method === 'then' && (ancestor.node.argumentCount ?? 0) >= 2 ? ancestor.getChildList('arguments')[1] : null;
                    if (callback != null && callback.node.type !== 'NullLiteral' && !(callback.node.type === 'Identifier' && callback.node.name === 'undefined')) {
                        return true;
                    }
                    expectedObjectId = ancestor.id;
                    memberId = null;
                    method = null;
                } else {
                    promiseChainActive = false;
                }
            }
        }
        if (!tryBlockResolved && (0, _noxcturnal().isStatementType)(ancestor.node.type)) {
            if (ancestor.node.type === 'BlockStatement') {
                tryBlockResolved = true;
                const parent = ancestor.parentPath;
                if (parent?.node.type === 'TryStatement' && parent.getChild('block')?.id === ancestor.id) {
                    return true;
                }
            } else if (++statementCount >= 3) {
                tryBlockResolved = true;
            }
        }
        if (!promiseChainActive && tryBlockResolved) return false;
    }
    return false;
}
function reconcileDependencyOptionality(state, key, dependency, optional) {
    if (dependency.data.isOptional && !optional) {
        const required = {
            ...dependency,
            data: {
                ...dependency.data,
                isOptional: false
            }
        };
        state.dependencies.set(key, required);
        return required;
    }
    return dependency;
}
function isIgnoredDynamicImport(path) {
    return path.getComments('inner').some((comment)=>/@metro-ignore|webpackIgnore\s*:\s*true/.test(comment.value));
}
function registerCommonJsDependency(state, name, location, optional = false) {
    const key = `${name}\0require`;
    let dependency = state.dependencies.get(key);
    if (!dependency) {
        dependency = {
            name,
            index: state.dependencies.size,
            imports: 0,
            data: {
                key: dependencyKeyHash(key),
                asyncType: null,
                isESMImport: false,
                ...optional ? {
                    isOptional: true
                } : {},
                locs: [],
                exportNames: [
                    '*'
                ]
            }
        };
        state.dependencies.set(key, dependency);
    } else {
        dependency = reconcileDependencyOptionality(state, key, dependency, optional);
    }
    dependency.imports++;
    dependency.data.locs.push(location);
    return dependency;
}
function registerNativeEsmDependency(state, name, location, exportNames = [
    '*'
]) {
    const key = `${name}\0import`;
    let dependency = state.dependencies.get(key);
    if (!dependency) {
        dependency = {
            name,
            index: state.dependencies.size,
            imports: 0,
            data: {
                key: dependencyKeyHash(key),
                asyncType: null,
                isESMImport: true,
                locs: [],
                exportNames: []
            },
            exportNameSet: new Set(exportNames)
        };
        state.dependencies.set(key, dependency);
    } else {
        for (const exportName of exportNames){
            dependency.exportNameSet.add(exportName);
        }
    }
    dependency.imports++;
    dependency.data.locs.push(location);
    return dependency;
}
function dependencyKeyHash(key) {
    return (0, _nodecrypto().createHash)('sha1').update(key).digest('base64');
}
function createMetroDependencyPlugin(nox) {
    const nativeRequire = (context, state, index, name, keepName = false)=>{
        const { input } = (0, _noxcturnaltransformer.metroPluginData)(context);
        return `${state.requireName}(${state.dependencyMapName}[${index}]${input.options.dev || keepName ? `, ${JSON.stringify(name)}` : ''})`;
    };
    return nox.defineNativePlugin({
        name: 'metro-collect-static-commonjs-dependencies',
        contract: {
            queries: [
                {
                    globals: [
                        {
                            name: '_$$_IMPORT_DEFAULT'
                        },
                        {
                            name: '_$$_IMPORT_ALL'
                        }
                    ]
                }
            ],
            metadata: {
                writes: [
                    'dependencies',
                    'dependencyMapName',
                    'metroRequireName',
                    'metroImportDefaultName',
                    'metroImportAllName'
                ]
            }
        },
        pre (context) {
            // One declared query seeds both program-level global checks. The scope
            // facade then serves hasGlobal() from the session cache instead of making
            // two synchronous native calls for every module.
            context.query({
                globals: [
                    {
                        name: '_$$_IMPORT_DEFAULT'
                    },
                    {
                        name: '_$$_IMPORT_ALL'
                    }
                ]
            });
        },
        createState: (context)=>{
            const { shared } = (0, _noxcturnaltransformer.metroPluginData)(context);
            shared.sawImportSyntax = false;
            return shared.state = {
                dependencies: new Map(),
                requireName: 'require',
                importDefaultName: '$$_IMPORT_DEFAULT',
                importAllName: '$$_IMPORT_ALL',
                dependencyMapName: 'dependencyMap'
            };
        },
        visitors: [
            nox.defineVisitor('Program', {
                scope: true
            }, {
                enter (program, state) {
                    const { input, normalizePseudoGlobals } = (0, _noxcturnaltransformer.metroPluginData)(program.context);
                    if (normalizePseudoGlobals) {
                        state.requireName = program.scope.hasBinding('r') ? program.scope.generateUid('r') : 'r';
                        state.importDefaultName = program.scope.hasBinding('i') ? program.scope.generateUid('i') : 'i';
                        state.importAllName = program.scope.hasBinding('a') ? program.scope.generateUid('a') : 'a';
                    } else if (input.config.unstable_disableModuleWrapping !== true) {
                        state.requireName = input.config.unstable_renameRequire === false ? 'require' : program.scope.generateUid('$$_REQUIRE');
                        state.importDefaultName = program.scope.hasGlobal('_$$_IMPORT_DEFAULT') ? '_$$_IMPORT_DEFAULT' : program.scope.generateUid('$$_IMPORT_DEFAULT');
                        state.importAllName = program.scope.hasGlobal('_$$_IMPORT_ALL') ? '_$$_IMPORT_ALL' : program.scope.generateUid('$$_IMPORT_ALL');
                    }
                    const reservedDependencyMap = input.config.unstable_dependencyMapReservedName;
                    if (reservedDependencyMap && program.scope.hasBinding(reservedDependencyMap)) {
                        program.unsupported('reserved-dependency-map-name-collision');
                    }
                    state.dependencyMapName = reservedDependencyMap ?? (normalizePseudoGlobals && !program.scope.hasBinding('d') ? 'd' : program.scope.generateUid('dependencyMap'));
                }
            }),
            nox.defineVisitor('ImportExpression', {
                ancestry: OPTIONAL_DEPENDENCY_ANCESTRY,
                comments: 'inner',
                children: {
                    source: {
                        route: 'StringLiteral',
                        fields: [
                            'value'
                        ]
                    }
                }
            }, (call, state)=>{
                const { input, collectOnly } = (0, _noxcturnaltransformer.metroPluginData)(call.context);
                if (isIgnoredDynamicImport(call)) return;
                const source = call.getChild('source');
                const sourceValue = source?.node.value;
                if (source?.node.type !== 'StringLiteral' || typeof sourceValue !== 'string') {
                    call.unsupported('dynamic-async-dependency');
                    return;
                }
                const name = sourceValue;
                const optional = isOptionalMetroDependency(input, name, call, true);
                const targetKey = `${name}\0import\0async`;
                let target = state.dependencies.get(targetKey);
                if (!target) {
                    target = {
                        name,
                        index: state.dependencies.size,
                        imports: 0,
                        data: {
                            key: dependencyKeyHash(targetKey),
                            asyncType: 'async',
                            isESMImport: true,
                            ...optional ? {
                                isOptional: true
                            } : {},
                            locs: [],
                            exportNames: [
                                '*'
                            ]
                        }
                    };
                    state.dependencies.set(targetKey, target);
                } else {
                    target = reconcileDependencyOptionality(state, targetKey, target, optional);
                }
                target.imports++;
                target.data.locs.push(call.getLocation());
                const runtimeName = input.config.asyncRequireModulePath;
                const runtimeKey = `${runtimeName}\0require`;
                let runtime = state.dependencies.get(runtimeKey);
                if (!runtime) {
                    runtime = {
                        name: runtimeName,
                        index: state.dependencies.size,
                        imports: 0,
                        data: {
                            key: dependencyKeyHash(runtimeKey),
                            asyncType: null,
                            isESMImport: false,
                            locs: [],
                            exportNames: [
                                '*'
                            ]
                        }
                    };
                    state.dependencies.set(runtimeKey, runtime);
                }
                runtime.imports++;
                runtime.data.locs.push(call.getLocation());
                if (!collectOnly && input.config.unstable_disableModuleWrapping !== true) {
                    call.replaceWith({
                        code: `${nativeRequire(call.context, state, runtime.index, runtimeName)}(${state.dependencyMapName}[${target.index}], ${state.dependencyMapName}.paths, ${JSON.stringify(name)})`,
                        mapping: 'anchor-boundaries'
                    });
                }
            }),
            nox.defineVisitor('NewExpression', {
                fields: [
                    'calleeName',
                    'argumentCount'
                ]
            }, (worker, state)=>{
                const callee = worker.node.calleeName;
                if (callee !== 'Worker' && callee !== 'SharedWorker') return;
                const source = worker.getSource().text();
                const match = source.match(/^(new\s+(?:Worker|SharedWorker)\s*\(\s*new\s+URL\s*\(\s*)(['"])([^'"]+)\2/);
                if (!match) return;
                const name = match[3];
                const { input, collectOnly } = (0, _noxcturnaltransformer.metroPluginData)(worker.context);
                const targetKey = `${name}\0require\0worker`;
                let target = state.dependencies.get(targetKey);
                if (!target) {
                    target = {
                        name,
                        index: state.dependencies.size,
                        imports: 0,
                        data: {
                            key: dependencyKeyHash(targetKey),
                            asyncType: 'worker',
                            isESMImport: false,
                            locs: [],
                            exportNames: [
                                '*'
                            ]
                        }
                    };
                    state.dependencies.set(targetKey, target);
                }
                target.imports++;
                target.data.locs.push(worker.getLocation());
                const runtimeName = input.config.asyncRequireModulePath;
                const runtimeKey = `${runtimeName}\0require`;
                let runtime = state.dependencies.get(runtimeKey);
                if (!runtime) {
                    runtime = {
                        name: runtimeName,
                        index: state.dependencies.size,
                        imports: 0,
                        data: {
                            key: dependencyKeyHash(runtimeKey),
                            asyncType: null,
                            isESMImport: false,
                            locs: [],
                            exportNames: [
                                '*'
                            ]
                        }
                    };
                    state.dependencies.set(runtimeKey, runtime);
                }
                runtime.imports++;
                runtime.data.locs.push(worker.getLocation());
                if (collectOnly) return;
                const requireRuntime = nativeRequire(worker.context, state, runtime.index, runtimeName);
                const resolved = `${requireRuntime}.unstable_resolve(${state.dependencyMapName}[${target.index}], ${state.dependencyMapName}.paths)`;
                const rewritten = source.replace(/^new\s+(?:Worker|SharedWorker)/, `new (${requireRuntime}.unstable_createWorker)`).replace(`${match[2]}${name}${match[2]}`, resolved);
                worker.replaceWith({
                    code: rewritten,
                    mapping: 'anchor-boundaries'
                });
            }),
            nox.defineVisitor('CallExpression', {
                fields: [
                    'calleeName',
                    'calleePath',
                    'calleeGlobal',
                    'staticArgument',
                    'argumentCount'
                ],
                comments: 'inner',
                where: {
                    calleePath: {
                        oneOf: [
                            '__noxcturnalImport',
                            'require.context',
                            'require.resolveWeak',
                            'require.unstable_resolveWorker',
                            'import',
                            '__prefetchImport',
                            'require.unstable_importMaybeSync',
                            'require'
                        ]
                    }
                },
                ancestry: OPTIONAL_DEPENDENCY_ANCESTRY,
                children: {
                    arguments: {
                        route: 'Expression',
                        fields: [
                            'name',
                            'value',
                            'pattern',
                            'flags'
                        ]
                    }
                }
            }, (call, state)=>{
                const { input, collectOnly } = (0, _noxcturnaltransformer.metroPluginData)(call.context);
                const callee = String(call.node.calleePath ?? call.node.calleeName ?? '');
                const isGlobalRequire = call.node.calleeGlobal === true;
                if (callee === '__noxcturnalImport' && call.node.calleeGlobal === true) {
                    if (call.node.argumentCount !== 1 || typeof call.node.staticArgument !== 'string') {
                        call.unsupported('invalid-native-esm-import');
                    }
                    const name = call.node.staticArgument;
                    const dependency = registerNativeEsmDependency(state, name, call.getLocation());
                    if (!collectOnly) {
                        call.replaceWith({
                            code: nativeRequire(call.context, state, dependency.index, name),
                            mapping: 'anchor-boundaries'
                        });
                    }
                    return;
                }
                if (callee === 'require.context' && isGlobalRequire && input.config.unstable_allowRequireContext === true) {
                    const args = call.getChildList('arguments');
                    const directory = args[0]?.node.value;
                    if (args.length < 1 || args.length > 4 || typeof directory !== 'string') {
                        call.unsupported('unsupported-require-context-arguments');
                    }
                    const recursiveArg = args[1];
                    const recursive = recursiveArg == null || recursiveArg.node.type === 'Identifier' && recursiveArg.node.name === 'undefined' ? true : recursiveArg.node.type === 'BooleanLiteral' && typeof recursiveArg.node.value === 'boolean' ? recursiveArg.node.value : null;
                    if (recursive == null) call.unsupported('unsupported-require-context-recursive');
                    let pattern = '.*';
                    let flags = '';
                    const filterArg = args[2];
                    if (filterArg && !(filterArg.node.type === 'Identifier' && filterArg.node.name === 'undefined')) {
                        if (filterArg.node.type !== 'RegExpLiteral') {
                            call.unsupported('unsupported-require-context-filter');
                        }
                        if (typeof filterArg.node.pattern !== 'string' || typeof filterArg.node.flags !== 'string') {
                            call.unsupported('unsupported-require-context-filter');
                        }
                        pattern = filterArg.node.pattern;
                        flags = filterArg.node.flags;
                    }
                    const modeArg = args[3];
                    const modeValue = modeArg?.node.value;
                    const mode = modeArg == null || modeArg.node.type === 'Identifier' && modeArg.node.name === 'undefined' ? 'sync' : typeof modeValue === 'string' && [
                        'sync',
                        'eager',
                        'lazy',
                        'lazy-once'
                    ].includes(modeValue) ? modeValue : null;
                    if (mode == null) call.unsupported('unsupported-require-context-mode');
                    const optional = isOptionalMetroDependency(input, directory, call);
                    const key = `${directory}\0require\0context\0${recursive}\0${pattern}\0${flags}\0${mode}`;
                    let dependency = state.dependencies.get(key);
                    if (!dependency) {
                        dependency = {
                            name: directory,
                            index: state.dependencies.size,
                            imports: 0,
                            data: {
                                key: dependencyKeyHash(key),
                                asyncType: null,
                                isESMImport: false,
                                ...optional ? {
                                    isOptional: true
                                } : {},
                                locs: [],
                                exportNames: [
                                    '*'
                                ],
                                contextParams: {
                                    recursive: recursive,
                                    filter: {
                                        pattern,
                                        flags
                                    },
                                    mode: mode
                                }
                            }
                        };
                        state.dependencies.set(key, dependency);
                    } else {
                        dependency = reconcileDependencyOptionality(state, key, dependency, optional);
                    }
                    dependency.imports++;
                    dependency.data.locs.push(call.getLocation());
                    if (!collectOnly) {
                        call.replaceWith({
                            code: nativeRequire(call.context, state, dependency.index, directory, optional),
                            mapping: 'anchor-boundaries'
                        });
                    }
                    return;
                }
                if (callee === 'require.resolveWeak' && isGlobalRequire) {
                    if (call.node.argumentCount !== 1 || typeof call.node.staticArgument !== 'string') {
                        call.unsupported('invalid-require-resolve-weak');
                    }
                    const name = call.node.staticArgument;
                    const optional = isOptionalMetroDependency(input, name, call);
                    const key = `${name}\0require\0weak`;
                    let dependency = state.dependencies.get(key);
                    if (!dependency) {
                        dependency = {
                            name,
                            index: state.dependencies.size,
                            imports: 0,
                            data: {
                                key: dependencyKeyHash(key),
                                asyncType: 'weak',
                                isESMImport: false,
                                ...optional ? {
                                    isOptional: true
                                } : {},
                                locs: [],
                                exportNames: [
                                    '*'
                                ]
                            }
                        };
                        state.dependencies.set(key, dependency);
                    } else {
                        dependency = reconcileDependencyOptionality(state, key, dependency, optional);
                    }
                    dependency.imports++;
                    dependency.data.locs.push(call.getLocation());
                    if (!collectOnly) {
                        call.replaceWith({
                            code: `${state.dependencyMapName}[${dependency.index}]`,
                            mapping: 'anchor-boundaries'
                        });
                    }
                    return;
                }
                if (callee === 'require.unstable_resolveWorker' && isGlobalRequire) {
                    if (call.node.argumentCount !== 1 || typeof call.node.staticArgument !== 'string') {
                        call.unsupported('invalid-require-resolve-worker');
                    }
                    const name = call.node.staticArgument;
                    const optional = isOptionalMetroDependency(input, name, call);
                    const targetKey = `${name}\0require\0worker`;
                    let target = state.dependencies.get(targetKey);
                    if (!target) {
                        target = {
                            name,
                            index: state.dependencies.size,
                            imports: 0,
                            data: {
                                key: dependencyKeyHash(targetKey),
                                asyncType: 'worker',
                                isESMImport: false,
                                ...optional ? {
                                    isOptional: true
                                } : {},
                                locs: [],
                                exportNames: [
                                    '*'
                                ]
                            }
                        };
                        state.dependencies.set(targetKey, target);
                    } else {
                        target = reconcileDependencyOptionality(state, targetKey, target, optional);
                    }
                    target.imports++;
                    target.data.locs.push(call.getLocation());
                    const runtimeName = input.config.asyncRequireModulePath;
                    const runtimeKey = `${runtimeName}\0require`;
                    let runtime = state.dependencies.get(runtimeKey);
                    if (!runtime) {
                        runtime = {
                            name: runtimeName,
                            index: state.dependencies.size,
                            imports: 0,
                            data: {
                                key: dependencyKeyHash(runtimeKey),
                                asyncType: null,
                                isESMImport: false,
                                locs: [],
                                exportNames: [
                                    '*'
                                ]
                            }
                        };
                        state.dependencies.set(runtimeKey, runtime);
                    }
                    runtime.imports++;
                    runtime.data.locs.push(call.getLocation());
                    if (!collectOnly) {
                        call.replaceWith({
                            code: `${nativeRequire(call.context, state, runtime.index, runtimeName)}.unstable_resolve(${state.dependencyMapName}[${target.index}], ${state.dependencyMapName}.paths)`,
                            mapping: 'anchor-boundaries'
                        });
                    }
                    return;
                }
                const asyncType = callee === 'import' ? 'async' : callee === '__prefetchImport' && call.node.calleeGlobal === true ? 'prefetch' : callee === 'require.unstable_importMaybeSync' && isGlobalRequire ? 'maybeSync' : null;
                if (asyncType) {
                    if (callee === 'import' && isIgnoredDynamicImport(call)) return;
                    if (call.node.argumentCount !== 1 || typeof call.node.staticArgument !== 'string') {
                        call.unsupported(`dynamic-${asyncType}-dependency`);
                    }
                    const name = call.node.staticArgument;
                    const optional = isOptionalMetroDependency(input, name, call, true);
                    const targetKey = `${name}\0import\0${asyncType}`;
                    let target = state.dependencies.get(targetKey);
                    if (!target) {
                        target = {
                            name,
                            index: state.dependencies.size,
                            imports: 0,
                            data: {
                                key: dependencyKeyHash(targetKey),
                                asyncType,
                                isESMImport: true,
                                ...optional ? {
                                    isOptional: true
                                } : {},
                                locs: [],
                                exportNames: [
                                    '*'
                                ]
                            }
                        };
                        state.dependencies.set(targetKey, target);
                    } else {
                        target = reconcileDependencyOptionality(state, targetKey, target, optional);
                    }
                    target.imports++;
                    target.data.locs.push(call.getLocation());
                    const runtimeName = input.config.asyncRequireModulePath;
                    const runtimeKey = `${runtimeName}\0require`;
                    let runtime = state.dependencies.get(runtimeKey);
                    if (!runtime) {
                        runtime = {
                            name: runtimeName,
                            index: state.dependencies.size,
                            imports: 0,
                            data: {
                                key: dependencyKeyHash(runtimeKey),
                                asyncType: null,
                                isESMImport: false,
                                locs: [],
                                exportNames: [
                                    '*'
                                ]
                            }
                        };
                        state.dependencies.set(runtimeKey, runtime);
                    }
                    runtime.imports++;
                    runtime.data.locs.push(call.getLocation());
                    const runtimeRequire = nativeRequire(call.context, state, runtime.index, runtimeName);
                    const method = asyncType === 'prefetch' ? '.prefetch' : asyncType === 'maybeSync' ? '.unstable_importMaybeSync' : '';
                    if (!collectOnly) {
                        call.replaceWith({
                            code: `${runtimeRequire}${method}(${state.dependencyMapName}[${target.index}], ${state.dependencyMapName}.paths, ${JSON.stringify(name)})`,
                            mapping: 'anchor-boundaries'
                        });
                    }
                    return;
                }
                if (callee === 'require.context' && input.config.unstable_allowRequireContext === true || callee === '__prefetchImport' && call.node.calleeGlobal === true || callee === 'require.unstable_importMaybeSync' && isGlobalRequire) {
                    call.unsupported(`unsupported-metro-dependency-call:${callee}`);
                }
                if (callee !== 'require' || call.node.calleeGlobal !== true) return;
                if (call.node.argumentCount !== 1 || typeof call.node.staticArgument !== 'string') {
                    call.unsupported('dynamic-require');
                }
                const name = call.node.staticArgument;
                const optional = isOptionalMetroDependency(input, name, call);
                const registered = registerCommonJsDependency(state, name, call.getLocation(), optional);
                if (!collectOnly && input.config.unstable_disableModuleWrapping !== true) {
                    const dependencyArgument = `${state.dependencyMapName}[${registered.index}]`;
                    const replacement = /^@babel\/runtime\/helpers\/[^/]+$/.test(name) ? `${state.importDefaultName}(${dependencyArgument}, ${JSON.stringify(name)})` : nativeRequire(call.context, state, registered.index, name, optional);
                    call.replaceWith({
                        code: replacement,
                        mapping: 'anchor-boundaries'
                    });
                }
            })
        ],
        post (context, state) {
            const { input, collectOnly, normalizePseudoGlobals } = (0, _noxcturnaltransformer.metroPluginData)(context);
            const dependencies = [
                ...state.dependencies.values()
            ].map(({ index, imports, exportNameSet, ...dependency })=>Object.freeze({
                    ...dependency,
                    data: Object.freeze({
                        ...dependency.data,
                        ...exportNameSet ? {
                            exportNames: [
                                ...exportNameSet
                            ]
                        } : {},
                        imports
                    })
                }));
            context.metadata.set('dependencies', dependencies);
            context.metadata.set('dependencyMapName', state.dependencyMapName);
            context.metadata.set('metroRequireName', state.requireName);
            context.metadata.set('metroImportDefaultName', state.importDefaultName);
            context.metadata.set('metroImportAllName', state.importAllName);
            if (!collectOnly && input.config.unstable_disableModuleWrapping !== true) {
                const pseudoGlobals = (0, _noxcturnaltransformer.metroPluginData)(context).pseudoGlobals;
                const parameters = [
                    normalizePseudoGlobals ? pseudoGlobals.global : 'global',
                    state.requireName,
                    state.importDefaultName,
                    state.importAllName,
                    normalizePseudoGlobals ? pseudoGlobals.module : 'module',
                    normalizePseudoGlobals ? pseudoGlobals.exports : 'exports',
                    state.dependencyMapName
                ];
                context.editor.prepend(`${input.config.globalPrefix}__d(function (${parameters.join(', ')}) {\n`);
                context.editor.append('\n});');
            }
        }
    });
}

//# sourceMappingURL=metro-dependency.js.map
