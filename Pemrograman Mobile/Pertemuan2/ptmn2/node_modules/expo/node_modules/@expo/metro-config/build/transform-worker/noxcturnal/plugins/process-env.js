"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
0 && (module.exports = {
    createProcessEnvPlugin: null,
    staticProcessEnvName: null
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: Object.getOwnPropertyDescriptor(all, name).get
    });
}
_export(exports, {
    get createProcessEnvPlugin () {
        return createProcessEnvPlugin;
    },
    get staticProcessEnvName () {
        return staticProcessEnvName;
    }
});
function _nodepath() {
    const data = /*#__PURE__*/ _interop_require_default(require("node:path"));
    _nodepath = function() {
        return data;
    };
    return data;
}
const _noxcturnaltransformer = require("../noxcturnal-transformer");
function _interop_require_default(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
const NO_PROCESS_ENV_REPLACEMENT = Symbol('NO_PROCESS_ENV_REPLACEMENT');
function staticProcessEnvName(node) {
    if (typeof node.property !== 'string' || node.memberPath !== `process.env.${node.property}`) {
        return null;
    }
    return node.property;
}
function routerRoot(state) {
    return state.absoluteRouterRoot ??= (()=>{
        const option = state.input.options.customTransformOptions?.routerRoot;
        const root = typeof option === 'string' ? decodeURI(option) : 'app';
        return _nodepath().default.isAbsolute(root) ? root : _nodepath().default.join(state.input.projectRoot, root);
    })();
}
function processEnvReplacement(name, state) {
    const { filename, options, projectRoot } = state.input;
    switch(name){
        case 'EXPO_PROJECT_ROOT':
            return projectRoot;
        case 'EXPO_ROUTER_ABS_APP_ROOT':
            return routerRoot(state);
        case 'EXPO_ROUTER_APP_ROOT':
            return _nodepath().default.relative(_nodepath().default.dirname(filename), routerRoot(state));
        case 'EXPO_ROUTER_IMPORT_MODE':
            return String(options.customTransformOptions?.asyncRoutes) === 'true' ? 'lazy' : 'sync';
        default:
            return !options.dev && (0, _noxcturnaltransformer.usesPublicEnvPlugin)(state.input) && name.startsWith('EXPO_PUBLIC_') ? process.env[name] : NO_PROCESS_ENV_REPLACEMENT;
    }
}
function createProcessEnvPlugin(nox) {
    return nox.defineNativePlugin({
        name: 'expo-process-env',
        createState: (context)=>({
                input: (0, _noxcturnaltransformer.expoPluginInput)(context)
            }),
        visitors: [
            nox.defineVisitor('StaticMemberExpression|ComputedMemberExpression', {
                fields: [
                    'memberPath',
                    'property'
                ],
                where: {
                    write: {
                        equals: false
                    }
                },
                scope: true
            }, (member, state)=>{
                const name = staticProcessEnvName(member.node);
                if (name === null) return;
                if (member.scope.hasBinding('process')) return;
                const replacement = processEnvReplacement(name, state);
                if (replacement === NO_PROCESS_ENV_REPLACEMENT) return;
                member.replaceWith((0, _noxcturnaltransformer.mappedLiteral)(member.context, replacement));
            })
        ]
    });
}

//# sourceMappingURL=process-env.js.map
