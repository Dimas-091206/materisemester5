"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "withAssetsAndroid", {
    enumerable: true,
    get: function() {
        return withAssetsAndroid;
    }
});
function _configplugins() {
    const data = require("expo/config-plugins");
    _configplugins = function() {
        return data;
    };
    return data;
}
function _fs() {
    const data = /*#__PURE__*/ _interop_require_default(require("fs"));
    _fs = function() {
        return data;
    };
    return data;
}
function _promises() {
    const data = /*#__PURE__*/ _interop_require_default(require("fs/promises"));
    _promises = function() {
        return data;
    };
    return data;
}
function _path() {
    const data = /*#__PURE__*/ _interop_require_default(require("path"));
    _path = function() {
        return data;
    };
    return data;
}
const _utils = require("./utils");
function _interop_require_default(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
const withAssetsAndroid = (config, assets)=>{
    return (0, _configplugins().withDangerousMod)(config, [
        'android',
        async (config)=>{
            const resolvedAssets = await (0, _utils.resolveAssetPaths)(assets, config.modRequest.projectRoot);
            const validAssets = (0, _utils.validateAssets)(resolvedAssets, 'android');
            validAssets.forEach((asset)=>{
                const assetsDir = getAssetDir(asset, config.modRequest.platformProjectRoot);
                _fs().default.mkdirSync(assetsDir, {
                    recursive: true
                });
            });
            await Promise.all(validAssets.map(async (asset)=>{
                const assetsDir = getAssetDir(asset, config.modRequest.platformProjectRoot);
                const output = _path().default.join(assetsDir, _path().default.basename(asset));
                await _promises().default.copyFile(asset, output);
            }));
            return config;
        }
    ]);
};
function getAssetDir(asset, root) {
    const assetPath = [
        'app',
        'src',
        'main',
        'assets'
    ];
    const resPath = [
        'app',
        'src',
        'main',
        'res'
    ];
    const ext = _path().default.extname(asset);
    if (_utils.IMAGE_TYPES.includes(ext)) {
        return _path().default.join(root, ...resPath, 'drawable');
    } else if (_utils.FONT_TYPES.includes(ext)) {
        return _path().default.join(root, ...assetPath, 'fonts');
    } else if (_utils.MEDIA_TYPES.includes(ext)) {
        return _path().default.join(root, ...resPath, 'raw');
    } else {
        return _path().default.join(root, ...assetPath);
    }
}

//# sourceMappingURL=withAssetsAndroid.js.map
