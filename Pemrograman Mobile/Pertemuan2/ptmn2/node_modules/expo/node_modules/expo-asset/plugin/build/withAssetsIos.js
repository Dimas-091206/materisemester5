"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "withAssetsIos", {
    enumerable: true,
    get: function() {
        return withAssetsIos;
    }
});
function _imageutils() {
    const data = require("@expo/image-utils");
    _imageutils = function() {
        return data;
    };
    return data;
}
function _configplugins() {
    const data = require("expo/config-plugins");
    _configplugins = function() {
        return data;
    };
    return data;
}
function _promises() {
    const data = /*#__PURE__*/ _interop_require_default(require("fs/promises"));
    _promises = function() {
        return data;
    };
    return data;
}
function _path() {
    const data = /*#__PURE__*/ _interop_require_default(require("path"));
    _path = function() {
        return data;
    };
    return data;
}
const _AssetContents = require("./AssetContents");
const _utils = require("./utils");
function _interop_require_default(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
const IMAGE_DIR = 'Images.xcassets';
const withAssetsIos = (config, assets)=>{
    config = addAssetsToTarget(config, assets);
    return config;
};
function addAssetsToTarget(config, assets) {
    return (0, _configplugins().withXcodeProject)(config, async (config)=>{
        const resolvedAssets = await (0, _utils.resolveAssetPaths)(assets, config.modRequest.projectRoot);
        const validAssets = (0, _utils.validateAssets)(resolvedAssets, 'ios');
        const project = config.modResults;
        const platformProjectRoot = config.modRequest.platformProjectRoot;
        _configplugins().IOSConfig.XcodeUtils.ensureGroupRecursively(project, 'Resources');
        const images = validAssets.filter((asset)=>_utils.IMAGE_TYPES.includes(_path().default.extname(asset)));
        const assetsForResourcesDir = validAssets.filter((asset)=>!_utils.IMAGE_TYPES.includes(_path().default.extname(asset)));
        await addImageAssets(images, config.modRequest.projectRoot);
        addResourceFiles(project, platformProjectRoot, assetsForResourcesDir);
        return config;
    });
}
function addResourceFiles(project, platformRoot, assets) {
    for (const asset of assets){
        const assetPath = _path().default.relative(platformRoot, asset);
        _configplugins().IOSConfig.XcodeUtils.addResourceFileToGroup({
            filepath: assetPath,
            groupName: 'Resources',
            project,
            isBuildFile: true,
            verbose: true
        });
    }
}
async function addImageAssets(assets, root) {
    const iosNamedProjectRoot = _configplugins().IOSConfig.Paths.getSourceRoot(root);
    for (const asset of assets){
        const name = _path().default.basename(asset, _path().default.extname(asset));
        const image = _path().default.basename(asset);
        const assetPath = _path().default.resolve(iosNamedProjectRoot, `${IMAGE_DIR}/${name}.imageset`);
        await _promises().default.mkdir(assetPath, {
            recursive: true
        });
        const buffer = await (0, _imageutils().generateImageAsync)({
            projectRoot: root
        }, {
            src: asset
        });
        await _promises().default.writeFile(_path().default.resolve(assetPath, image), buffer.source);
        await writeContentsJsonFileAsync({
            assetPath,
            image
        });
    }
}
async function writeContentsJsonFileAsync({ assetPath, image }) {
    const images = buildContentsJsonImages({
        image
    });
    await (0, _AssetContents.writeContentsJsonAsync)(assetPath, {
        images
    });
}
function buildContentsJsonImages({ image }) {
    return [
        {
            idiom: 'universal',
            filename: image,
            scale: '1x'
        },
        {
            idiom: 'universal',
            scale: '2x'
        },
        {
            idiom: 'universal',
            scale: '3x'
        }
    ];
}

//# sourceMappingURL=withAssetsIos.js.map
