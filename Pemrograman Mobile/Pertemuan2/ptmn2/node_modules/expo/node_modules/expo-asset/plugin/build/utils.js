"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
0 && (module.exports = {
    ACCEPTED_TYPES: null,
    FONT_TYPES: null,
    IMAGE_TYPES: null,
    MEDIA_TYPES: null,
    MODEL_TYPES: null,
    resolveAssetPaths: null,
    validateAssets: null
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: Object.getOwnPropertyDescriptor(all, name).get
    });
}
_export(exports, {
    get ACCEPTED_TYPES () {
        return ACCEPTED_TYPES;
    },
    get FONT_TYPES () {
        return FONT_TYPES;
    },
    get IMAGE_TYPES () {
        return IMAGE_TYPES;
    },
    get MEDIA_TYPES () {
        return MEDIA_TYPES;
    },
    get MODEL_TYPES () {
        return MODEL_TYPES;
    },
    get resolveAssetPaths () {
        return resolveAssetPaths;
    },
    get validateAssets () {
        return validateAssets;
    }
});
function _configplugins() {
    const data = require("expo/config-plugins");
    _configplugins = function() {
        return data;
    };
    return data;
}
function _promises() {
    const data = /*#__PURE__*/ _interop_require_default(require("fs/promises"));
    _promises = function() {
        return data;
    };
    return data;
}
function _path() {
    const data = /*#__PURE__*/ _interop_require_default(require("path"));
    _path = function() {
        return data;
    };
    return data;
}
function _interop_require_default(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
const IMAGE_TYPES = [
    '.png',
    '.jpg',
    '.gif'
];
const FONT_TYPES = [
    '.otf',
    '.ttf'
];
const MEDIA_TYPES = [
    '.mp4',
    '.mp3',
    '.lottie',
    '.riv'
];
const MODEL_TYPES = [
    '.glb'
];
const ACCEPTED_TYPES = [
    '.json',
    '.db',
    ...IMAGE_TYPES,
    ...MEDIA_TYPES,
    ...FONT_TYPES,
    ...MODEL_TYPES
];
async function resolveAssetPaths(assets, projectRoot) {
    const promises = assets.map(async (p)=>{
        const resolvedPath = _path().default.resolve(projectRoot, p);
        const stat = await _promises().default.stat(resolvedPath);
        if (stat.isDirectory()) {
            const dir = await _promises().default.readdir(resolvedPath);
            return dir.map((file)=>_path().default.join(resolvedPath, file));
        }
        return [
            resolvedPath
        ];
    });
    return (await Promise.all(promises)).flat();
}
function validateAssets(assets, platform) {
    return assets.filter((asset)=>{
        const ext = _path().default.extname(asset);
        const name = _path().default.basename(asset, ext);
        const isNameValid = platform === 'android' ? (0, _configplugins().isValidAndroidAssetName)(name) : true;
        const accepted = ACCEPTED_TYPES.includes(ext);
        const isFont = FONT_TYPES.includes(ext);
        if (!isNameValid) {
            _configplugins().WarningAggregator.addWarningForPlatform(platform, 'expo-asset', `\`${name}\` is not a supported asset name - file-based resource names must contain only lowercase a-z, 0-9, or underscore`);
            return;
        }
        if (!accepted) {
            _configplugins().WarningAggregator.addWarningForPlatform(platform, 'expo-asset', `\`${ext}\` is not a supported asset type`);
            return;
        }
        if (isFont) {
            _configplugins().WarningAggregator.addWarningForPlatform(platform, 'expo-asset', `Fonts are not supported with the \`expo-asset\` plugin. Use \`expo-font\` instead. Ignoring ${asset}`);
            return;
        }
        return asset;
    });
}

//# sourceMappingURL=utils.js.map
