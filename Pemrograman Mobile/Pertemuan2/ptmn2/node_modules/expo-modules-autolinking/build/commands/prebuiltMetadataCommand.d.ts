import type commander from 'commander';
/** Emits the prebuilt-modules metadata document (ENG-25370). */
export declare function prebuiltMetadataCommand(cli: commander.CommanderStatic): commander.Command;
